# MAPA_FUNCIONES.md

> **Revisión de la copia v3.20.0 (2026-09-24):** se contrastó este índice con el árbol entregado para edición. El árbol contiene **66 archivos JavaScript** distribuidos en `js/`; `asistente-bandeja.js` está presente en `js/core/` (no en `js/asistente/`). Este mapa se actualizó junto con los cambios de v3.20.0 descritos en la revisión funcional y en las secciones de cada módulo.

Índice de referencia rápida de qué exporta cada archivo `.js` del proyecto y para
qué sirve, para no tener que leer/grepear archivos completos en cada prompt. Solo se
documentan **exports** (lo que otros archivos pueden importar de acá) - funciones
privadas internas no aparecen.

Para la vista de alto nivel (capas, por qué existen los imports circulares, y "en
qué archivo empiezo si me piden X") ver `ARQUITECTURA.md`. Este documento es el
detalle función-por-función; `ARQUITECTURA.md` es el mapa de decisión.

> **Estado:** la copia v3.20.0 contiene 66 archivos `.js`; el índice de esta entrega se contrastó contra ese inventario y las modificaciones actuales se resumen en "Revisión funcional v3.20.0" más abajo. Las fechas y notas de las rondas anteriores se conservan como historial, no como estado vigente.

**2026-09-17 - Ronda de bugs del timer + Competencias (Partes 1 a 5 del prompt).** **Bug real de pérdida de tiempo, resuelto:** `revisarSesionOlvidadaAlAbrir()` (tiempo-estudio-timer.js) nunca volvía a levantar el timer en memoria al reabrir la app - sus 3 caminos terminaban en `localStorage.removeItem()`, así que cerrar/recargar la pestaña mataba la sesión activa en silencio; ahora existe `restaurarTimerDesdeSnapshot()` y el salvavidas dejó de ser destructivo. Además: avisos de fin de bloque que sí llegan con la app en 2do plano (Service Worker + alarma de fase), `saltarDescansoPomodoro()`, "Tiempo de Estudio" → "Tiempo" en la interfaz, ningún modal de la sección se cierra al tocar fuera, y 2 archivos nuevos - `tiempo-estudio-celebracion.js` (confeti/audio/aviso previo) y `tiempo-estudio-competencias-gestion.js` (menú Gestionar + Registro de finalizadas, separado porque `tiempo-estudio-competencias.js` ya pasaba las 1100 líneas). Del lado del Worker: 4 endpoints nuevos + 2 columnas D1 (`competencias.estado`, `participantes.horas_totales_historicas`, ver `migracion_competencias_gestion.sql`).

**2026-09-17 (misma fecha, ronda siguiente) - Auditoría de blindaje contra choques de versión / pérdida de sesión / concurrencia en Competencias, sin features nuevas.** Recorrida completa de las 4 superficies de riesgo del proyecto (motor de sync con Drive, merge/reloj de Lamport, sesión/token, Competencias en D1) buscando huecos donde el diseño existente se puede romper en la práctica. **Hallazgos reales arreglados** (detalle en cada sección de este mapa): (1) un cambio local hecho MIENTRAS una subida a Drive estaba en vuelo podía quedar marcado como sincronizado sin haber viajado nunca (`ejecutarUnaSincronizacion`, storage-sync.js); (2) 4 fallos de reconexión seguidos con cambios sin sincronizar forzaba un cierre de sesión que BORRA la caché local - el bug de pérdida de datos más serio de la ronda (`forzarCierreSesionPorFalloDeReconexion`, storage-sync.js); (3) dos pestañas del mismo navegador no se enteraban entre sí ni de una subida nueva ni de un cierre de sesión - nuevo `BroadcastChannel` (`inicializarCanalEntrePestanas`, storage-sync.js); (4) un timer de estudio corriendo no contaba como "cambio sin sincronizar" al cerrar sesión, porque vive fuera de `estado.datos` hasta que se detiene (`pedirConfirmacionCerrarSesion`, main.js); (5) el desempate de un conflicto de sync no era determinístico entre dispositivos - cada uno mostraba su propia versión como "la principal" (`marcarConflictoSiCorresponde`, storage-merge.js); (6) `fusionarPlanesEstudio` era la única función de fusión de nivel superior que no adelantaba el reloj lógico con planes que llegaban solo del lado remoto (bug de pérdida silenciosa, no pedido por ningún punto puntual - paridad con sus hermanas); (7) en el Worker, tres endpoints de Competencias tenían ventanas de carrera reales: `procesarCierreCompetencia` (cron vs. "Finalizar" simultáneos podían duplicar historial/horas - ahora usa compare-and-swap sobre `ultimo_cierre_en`), el DELETE de participante no era idempotente (ahora `changes === 0` es éxito, no 404), y `manejarActualizarHoras` chequeaba el estado en un SELECT separado del UPDATE (ahora es una sola sentencia con guard). **Sin cambios de contrato para el cliente en ninguno de los fixes del Worker.** **Puntos auditados sin hallazgo** (ya no hace falta re-auditarlos en un próximo prompt salvo que cambie el código de esa zona): backup rotativo (nunca sobreescribe con datos a medio fundir); Guarda 1 de `hayConflictoReal` (mismo valor final en ambos dispositivos → no dispara conflicto falso); edición vs. tumba en las 6 fusiones anidadas (la tumba siempre gana, en todas las colecciones); deduplicación de tumbas por id (nunca quedan duplicadas); una resolución de conflicto no vuelve a chocar contra la base vieja al viajar a otro dispositivo; deduplicación de refrescos de token en paralelo (`refrescoEnCurso`); reintento automático tras 401 en pleno vuelo (`conReintentoSi401`, ya cubría `leerDatos`/`guardarDatos`/`obtenerMetadatosArchivo`); la carrera de `onLoginExitoso` (nada puede escribir sobre el esqueleto de un usuario nuevo pisando el archivo real); `renombrar`/`apodo` en Competencias (ya eran un UPDATE de una sola sentencia, last-write-wins consistente entre 2 dispositivos del mismo creador). **Pendiente cerrado en ronda de seguimiento (mismo día):** `abrirModalEditarSesion` - corrección de referencia: vive en `tiempo-estudio/tiempo-estudio-registro.js`, no en `tiempo-estudio-sesiones.js` (ese archivo no existe en el proyecto). Era el único de los flujos de relectura de entidad viva (punto 2.3) que mutaba la `sesion` del closure in-place sin releerla. 🔧 Arreglado: al guardar, ahora relee `estado.datos.sesiones_estudio.find(s => s.id === sesion.id)` justo antes de mutar (mismo patrón que los 4 flujos de Gestionar Competencias); si la sesión ya no existe (borrada o cambiada en otra pestaña/dispositivo mientras el modal estaba abierto), avisa con un toast y cierra sin tocar nada. Ver entrada actualizada de `abrirModalEditarSesion` más abajo.

**2026-09-19 - Ronda de 3 partes: estado fantasma en Competencias (A), "sesión desaparecida" (B), vista de horas totales (C).** Sin migración SQL; **requiere redesplegar el Worker** (`index.js`) además del cliente. **A (urgente):** causa raíz - la entrada de `competencias_unidas` usa como `id` el de la COMPETENCIA (no el de la membresía), así que salir y volver a unirse reutilizaba el id, y la tumba de la salida (`fusionarColeccion` deja ganar SIEMPRE a la tumba; `fusionarTumbas` nunca purga) descartaba la alta nueva en el siguiente sync, en todos los dispositivos. Resultado: el Worker sí tenía la fila (200 en `/unirse`) pero el cliente perdía el puntero → "no estás" + 409 "ya sos parte" con horas congeladas. Fix en 3 capas: (1) altas con `unido_en` + `podarTumbasSuperadasPorAltas` (storage-merge.js) - una tumba con `eliminadoEn` menor que el `unido_en` de una alta viva se descarta y se purga; (2) Worker: `/unirse` idempotente (200 + `participante_id` existente en vez de 409); (3) `ejecutarSalidaCompetencia` ya no traga fallos del DELETE (antes dejaba filas fantasma en D1). **B:** 4 huecos reales - (a) `aplicarDatosRemotosFrescos` nunca repintaba Tiempo (ahora hook post-fusión registrado en `inicializarTiempoEstudio`); (b) tarjetas de materias sin meta no mostraban lo estudiado; (c) `toISOString().slice(0,10)` = fecha UTC: en Costa Rica desde las 6 pm el corte "Día" de Estadísticas abría en mañana con 0 min (`fechaLocalStr`); (d) hora con segundos en registro manual/edición guardaba `inicio = NaN` (invisible en todo filtro) - hueco latente, no confirmado como causa del caso reportado. El borrado de sesiones se reverificó (tumba `{id, eliminadoEn}`, no resucita en sync). **C:** sección "Horas totales" (hoy/semana/mes/año calendario, todas las materias) al inicio de Estadísticas. `tiempo-estudio-estadisticas.js` **se vio de verdad en esta ronda** (el pendiente de la ronda 2026-09-17 sobre documentarlo de memoria queda cerrado). Probado con Node + jsdom + el `fusionarDatos` real, con control negativo contra el código anterior.

**2026-09-19 (segunda ronda del día) - Rediseño del indicador de sesión activa ("el timer me estorba").** El badge fijo `#badge-tiempo-estudio` (abajo-IZQUIERDA, gradiente de acento, "⏱ IC-1010 · Cálculo I · 12:34") tapaba el perfil de la barra lateral. Ahora es UN componente con **3 presentaciones** que el CSS alterna por ancho de pantalla (mismos cortes que ya usa el layout, 900px y 1500px): **≥1500px** tarjeta anclada al FONDO de una columna derecha nueva (`.columna-derecha`, con los enlaces rápidos arriba); **900–1499px** tarjeta flotante abajo-derecha sobre el botón flotante de Enlaces (ahí no hay columna ni barra superior); **<900px** segunda línea bajo "App Académica" en `.mobile-topbar`. Diseño pedido: lucecita que parpadea del **color de la materia** (mismo criterio que Horario/Agenda: color propio > color de categoría > violeta por defecto), nombre **sin código**, tiempo; en pausa la luz queda fija y se agrega "En pausa"; en descanso de Pomodoro, "Descanso". Al tocarlo cualquiera de las 3 abre el detalle de la materia (ahí están pausar/reanudar/detener). **Archivos:** `tiempo-estudio-indicador.js` (NUEVO), `tiempo-estudio.js`, `index.html` (3 contenedores + wrapper `.columna-derecha` + wrapper `.mobile-topbar-centro`), `design-system.css` (se elimina `.badge-tiempo-estudio-fijo`; bloque nuevo `.timer-indicador*` en la sección "Tiempo de Estudio"). Verificado en Chromium real (Playwright) a 1594, 1200, 390 y 360px, modo oscuro y claro, con 3 y con 30 enlaces, con/sin Modo edición, y con jsdom para el cableado.

**2026-09-19 (tercera ronda) - Pomodoro: las fases ya NO avanzan solas + tiempo extra.** Pedido: "odio revisar la app y encontrarla en descanso infinito en lugar de estudiando". Antes, al cumplirse el tiempo de un bloque el motor guardaba la sesión y arrancaba el descanso solo (y al cumplirse el descanso volvía solo al trabajo). **Ahora el tiempo configurado es un hito, no un corte:** al cumplirse suena UN aviso y la fase SIGUE corriendo como **tiempo extra**; el cronómetro principal se queda clavado en la duración (ej. `40:00`) y el extra cuenta aparte (`+05:12`) en un renglón propio debajo, que reserva su espacio para que el principal no se mueva. Se aplica igual a estudio y a descanso. El extra de un bloque de trabajo **sí cuenta** como estudio (al tocar "Descanso" se guarda bloque + extra en una sola sesión); el extra de un descanso **nunca** suma. Solo cambia de fase un toque de la persona: "☕ Descanso" (aparece cuando el bloque cumplió su tiempo) y "✔ Terminar descanso" (el mismo botón que "⏭ Saltar descanso", que pasa a decir Terminar cuando el descanso ya cumplió su tiempo). Archivos tocados: `tiempo-estudio-timer.js` (se eliminó `avanzarFasePomodoro` y el bucle de "ponerse al día" de `tick()`; nuevos `tiempoDeFase`, `iniciarDescansoPomodoro`, campo `avisoFaseDisparado` en timer y snapshot), `tiempo-estudio.js` (detalle + `resolverInfoIndicadorTimer`) y `design-system.css` (`.te-timer-extra*`, `nowrap`/`wrap` en el indicador). `tiempo-estudio-indicador.js` e `index.html` NO cambian. Sin Worker ni SQL. **Consecuencia deliberada:** una fase de Pomodoro olvidada ahora SÍ llega al salvavidas de 3 h (antes las fases avanzaban y nunca lo alcanzaba).



**2026-09-19 (cuarta ronda) - Estadísticas: semestre por semanas, color por materia y tiempo real.** Sin Worker ni SQL. **(1) "Semestre sin datos":** "Horas estudiadas" y "Tendencia" armaban la ventana de cada semana a mano (`fecha_inicio + (i-1)*7`), desalineada de la "Semana N" que muestran Horario/Agenda cuando `fecha_inicio` no cae en lunes; ahora cada SESIÓN se clasifica con `calcularNumeroSemanaParaFecha` (misma fuente que numera la semana), y el eje muestra S1…S-actual. **(2) Color por materia:** "Tendencia" (semana y semestre) dibuja barras APILADAS, un tramo por materia con su color efectivo (propio > categoría > violeta), + leyenda. **(3) Tiempo real:** `tiempo-estudio-timer.js` exporta `notificarSesionesEstudioActualizadas()` (evento `te:sesiones-actualizadas` en `window`) y la llaman `detenerTimerEstudio`, `iniciarDescansoPomodoro`, las 2 salidas del salvavidas y los 3 puntos de `tiempo-estudio-registro.js` (registro manual, editar, borrar). Estadísticas escucha ese evento y además suma a lo que dibuja la sesión que el timer lleva EN CURSO (fase de estudio, incluido el tiempo extra; nunca descansos; congelada en pausa), con un latido por minuto. El repintado es en el sitio (cada sección se reemplaza a sí misma; conserva el scroll horizontal de la gráfica semestral y no toca una sección con un campo en foco). **Trampa a recordar:** `calcularMinutosTotalesEnRango` sigue leyendo SOLO sesiones guardadas (la usa `sincronizarHorasCompetencias`, que corre en `detenerTimerEstudio` antes de vaciar el timer); lo "en vivo" es opt-in con `incluirEnCurso`. Nuevo import circular intencional: estadísticas → timer → competencias → estadísticas (seguro: solo se usa dentro de funciones). Probado en Chromium real (Playwright) con reloj simulado: timer simple, pausa, detener (sin doble conteo), registro manual, Pomodoro (extra suma, descanso no), scroll semestral, foco en selector de fecha y materia ajena.

**2026-09-19 (quinta ronda) - Racha de estudio (estilo Duolingo).** Sin Worker ni SQL. **Regla final (la pidió así el usuario, no la del primer borrador):** un día "cumplido" = ≥30 min sumando TODAS las materias y orígenes; cada SEMANA (lun–dom) da **2 días de descanso** que se gastan cuando se quiera (o no); un 3.er día sin cumplir en la misma semana rompe la racha. Una racha que "nace" un jueves NO cuenta lun–mié como faltas (el recorrido arranca en el primer día con datos). Hoy es "pendiente": no gasta descanso hasta que termina. La racha vale los días CUMPLIDOS; los descansos no suman ni cortan. **Recuperación:** una racha de ≥30 días perdida se puede restaurar el día siguiente a la ruptura estudiando 90 min (ese día cuenta: largo+1); pasado ese día expira. **Es DERIVADA** (no se guarda ningún número): se recalcula desde `sesiones_estudio` (~1 ms con 1.000 sesiones, ~2 ms con 10.000, medido; no se cachea). **Corte histórico:** `RACHA_DESDE_ISO = "2026-09-14"` (lunes de la semana de estreno) - a quien ya venía estudiando esta semana se le da su racha sin arrastrar historial viejo ni ofrecer "recuperar" rachas de hace meses; consecuencia: **hasta ~15-oct no puede existir una racha recuperable** (hacen falta ≥30 días desde esa fecha); poner `null` para mirar todo el historial. **UI:** chip 🔥 + número en `construirEncabezado` (a la derecha del título, antes de ＋ y ⚙️) con 3 estados (encendida / pendiente -pulsa en rojo si "hoy es clave"- / apagada); avisos a pantalla completa con llama SVG animada, halo, chispas y **odómetro** (el número rueda de n-1 a n; 0→n con giro en los avisos): "Has iniciado una racha" (con casilla "No volver a mostrar"), "¡Racha recuperada!" y "¿Quieres recuperar tu racha?"; al subir la racha: pop del chip + toast. **Cuándo celebra:** solo lo escrito en ESTE dispositivo (evento `te:sesiones-actualizadas`) y solo cuando HOY pasa de sin-cumplir a cumplido; lo que llega por sync de otro dispositivo, editar/cargar un día viejo, o una 2.ª sesión el mismo día NO celebran. Al abrir la app hay una revisión única por día (oferta de recuperar, o "Has iniciado una racha" para quien ya traía racha viva sin haberla visto anunciada). **Archivos:** `tiempo-estudio-racha.js` (NUEVO, cálculo puro), `tiempo-estudio-racha-ui.js` (NUEVO, chip/avisos/coordinación), `tiempo-estudio.js` (import, chip en el encabezado, `inicializarRacha` + hook post-fusión), `schema.js` (2 preferencias en `configuracion`), `design-system.css` (bloque "Racha de estudio" al final). **Trampas a recordar:** (1) el `<input>` de `.checkbox` es `display:none` (no recibe foco con Tab) - la casilla de la racha (`.te-racha-check`) lo pisa con "invisible pero enfocable"; (2) las celdas del odómetro miden 1.3em (con 1em justo, la tinta del dígito vecino se asomaba como un filo bajo el número); (3) durante el día de recuperación (30–89 min) NO se muestra "Has iniciado una racha": solo un toast con el avance. Probado con Node en 6 zonas horarias (Costa Rica, Nueva York, Madrid, Auckland, Kolkata, UTC; incluye cambio de horario de verano) y en Chromium real (Playwright): estados del chip, aviso de inicio, casilla + teclado, racha que sube, 9→10, 0→137, recuperación completa (35→36), oscuro/claro, otra paleta y "reducir movimiento".

**2026-09-19 (quinta ronda, seguimiento) - Tocar el chip 🔥 ya NO lanza un toast: abre una ventana de detalle.** Mismo lenguaje visual de los avisos (llama animada, halo, número que rueda de 0 a n), con: título según el estado ("¡Hoy ya cumpliste!" / "Hoy falta estudiar" / "¡Hoy es clave!" / "Aún no tienes racha"), la **semana lun→dom día por día** (🔥 cumplido, ☾ descanso usado, anillo punteado = hoy -rojo si es clave-, tenue = por venir / antes de la racha) con entrada escalonada, y las filas **Hoy** (minutos + barra), **Esta semana** (X de 5 días), **Días de descanso restantes** (X de 2 con pips) y **Racha iniciada**. Sin racha: llama apagada, sin semana ni descansos. Si hay una racha larga recuperable HOY, tocar el chip abre directamente la oferta de recuperar. Los toasts que siguen existiendo son los de celebración (racha sube / iniciada) y el de avance de recuperación. Archivos: `tiempo-estudio-racha.js` (nuevo `minutosSemana`; la agrupación por día se extrajo a `agruparMinutosPorDia`, sin cambio de reglas: las 34 pruebas originales siguen pasando), `tiempo-estudio-racha-ui.js`, `design-system.css` (`.te-racha-detalle`, `.te-racha-semana`, `.te-racha-dia*`, `.te-racha-fila*`, `.te-racha-pip*`, modificador `.te-racha-card--apagada`).

**2026-09-21 - Agenda/Resumen: estado "Perdido", fechas relativas en todo Resumen y fix de "Clase" solo.** Sin Worker ni SQL. **(1) Estado "Perdido":** nuevo booleano `perdida` en `EventoAgenda` (`crearEventoAgenda`, default `false`; migración defensiva en `migrarDatosAntiguos`, sin tocar datos existentes). Es un tercer estado EXPLÍCITO y manual de la tarea, exclusivo con `completada` y solo válido en `tipo: "tarea"`. Al tocar el círculo: ya completada/perdida → vuelve a pendiente (reversible); pendiente y VENCIDA → ventanita "Completado / Perdido" (armada por JS, no depende de `index.html`); pendiente no vencida → completa directo como siempre. Una tarea perdida deja de ser "vencida" (`esTareaVencida`/`tareaVenceHoy` → `false`): sale de "Tareas vencidas", del badge "⚠ Vencida" y de los pendientes. Visual: borde/badge rojo muy oscuro (`#7f1d1d`, texto `#fecaca`), badge "Perdida", círculo con ✕, nombre tachado con raya roja (`#dc2626`, 2px) y texto SIN atenuar (la completada sigue azul, tachada y a 0.7 de opacidad). Filtro de Agenda: pasó de 6 a 7 badges (`Perdida` va tras `Completado`); una tarea cae en exactamente UNO de completado/perdida/pendiente. Editar la fecha de una perdida al futuro (o cambiarle el tipo) la devuelve a pendiente, con toast. Google Calendar: `notificaciones-calendario.js` trata `perdida` igual que `completada` (elimina el espejo; al restaurar se recrea) en `sincronizarEventoCalendario`, `resincronizarTodaLaAgendaConCalendar` y `generarTextoResumenHoy`. **Cómo se refleja en Resumen:** tarjeta "Tarea(s) perdida(s)" con el conteo (solo si hay ≥1; no cuenta como pendiente). Los estilos viven en `design-system.css`, bloque "Estado Perdido" (junto al filtro de Agenda): `.badge-perdida`, `.agenda-check-completada.perdida` (✕ por `::after`), `.agenda-filtro-estado-perdida.active` y `.agenda-selector-estado-*` (ventanita, que usa `glass-card modal-card`). `COLOR_PERDIDA_BORDE`/`COLOR_PERDIDA_RAYA` en `agenda-utils.js` deben coincidir con esos hex. **(2) Fechas relativas:** `formatearFechaRelativa(fecha)` en `agenda-utils.js` (no existía; la escala anterior era `formatearEncabezadoDia`, que solo sabía decir "Mañana" y agrupaba). Escala exacta pedida (Hoy/Mañana/Pasado Mañana/En N días · Ayer/Antier/Hace N días, sin tope). Compara días de calendario locales vía `Date.UTC`, no horas (probada en Costa Rica, Nueva York con cambio de horario y Auckland). En `resumen.js` se eliminaron `formatearEncabezadoDia` y `construirListaEventosAgrupadaPorFecha` (el encabezado de grupo "MAÑANA"): ahora existe UNA sola lista, `construirListaEventos`, que antepone a cada tarjeta una fila con la fecha real a la izquierda y, a la derecha, el texto relativo. Aplica a Exámenes próximos, Tareas de hoy, Tareas vencidas, Próximas tareas y Próximo evento. **(3) Bug "Clase" solo:** en `construirBloqueDia` (`agenda.js`) el día se descartaba (`return null`) mirando solo `eventosDelDia`, ANTES de construir la sección de clases; con "Clase" como único badge, `eventosDelDia` queda siempre vacío y con "Mostrar días sin eventos ni tareas" apagado se descartaban todos los días. Ahora las clases se resuelven antes y cuentan como contenido (**cambio de comportamiento:** con "días vacíos" apagado, un día que solo tiene clases ahora SÍ aparece, también sin filtros). "Sin pendientes." ya no sale bajo las clases cuando el filtro dejó fuera todos los tipos de evento. **Archivos:** `core/schema.js`, `core/notificaciones-calendario.js`, `design-system.css`, `agenda/agenda-utils.js`, `agenda/agenda-modal.js`, `agenda/agenda.js`, `resumen/resumen.js`. **Revisados sin cambios:** `agenda/agenda-calendario.js` (sus puntos pintan TODO evento del día, completadas incluidas, con `obtenerEstiloEvento`, así que una perdida sale con punto rojo oscuro, igual que una completada sale azul), `asistente/asistente.js` (sus consultas listan por fecha/materia/tipo sin mirar `completada`) e `index.html` (la tarjeta de info reutiliza sus elementos; el círculo es un botón vacío, compatible con el ✕ por CSS). Probado con jsdom + `schema.js` real: 95 pruebas OK, con control negativo contra el código anterior (reproduce "Nada pendiente en este rango").

**2026-09-22 - Recuperar historial perdido de competencias (Registro de finalizadas).** Sin migración SQL; **requiere redesplegar el Worker** (`index.js`) además del cliente. **Causa raíz:** el cliente solo guarda un PUNTERO local de "a qué competencias estoy unido" (`estado.datos.competencias_unidas`, sincronizado por Drive) - el contenido real siempre vivió 100% en D1. Si ese puntero se pierde (tumba vieja de antes del fix de `podarTumbasSuperadasPorAltas` del 2026-09-19, dispositivo nuevo, cualquier otro bug de sync), la competencia desaparecía de "Registro de competencias" sin ningún camino para volver a verla: todos los endpoints existentes piden un `id` de competencia puntual, que es justo lo que el cliente ya no tiene. **Worker:** endpoint nuevo de solo lectura `GET /identificadores/:correo/competencias` - todas las filas de `participantes` de ese `identificador_usuario` (activas y finalizadas por igual), con nombre/estado/participante_id/apodo; es el único endpoint que busca por persona en vez de por `id` de competencia. **Cliente:** `recuperarCompetenciasPerdidas()` (nueva, `tiempo-estudio-competencias-gestion.js`) llama a ese endpoint con el correo de `estado.datos.perfil.correo`, y agrega a `competencias_unidas` lo que el Worker devuelve y no está ya localmente por `id` (SOLO ADITIVO: nunca pisa ni borra nada existente), sellado con `sellarTimestamp` + `marcarCambioPendiente`. Las recuperadas entran con `es_creador: false` (el endpoint no expone `token_creador`, así que quedan sin permisos de creador acá - mismo criterio de siempre, el Worker es la autoridad real). `construirRegistroCompetencias` **cambia de comportamiento:** antes no dibujaba nada si no había ninguna finalizada localmente (`if (finalizadas.length === 0) return`); ahora la sección SIEMPRE se dibuja, porque además de la lista es la única entrada al botón "🔄 Recuperar historial" - si dependiera de ya tener algo local, quien perdió el puntero nunca vería cómo recuperarlo. **Trade-off aceptado a propósito:** no hay forma de distinguir "salí a propósito de esta" vs. "se perdió el puntero" (el Worker no guarda eso), así que una competencia de la que alguien salió hace mucho y cuya tumba ya se purgó podría, en teoría, reaparecer al recuperar - aceptable para el caso de uso real (grupo de amigos, "Salir" sigue siendo un botón), preferible a perder historial de verdad. **Archivos:** `worker-notificaciones-agenda/src/index.js` (endpoint + ruta), `tiempo-estudio-competencias-gestion.js` (`recuperarCompetenciasPerdidas`, `construirRegistroCompetencias`). **Pendiente de la misma ronda de bugs, sin resolver todavía:** (1) fondo de la tarjeta del podio en morado en vez de la paleta propia de cada quien (el pedestal en sí ya toma el color bien - confirmado -, es la tarjeta contenedora la que no); (2) horas de estudio en Agenda, que se quitaron y hay que devolver estilizadas según la app; (3) modal "Ajustes de Tiempo" - quitar "Próximamente" de Torneos/Competencias y agregar botón "Agregar materia personalizada" + flujo de materia fuera del semestre.

**2026-09-21 a 2026-09-26 - Rediseño visual completo de Competencias (`prototipo-competencias-v2.html`) + invitación por deep link + animación de unirse + tarjeta "Estudio para hoy" de vuelta en Agenda + registro manual rediseñado.** **2 archivos nuevos:** `tiempo-estudio/tiempo-estudio-competencias-visual.js` (todo lo que se DIBUJA: avatares con foto de Google con fallback a inicial de color estable, filas, podio, tarjeta de competencia, hoja modal deslizable, historial por semana, aviso de posición - sin red ni `estado`, HTML/DOM puro) y `tiempo-estudio/tiempo-estudio-competencias-visual-estilos.js` (el CSS del rediseño como string que el anterior importa e inyecta) - ver sus entradas propias más abajo. `tiempo-estudio-competencias.js` y `tiempo-estudio-celebracion.js` dejan de dibujar ellos mismos y pasan a importar de este módulo (import de una sola dirección, sin ciclos). **Cambios visibles:** (1) la tarjeta de cada competencia es ahora un marcador con podio o lista (selector arriba a la derecha, se recuerda por dispositivo); (2) el trofeo de Historial trae ahora el PODIO COMPLETO de cada semana cerrada (`GET /competencias/:id/podios`, nuevo, reemplaza a `GET /historial` que solo traía el ganador) en hojas desplegables; (3) el botón "Enlace" salió de la tarjeta - la invitación vive arriba de todo en el menú Gestionar; (4) color de acento propio (`--accent-1` de la paleta de cada quien) pinta pedestal/aro/barra del podio de todos, con foto de Google sincronizada sola si cambia. **Invitación por deep link (nuevo):** `revisarLinkInvitacionAlCargar()`, a llamar UNA sola vez desde el `DOMContentLoaded` de `main.js` después de que `estado` termine de cargar (**`main.js` integra el caller en v3.20.0**), lee `?comp=<id>` de la URL, lo saca con `replaceState` (un F5 no reabre nada) y abre `abrirModalInvitacionRecibida` *(interna)* con el nombre y el marcador de la competencia ANTES de unirse - ya no hace falta pegar el link a mano (`abrirModalUnirseCompetencia` sigue existiendo igual para ese caso). Al unirse por cualquiera de las 2 vías dispara `mostrarAnimacionUnirseCompetencia()` (celebracion.js, nueva, 2026-09-22): chips de avatares de quienes ya estaban entrando uno a uno y el propio cerrando la fila, **sin confeti a propósito** (el cañón de confeti queda reservado para ganar una semana, no para unirse). **Fix 2026-09-22 resuelve el pendiente (1) de la ronda "Recuperar historial perdido" de más abajo** ("fondo del podio en morado para todos"): `obtenerMiColor()` mandaba siempre el `--accent-1` computado aunque la persona nunca hubiera tocado la paleta - ahora la paleta default de la app se trata como "sin color propio" (`null`), igual que ya hacía `tonosDeColor()` con oro/plata/bronce; hubo 2 rondas más de ese mismo bug en otras reglas del CSS (ver `tiempo-estudio-competencias-visual-estilos.js`) y una saga de fixes de contraste claro/oscuro entre el 2026-09-22 y el 2026-09-26 que termina en `ajustarContrasteCpScope()`. **2026-09-23:** los 2 botones de simulación de resultado (victoria/derrota) dejan de mostrarse por defecto - quedan detrás de un easter egg (mantener presionado 5s el título "Competencias"). **2026-09-26:** fix de espaciado (la sección cuelga sus piezas de un `envoltorio` propio en vez de directo de `cont`, para no heredar el gap de la clase compartida "stack"). **Resuelve también el pendiente (2) de la ronda "Recuperar historial perdido"** ("horas de estudio en Agenda, que se quitaron y hay que devolver estilizadas"): `agenda/agenda.js` suma `construirTarjetaEstudioHoy()` / `asegurarEstilosEstudioHoyAgenda()` *(internas, 2026-09-22)* - tarjeta con barra de progreso por materia (color propio vía `color-mix()` contra `--bg-panel`, mismo patrón que `.timer-indicador*`) arriba de todo en el bloque de HOY, solo con el switch de Ajustes activado, leyendo `obtenerEstudioParaHoy()` de `tiempo-estudio.js` (sin cambios ahí). **En la fecha de esta entrada, el pendiente (3) del modal "Ajustes de Tiempo" seguía sin resolver; se atendió en beta4 (se retiró el bloque "Torneos / Competencias — Próximamente" y el alta de materia propia se ubicó al final del listado).** **2026-09-22 (mismo día, aparte):** `tiempo-estudio-registro.js` - el registro manual cambia el `<select>` nativo de materia (se veía gris/roto sobre fondo oscuro) por tarjetas seleccionables de un solo click (mismo patrón visual que `.agenda-semestre-tarjeta`), y gana un pill-switch **Hora / Tiempo**: modo Hora pide inicio+fin y calcula la duración sola (como antes); modo Tiempo pide horas+minutos directos con una hora de inicio OPCIONAL puramente informativa (checkbox "Agregar hora de inicio", default = ahora mismo, nunca deriva una hora de fin). **Archivos de esta ronda:** `tiempo-estudio-competencias-visual.js` (nuevo), `tiempo-estudio-competencias-visual-estilos.js` (nuevo), `tiempo-estudio-competencias.js`, `tiempo-estudio-celebracion.js`, `tiempo-estudio-registro.js`, `agenda/agenda.js`. **`main.js` ahora llama a `revisarLinkInvitacionAlCargar()` después de cargar el estado**, por lo que el deep link queda conectado en esta versión.

**2026-09-12 - Becas y Pagos de Matrícula (rediseño del modelo de finanzas por semestre), Partes A y B:** modelo nuevo `pagos_matricula`/`ingresos_beca` (listas, en vez de `costo_matricula`/`beca_monto` como números sueltos) + migración de una sola vez + fusión anidada nueva en `storage-merge.js` (Parte A); UI de tarjetas expandibles in-place con alta/edición/borrado por ítem y "dividir pago en partes" (Parte B), reemplazando el modal único que existía como parche transicional de Parte A. Archivos tocados: `core/schema.js`, `core/storage-merge.js`, `finanzas/finanzas.js`, `finanzas/finanzas-graficas.js` (fix de bug de lectura de campos retirados en ambos), `finanzas/finanzas-semestres.js` (reescritura de Parte A a Parte B). **Pendientes: Parte C** (botones rápidos "Pago de matrícula"/"Ingreso de beca" fuera de un semestre puntual) y **Parte D** (modo edición explícito con "no salir sin guardar" - confirmado que `plan-modo-edicion.js` NO se puede reusar tal cual, solo tiene el toggle+badge visual, hay que construir la detección de cambios pendientes + guard de salida desde cero).

---

## Revisión funcional v3.20.0-beta4 (2026-09-25)

Se contrastó el mapa con la copia de edición completa (66 archivos `.js`) y se actualizaron las áreas modificadas para esta entrega:

- **Preferencias por dispositivo:** `ui/tema.js` lee y guarda Claro/Oscuro y Optimizado/Fancy en `localStorage`; `core/schema.js#migrarDatosAntiguos` rescata una vez los valores sincronizados en dispositivos ya usados y elimina las claves antiguas de `configuracion`. El modo vacío usa el tema del sistema y Optimizado. `main.js`, `storage-sync.js`, `config/config-ajustes.js`, `ui/paleta-personalizada.js` y `plan/plan-esquema.js` ya aplican la preferencia local. `storage-sync.js#aplicarDatosRemotosFrescos` vuelve a aplicar visibilidad de navegación tras incorporar cambios recibidos de Drive.
- **Competencias sin conexión:** `tiempo-estudio-competencias.js` conserva por id la última respuesta exitosa del marcador en localStorage y `tiempo-estudio-competencias-visual.js` la presenta con hora de actualización cuando falla la consulta viva.
- **Atrás interno:** `main.js#mostrarSeccion` registra cambios entre secciones con History API y restaura la sección al recibir `popstate`. El historial cuenta secciones; modales y detalles no agregan entradas. **Plan:** al entrar se restablecen grupos abiertos y materias cerradas en cada dispositivo.
- **Acceso a Cronograma:** desde la tarjeta abierta de una clase en `horario.js`, “Ir a Cronograma” aparece a la izquierda de “Editar” y abre Agenda > Cronograma filtrado por esa matrícula; `agenda.js#abrirCronogramaAgenda(semestreId, materiaMatriculadaId)` recibe el foco. **Tiempo:** el listado termina en dos pestañas táctiles discretas (alternar materias activas/todas y “Nueva Materia”); al crear una materia sin meta se cambia a “Todo” si hacía falta para mostrarla. Se retiró el bloque “Torneos / Competencias — Próximamente” de Ajustes de Tiempo. **Resumen:** Estudio de hoy aparece antes de Clases de hoy y el fondo de su barra queda visible incluso con progreso cero; en el sidebar claro el pie de perfil ya no pinta un rectángulo opaco.
- **Materias propias en Tiempo:** `core/schema.js#crearMateriaEstudioIndependiente` crea entradas `tipo: "independiente"` en `tiempo_estudio_materias`; `storage-merge.js#fusionarDatos` las sincroniza como colección propia. Las sesiones usan `materia_independiente_id`; Tiempo, Timer, registro manual y estadísticas resuelven ese id sin crear matrículas ni editar Plan/Semestres.
- **Puntos anteriores corregidos contra esta copia:** la invitación por deep link sí llama a `revisarLinkInvitacionAlCargar()` desde `main.js`; la nota de que faltaba conectar ese llamado ya no aplica. La Bandeja está en `js/core/asistente-bandeja.js` y exporta `sincronizarBandejaPendiente()`.

La versión visible/caché del PWA se fija en `service-worker.js` (`v3.20.0-beta4`).

---

## Índice de carpetas

- [`js/core/`](#js--core) - datos, sesión, sincronización (12 archivos)
- [`js/ui/`](#js--ui) - componentes de interfaz genéricos (6 archivos)
- [`js/config/`](#js--config) - sección Configuración (2 archivos)
- [`js/plan/`](#js--plan) - Plan de Estudios (10 archivos)
- [`js/semestres/`](#js--semestres) - Semestres y matrícula (3 archivos)
- [`js/finanzas/`](#js--finanzas) - Finanzas (4 archivos)
- [`js/comunidad/`](#js--comunidad) - Profesores y compañeros (1 archivo)
- [`js/horario/`](#js--horario) - Horario y horario entre amigos (4 archivos)
- [`js/agenda/`](#js--agenda) - Agenda (8 archivos)
- [`js/asistente/`](#js--asistente) - Asistente IA (1 archivo; la Bandeja se documenta en `js/core/`)
- [`js/tiempo-estudio/`](#js--tiempo-de-estudio) - Tiempo de Estudio (13 archivos, con los 2 de la racha y los 2 nuevos de la capa visual de Competencias, 2026-09-21; la sección se llama "Tiempo" en la interfaz desde 2026-09-17, los archivos NO se renombraron para no romper imports)
- [`js/resumen/`](#js--resumen) - Resumen (1 archivo)
- [`js/main.js`](#jsmainjs) - arranque (1 archivo)

---

## JS - core

### core/auth.js
Propósito: toda la integración con Google (login/token) y con las API de Google Drive y Google Calendar - es la única capa que habla HTTP con Google.
Depende de: schema.js (`crearDatosUsuarioNuevo`)
Exporta:
* `NOMBRE_CARPETA_BACKUP` - constante `"AppAcademica"`, nombre de la carpeta de Drive donde vive el backup rotativo.
* `URL_WORKER_OAUTH` - **(2026-09-09, recién exportada - antes vivía privada del archivo)** URL del Worker de Cloudflare, `https://worker-notificaciones-agenda.appacademica.workers.dev`. Se exportó para que `tiempo-estudio/tiempo-estudio-competencias.js` la reuse en vez de duplicar el literal - el Worker gana rutas nuevas ahí (`/competencias/...`) que no son OAuth, el nombre ya no describe 100% su alcance pero se dejó tal cual para no tocar el resto de este archivo.
* `inicializarGoogleAuth({ alObtenerToken, alListo, alFallar, alRechazarPermiso })` - arranca el CodeClient de Google Identity al cargar la página; llama a los callbacks según el resultado.
* `iniciarSesionConGoogle()` - dispara la ventana de login/consentimiento de Google (debe llamarse directo desde un click, sin await antes). 2026-08-25: migrado de `initTokenClient` a `initCodeClient` (flujo de código) - ver `tieneScopeCalendarOtorgado` más abajo.
* `obtenerPerfilGoogle(token)` - pide nombre y foto de perfil a Google. Devuelve `{ nombre, foto_url }` o `null` si falla.
* `cerrarSesionGoogle()` - revoca el token en memoria y borra el refresh_token guardado (`borrarRefreshTokenGoogle()`) - no borra datos locales, eso lo hace storage.js/main.js.
* `buscarOCrearArchivoDatos(token)` - busca el JSON central de la app en Drive; si no existe lo crea con datos de fábrica. Devuelve `{ fileId, datos }`.
* `crearArchivoJsonEnDrive(token, nombreArchivo, datos)` - versión genérica de creación de archivo JSON en Drive con nombre/contenido arbitrarios (usada para horarios compartidos públicos).
* `crearPermisoPublicoLectura(token, fileId)` - aplica permiso público de solo lectura sobre un archivo. Devuelve `{ id }` (el `permissionId` a guardar para poder revocarlo).
* `eliminarPermisoDrive(token, fileId, permissionId)` - revoca un permiso público (botón "Revocar" de un enlace compartido); un 404 cuenta como éxito.
* `leerDatos(token, fileId)` - descarga y parsea el JSON completo de un archivo de Drive.
* `obtenerMetadatosArchivo(token, fileId)` - pide solo `modifiedTime` de un archivo (llamada barata para sondeo de cambios remotos).
* `guardarDatos(token, fileId, datos)` - sobrescribe el archivo de datos en Drive con el objeto completo; valida `respuesta.ok` y lanza si falla.
* `refrescarAccessTokenViaWorker(refreshToken)` - pide un access_token nuevo vía `POST /oauth/refresh` del Worker - llamada REST servidor-a-servidor pura, nunca puede mostrar una ventana de Google. Devuelve `{ token, expiresIn, refreshTokenNuevo }` (`refreshTokenNuevo` solo si Google rotó el refresh_token). Rechaza con `error.invalidGrant = true` si el refresh_token venció/fue revocado.
* `guardarRefreshTokenGoogle(refreshToken)` / `leerRefreshTokenGoogle()` / `borrarRefreshTokenGoogle()` - guardan/leen/borran el refresh_token en `localStorage`, local a este dispositivo (nunca sincroniza a Drive, es una credencial no un dato de la app).

### core/asistente-bandeja.js
Propósito: trae del Worker los ítems crudos de Captura por voz, los procesa con el mismo pipeline de Gemini del Asistente y guarda los eventos válidos en Agenda.
Depende de: `core/storage.js`, `ui/componentes.js`, `asistente/asistente.js`.
Exporta:
* `sincronizarBandejaPendiente()` - obtiene y procesa la bandeja pendiente de forma best-effort al abrir la app; los errores no interrumpen el resto del arranque.
* `tieneScopeCalendarOtorgado()` - (2026-08-25, Calendario Secundario) si el login/canje más reciente incluyó el scope `.../auth/calendar` - Drive es obligatorio desde siempre, Calendar es scope nuevo y opcional en el sentido de que su ausencia no bloquea el login.
* `crearCalendarioSecundario(token, nombreCalendario)` - `calendars.insert`, crea el calendario secundario (una sola vez por usuario, ver `asegurarCalendarioSecundario` en notificaciones-calendario.js). Devuelve el objeto de Google (`.id` es lo que hay que persistir).
* `fijarColorCalendario(token, calendarId, colorId)` - `calendarList.patch`, fija el color de fondo del calendario secundario en sí (no de eventos individuales).
* `insertarEventoCalendar(token, calendarId, evento)` / `actualizarEventoCalendar(token, calendarId, eventId, evento)` / `eliminarEventoCalendar(token, calendarId, eventId)` - `events.insert`/`events.update` (PUT completo)/`events.delete` crudos contra Google Calendar; la orquestación (mapear un EventoAgenda, colorId, reminders, best-effort) vive en notificaciones-calendario.js.
* `subirArchivoBinarioADrive(token, archivo)` - sube un `File`/`Blob` arbitrario (adjuntos) como archivo nuevo e independiente en Drive.
* `descargarArchivoBinarioDeDrive(token, driveFileId)` - descarga el contenido real de un adjunto por demanda.
* `eliminarArchivoDeDriveConId(token, driveFileId)` - borra el archivo real de Drive de un adjunto eliminado; 404 cuenta como éxito.
* `buscarOCrearCarpetaEnDrive(token, nombreCarpeta)` - busca (o crea) una carpeta visible para la app (scope `drive.file`). Devuelve el `folderId`.
* `buscarArchivoEnCarpeta(token, folderId, nombreArchivo)` - busca un archivo por nombre dentro de una carpeta puntual. Devuelve su `fileId` o `null`.
* `renombrarArchivoDrive(token, fileId, nuevoNombre)` - renombra un archivo sin tocar su contenido (solo metadata).
* `copiarArchivoDrive(token, fileId, nombreCopia, folderId)` - copia un archivo existente del lado del servidor de Google (sin bajar/subir bytes).
* `moverArchivoAlaCarpeta(token, fileId, folderIdDestino)` - mueve un archivo existente a una carpeta, mismo `fileId`, sin tocar contenido.

> **Nota 2026-08-25:** este archivo YA estaba documentado acá con `refrescarAccessTokenViaWorker`/`guardarRefreshTokenGoogle`/`leerRefreshTokenGoogle`/`borrarRefreshTokenGoogle` desde una sesión anterior - pero el auth.js real que se auditó al empezar esta migración todavía tenía la versión vieja (`initTokenClient`, sin refresh_token, sin ninguna de esas 4 funciones). Es decir, esta parte de OAuth ya se había dado por hecha en la documentación sin haberse aplicado nunca al código real. Se implementó ahora con los nombres que ya documentaba este archivo (no con nombres nuevos), asumiendo que storage-sync.js ya los llama así - si storage-sync.js resulta usar otros nombres, hay que ajustar auth.js, no este documento.

### core/clipboard.js
Propósito: blindaje del flujo "copiar prompt al portapapeles" (usado en "Enviar a Claude/ChatGPT") - garantiza que el usuario siempre se entera si la copia falló y siempre tiene una forma manual de recuperarse.
Depende de: storage.js (`estado`), ui/componentes.js (`mostrarToast`)
Exporta:
* `comprobarPermisoPortapapelesAlIniciar()` - se llama una vez tras un login exitoso; guarda en `estado.permisoPortapapeles` el estado del permiso ("otorgado"/"denegado"/"desconocido").
* `copiarAlPortapapelesBlindado(texto)` - intenta copiar con `navigator.clipboard.writeText`, y si falla usa `execCommand("copy")` como respaldo. Devuelve `true`/`false` real (nunca falla en silencio).
* `copiarPromptConAviso(texto)` - punto único que debe usar el resto de la app para copiar el prompt de importación: muestra toast si funcionó, o abre el modal de copia manual si falló por completo.
* `abrirModalCopiaManualPortapapeles(texto)` - construye (si hace falta) y muestra un modal con un textarea de solo lectura ya seleccionado, como último recurso de copia manual.

### core/schema.js
Propósito: el molde de datos completo del usuario - factories (`crear*`) de cada entidad, migraciones de esquema, el reloj lógico (Lamport) para sincronización, y todo el motor de cálculo de notas/promedios/finanzas. Es el archivo más grande y más importado del proyecto; no renderiza nada, no toca el DOM.
Depende de: ninguno (archivo base, sin imports).
Exporta:

*Datos de usuario y migraciones:*
* `crearDatosUsuarioNuevo()` - objeto de datos "vacío" para un usuario que inicia sesión por primera vez (estructura completa de fábrica).
* `migrarDatosAntiguos(datos)` - corre todas las migraciones de esquema necesarias sobre datos ya existentes, de forma idempotente. También mueve una sola vez `configuracion.modo`/`modo_rendimiento` heredados a localStorage del dispositivo cuando corresponde, elimina esos campos del objeto sincronizable y deja que dispositivos sin valor local usen Optimizado + tema del sistema. Incluye la migración de `plan.universidad` de string a `{ nombre_completo, siglas }`; `siglas: ""` activa el aviso bloqueante de planes incompletos en main.js.
* **(2026-09-19, racha de estudio)** `configuracion.racha_aviso_inicio_oculto` (bool, default `false`) y `configuracion.racha_ultimo_inicio_avisado` (ISO `"YYYY-MM-DD"` o `null`) - declarados en `crearDatosUsuarioNuevo` y rellenados defensivamente en `migrarDatosAntiguos`. La racha en sí NO se persiste (es derivada de `sesiones_estudio`).
* `MAPEO_HORAS_VIEJO_A_NUEVO` - tabla de migración del modelo viejo de horas fijo al modelo dinámico de `tipos_horas`.

*Reloj lógico / sincronización (Lamport):*
* `obtenerDispositivoId()` - id único y estable de este navegador/dispositivo (persiste en localStorage), usado como desempate determinista.
* `observarRelojLogico(valorAjeno)` - adelanta el reloj lógico propio al ver un contador ajeno mayor (regla estándar de Lamport).
* `sellarTimestamp(entidad)` - sella una entidad con el contador lógico, el id de dispositivo y `_version_base` (el contador previo, para distinguir edición secuencial de conflicto real). Punto único por el que debe pasar cualquier entidad al crearse o editarse - no inventar un sellado propio en otro archivo.

*Enlaces rápidos / adjuntos:*
* `crearEnlaceRapido({ nombre, url, icono_tipo, icono_valor })` - estructura de un enlace rápido (máx. 20).
* `LIMITE_ENLACES_RAPIDOS` - tope de enlaces rápidos por usuario (20).
* `crearAdjunto({ nombre, mimeType, tamanoBytes, entidadTipo, entidadId })` - referencia liviana de un adjunto (el binario real vive aparte en Drive, ver storage-adjuntos.js).
* `LIMITE_MB_ADJUNTO` - tamaño máximo por adjunto en MB (25).

*Plan de Estudios:*
* `crearPlanEstudio({ nombre_carrera, universidad, codigo_plan, tipo_titulo, parametros_universidad })` - crea un Plan de Estudios nuevo. `universidad` (2026-08-22, separación nombre_completo/siglas) ya no es un string plano - es `{ nombre_completo, siglas }`; `siglas` es lo que se usa en todos los badges cortos de la app, `nombre_completo` queda para contexto donde hace falta el nombre real. Quien llama es responsable de armar el objeto completo (ver `abrirModalCrearPlan`/`btn-confirmar-crear-plan` en plan-esquema.js).
* `crearMateria({ codigo, nombre, creditos, horas, tiposHoras, bloque, requisitos, correquisitos, esOptativa, sinDefinir })` - crea una materia a partir de una fila de CSV o del formulario manual.
* `crearCategoria({ nombre, color })` - crea una categoría de materias (con timestamp sellado).
* `PARAMETROS_UNIVERSIDAD_DEFAULT` - valores sugeridos por universidad (tipos de horas, etc.).
* `NOMBRES_UNIVERSIDAD_PRESET` (2026-08-22) - nombres completos de los 2 presets rápidos TEC/UCR (`{ TEC: "Instituto Tecnológico de Costa Rica", UCR: "Universidad de Costa Rica" }`), usados para armar `universidad.nombre_completo` sin que el usuario lo tipee en esos 2 casos.
* `PRESETS_TIPOS_HORAS` - presets rápidos de `tipos_horas` para el modal "Nuevo Plan".

*Árbol de requisitos Y/O:*
* `crearNodoCodigo(valor)` - crea un nodo hoja `{ tipo: "codigo", valor }`.
* `crearNodoY(hijos)` / `crearNodoO(hijos)` - crean nodos operadores Y/O (colapsan a hoja simple si solo hay 1 hijo).
* `evaluarNodoRequisito(nodo, estaAprobada)` - evalúa recursivamente si un árbol de requisitos está cumplido, dado un callback de aprobación por código.
* `recorrerHojasArbol(nodo, callback)` - recorre todas las hojas del árbol sin importar la profundidad.
* `arbolContieneCodigo(nodo, codigo)` - true/false: ¿existe una hoja con este código en algún nivel?
* `migrarRequisitoAArbol(valorViejo)` - migra el formato viejo de requisitos (string/array plano) al árbol Y/O nuevo.

*Semestres:*
* `crearSemestre({ nombre, fecha_inicio, duracion_semanas, planesEstudioIds })` - crea un Semestre nuevo.
* `LIMITE_SEMANAS_SEMESTRE` - tope de semanas por semestre (25).
* `obtenerEstadoEfectivoSemestre(semestre)` - calcula en vivo si un semestre es "futuro"/"actual"/"pasado" (nunca se guarda el estado).
* `obtenerPlanesActivos(configuracion)` - ids de los planes activos ahora mismo, según Modo Hardcore (hasta 3).
* `obtenerEstadoEfectivoMateria(materia, planEstudioId, datos)` - estado efectivo de una materia del plan ("Cursando" se calcula en vivo, nunca se guarda).

*Notas y calificaciones:*
* `crearCriterio({ nombre, valorTotal, orden, esExtra })` / `crearAsignacion({ nombre, valor, orden, modoCalificacion })` - crean criterio/asignación de evaluación dentro de una materia matriculada.
* `crearMateriaMatriculada({ materiaId, planEstudioId })` - matricula una materia real dentro de un semestre. Incluye `tiempo_estudio: { meta_horas_semana, pomodoro, color, dias_estudio, ultima_semana_felicitada }` (Tiempo de Estudio, Parte 1 + 3 + "Entrega 3"): `ultima_semana_felicitada` (Parte 3, 2026-09-05) guarda el epoch ms del lunes de la última semana en la que ya se felicitó a esta materia por cruzar su meta - evita repetir la felicitación en cada apertura de la app la misma semana, ver `revisarFelicitacionMeta()` en `tiempo-estudio-timer.js`. `dias_estudio` (2026-09-09, "Entrega 3") - array de códigos de día (`DIAS_SEMANA_CONFIG.abrevDefault`, `"L"|"K"|"M"|"J"|"V"|"S"|"D"`, mismo código que Horario) que dice qué días de la semana se estudia esta materia; `null` = sin configurar todavía → se interpreta como "todos los días" (retrocompatible con toda materia creada antes de este campo). Se configura en el nuevo pill de días de `abrirModalConfigTiempoEstudio()` (`tiempo-estudio-config.js`) y lo consumen `calcularMetaDiariaMateria()` y `obtenerEstudioParaHoy()` (ver esos dos más abajo) - un `[]` explícito (todos los días destildados) no se permite desde la UI, siempre queda al menos 1.

*Tiempo de Estudio (nunca documentado acá hasta esta ronda - Parte 1/2/3):*
* `COLOR_TIEMPO_ESTUDIO_DEFAULT` - `"#a78bfa"`, mismo violeta de respaldo que `obtenerColorBloque()` en `horario.js`, para que una materia sin color propio se vea igual en las dos secciones.
* `crearConfigPomodoroDefault()` - defaults de Pomodoro (25 min bloque / 4 bloques / 5 min descanso corto / 10 min descanso largo), único punto de verdad reusado por el modal por materia y el modal predeterminado global (`tiempo-estudio-config.js`).
* `crearMateriaEstudioIndependiente(nombre)` - crea una entrada propia de Tiempo, con `tipo: "independiente"`, nombre libre, ID propio y configuración de meta/Pomodoro/color/días; se guarda en `tiempo_estudio_materias`, fuera de Plan y Semestres.
* `crearSesionEstudio({ materiaMatriculadaId, materiaIndependienteId, inicio, fin, origen })` - crea una `sesion_estudio` ya cerrada (con inicio Y fin conocidos); admite guardar el ID de una materia independiente en `materia_independiente_id` sin fingir que es una matrícula real; `duracionMinutos` se calcula siempre acá adentro a partir de los timestamps, nunca se recibe como parámetro. `origen`: `"timer"` (Parte 1) | `"pomodoro"` (Parte 2) | `"manual"` (Parte 3, registro retroactivo - cuenta exactamente igual que una sesión en vivo para meta y felicitación, sin ninguna validación anti-trampa, pedido explícito).
* `repartirEquitativoCriterio(criterio)` - reparte equitativamente el peso sobrante entre asignaciones en modo "automático".
* `obtenerEscalaNotasMateria(materia, plan, configuracion)` - resuelve la escala de notas activa (override > plan > default 100).
* `ESCALAS_DISPONIBLES` - lista de escalas de calificación disponibles (10, 100, letras, gpa4, etc.).
* `obtenerEscalaPorId(escalaId)` - descriptor completo de una escala a partir de su id crudo.
* `convertirA100(valorEnEscala, escala)` / `convertirDesde100(valorEn100, escala)` - conversión entre una escala puntual y 0-100 interno.
* `migrarNotasAsignacionesEscalaPlan(datos, planId, escalaIdVieja, escalaIdNueva)` - re-escala todas las notas ya cargadas de un plan al cambiar su escala de notas.
* `obtenerFraccionNota(nota, escalaId)` / `notaMinimaParaFraccion(fraccion, escalaId)` - conversión nota↔fracción (0-1) para comparar/simular entre escalas distintas.
* `calcularPuntosAsignacion(asignacion, escalaActiva)` - puntos ponderados reales que aporta una asignación calificada, normalizados a 0-100.
* `recalcularNotaDesdePuntaje(asignacion, escalaActiva)` - dado un puntaje crudo, calcula la nota equivalente en la escala activa.
* `calcularNotaFinalMateria(materiaMatriculada, escalaActiva)` - nota final (0-100) sumando los puntos de todas las asignaciones calificadas.
* `redondearDecimales(num, decimales)` - redondeo "decimal-safe" que limpia ruido binario de coma flotante.
* `redondearNotaFinalAlCincoMasCercano(nota)` - redondea la nota final al múltiplo de 5 más cercano antes de decidir aprobado/reprobado.
* `obtenerAsignacionesPendientes(materiaMatriculada)` - lista de asignaciones sin nota todavía (para el simulador "Proyectar").
* `calcularMaximoPosibleMateria(materiaMatriculada, escalaActiva)` - nota final si se saca el máximo en todo lo pendiente.
* `calcularNotaNecesariaUniforme(materiaMatriculada, escalaActiva, objetivo)` - fracción uniforme necesaria en cada pendiente para llegar a un objetivo.
* `calcularObjetivoPasarRaspando(notaAprobacion)` - objetivo real de "pasar raspando" dado el redondeo al 5.
* `resolverObjetivoPasarRaspando(params)` - igual que arriba pero respetando un override manual por plan.
* `calcularNotaFinalVigenteMateria(mm, materia, plan, configuracion)` / `calcularNotaParaPromedio(mm, materia, plan, configuracion)` - nota final vigente de una matrícula, con y sin redondeo al 5, para dashboards.
* `listarMatriculasResolubles(datos, incluirActuales = true)` - lista base de matrículas con nota resoluble, para alimentar los cálculos de promedio.
* `calcularPromedioPonderado(entradas)` - Σ(nota×créditos)/Σcréditos sobre una lista ya filtrada.
* `calcularPromedioPorSemestreYUniversidad(datos)` / `calcularPromedioPorPlan(datos)` / `calcularPromedioTotalCombinado(datos)` - los 3 niveles de promedio del dashboard (por semestre+universidad, por plan/carrera, combinado total). Agrupa por `plan.universidad.siglas` (con fallback a `nombre_completo` y luego "Sin universidad") desde la separación nombre_completo/siglas (2026-08-22).
* `calcularEstadisticasAprobacion(datos, planId)` - % de cursos aprobados/reprobados (solo materias cerradas).
* `calcularDetallePorEstado(datos, planId)` - conteo por los 4 estados manuales del Plan (Aprobada/Cursando/Reprobada/Pendiente).

*Finanzas:*
* `crearRegistroFinancieroSemestre({ semestreId, costoMatricula, becaMonto })` - registro financiero de un semestre: `{ id, semestre_id, pagos_matricula: [], ingresos_beca: [], _eliminados_pagos_matricula: [], _eliminados_ingresos_beca: [] }`, sellado con `sellarTimestamp`. **Becas y Pagos de Matrícula - Parte A (2026-09-12):** ya NO guarda `costo_matricula`/`beca_monto` como números sueltos - esos totales siempre se calculan sumando las listas (`calcularTotalPagosMatricula`/`calcularTotalIngresosBeca`), nunca se guardan aparte, para que no puedan quedar desincronizados. Los parámetros `costoMatricula`/`becaMonto` se conservan solo por compatibilidad con el call site transicional que existió en `finanzas-semestres.js` durante Parte A (creaba el registro con un pago/ingreso inicial ya cargado); Parte B ya no los usa - el registro nuevo se crea siempre vacío y el primer pago/ingreso se agrega después, como cualquier otro.
* `crearPagoMatricula({ descripcion, monto, fecha })` - **Parte A:** un pago individual dentro de `pagos_matricula` - `{ id: "pagomat_"+uuid, descripcion (opcional, null si no se da), monto, fecha (opcional, "YYYY-MM-DD" | null) }`, sellado con `sellarTimestamp`. Puede haber varios por semestre.
* `crearIngresoBeca({ descripcion, monto, fecha })` - **Parte A:** equivalente a `crearPagoMatricula` pero para `ingresos_beca` (`id: "ingbeca_"+uuid`) - mismo shape a propósito, la beca tampoco cae en un monto ni fecha fija, así que cada depósito se registra aparte.
* `calcularTotalPagosMatricula(registro)` / `calcularTotalIngresosBeca(registro)` - **Parte A:** único punto de verdad de "cuánto se pagó de matrícula"/"cuánta beca entró" en un semestre - suman `pagos_matricula`/`ingresos_beca` de ese registro. Reemplazan la lectura directa de `registro.costo_matricula`/`registro.beca_monto`, que ya no existen - usados por `finanzas.js#calcularTotalesResumenFinanzas`, `finanzas-graficas.js#calcularSerieFinancieraPorSemestre` y `finanzas-semestres.js`.
* `migrarDatosAntiguos` - la migración de Finanzas (Parte A.2, dentro de la migración general) detecta un registro viejo por la AUSENCIA de `pagos_matricula` y convierte: `desglose_mensual.meses` (si existía guardado) → un `pago_matricula` por mes (con el mes como descripción) + un `pago_matricula` extra "Resto" si la suma de los meses no cubría `costo_matricula`; sin desglose → un único `pago_matricula` "Matrícula" por `costo_matricula`; `beca_monto` → siempre un único `ingreso_beca` "Beca (migrada)". Corre una sola vez por cuenta.
* `crearGastoU({ nombre, tipo, costo, nota, semestreId, recurrente })` - gasto (o **ingreso**, ver `tipo`) general de universidad, con soporte para recurrentes. **v2.9.2 (2026-08-26):** se agrega `tipo` (`"gasto"` | `"ingreso"`, default `"gasto"` - cualquier valor que no sea exactamente `"ingreso"` cae a `"gasto"`, así los `gastos_u` guardados antes de este cambio, sin el campo, se siguen tratando igual sin migración). Reutiliza el mismo objeto/CRUD/recurrencia para ambos tipos - la diferencia es puramente de clasificación para los totales (`calcularTotalesResumenFinanzas`, finanzas.js) y de color en la UI (finanzas-gastos.js), no de estructura.
* `calcularPagosRecurrentesTranscurridos(recurrente)` - cuenta cuántos pagos de un gasto recurrente ya cayeron hasta hoy (nunca futuros).
* `crearBackupDriveDefault()` - objeto default de `configuracion.backup_drive`.
* `FRECUENCIAS_BACKUP_DRIVE` - opciones de frecuencia de backup rotativo (diaria..mensual, con sus días de intervalo).
* `MONEDAS_DISPONIBLES` - catálogo de monedas con símbolo, para el selector global de moneda. **v2.9.2 (2026-08-26):** se agregan ~14 monedas propias de países latinoamericanos (MXN, ARS, COP, CLP, UYU, CUP, BOB, VES, DOP, HNL, NIO, PAB, HTG) más la de Senegal (XOF) - a propósito SIN aplicar acá el criterio de "un representante por símbolo" que se usó para el resto del catálogo (pedido explícito: aunque el símbolo se repita entre varias, cada país listado tiene su propia entrada).
* `PALETAS_DISPONIBLES` - lista de las 13 paletas de color disponibles.

*Horario:*
* `MODALIDADES_HORARIO` - valores fijos de modalidad de clase (presencial/semipresencial/virtual/personalizado/sin_clase).
* `crearModalidadHorario(tipo, textoPersonalizado)` - constructor del valor de modalidad (objeto, no string plano).
* `crearBloqueHorario({ materiaId, planEstudioId, nombre, apodo, grupo, dias, modalidad, aula, profesorId, enlace, notas, color })` - plantilla base de un bloque de horario para todo el semestre.
* `crearDiaCronograma({ numeroSemana, dia, modalidad })` - override de modalidad de un día puntual de una semana puntual.
* `obtenerClasesEfectivasSemana(bloque, numeroSemana)` - lista de clases efectivas de un bloque para una semana concreta, resolviendo overrides del cronograma.
* `calcularNumeroSemanaSemestre(semestre)` - número de semana 1-based dentro de un semestre, según `fecha_inicio`.
* `crearEnlaceHorarioCompartido({ fileId, permissionId, semestreId, apodoPropietario })` - registro de un enlace de horario compartido generado por el usuario.
* `crearAmigoVinculado({ fileId, nombre, color })` - registro de un horario de amigo vinculado al propio.

*Agenda:*
* `TIPOS_EVENTO_AGENDA` - `["evento", "tarea", "examen"]`.
* `crearEventoAgenda({ tipo, nombre, fecha, hora, materiaMatriculadaId, semestreId, notas, esFeriado })` - crea un evento/tarea/examen de agenda, ya sellado. Incluye `google_calendar_event_id: null` (2026-08-25, ver Calendario Secundario más abajo) y `perdida: false` (2026-09-21, estado "Perdido": exclusivo con `completada`, solo válido en `tarea`; `migrarDatosAntiguos` lo rellena en `false` en datos viejos y lo descarta si viene junto a `completada: true` o en otro tipo).
* `OFFSETS_RECORDATORIO_AGENDA` - offsets disponibles para un recordatorio (id/etiqueta/minutosAntes).
* `NOMBRE_CALENDARIO_SECUNDARIO` - (2026-08-25) `"AppAcademica"`, nombre exacto del calendario secundario de Google creado vía `calendars.insert` (ver `crearCalendarioSecundario` en auth.js).
* `COLOR_ID_GOOGLE_CALENDAR_POR_TIPO` - (2026-08-25) mapa tipo→colorId de evento de Google Calendar (tarea/examen/evento/feriado), usado por `core/notificaciones-calendario.js` al armar cada evento espejo.

*Comunidad:*
* `crearProfesor({ nombre, materias, correo, telefono })` / `crearCompanero({ nombre_completo, carnet, lista, materias_compartidas, nota, telefono })` - crean profesor/compañero.
* `obtenerHistorialProfesor(profesorId, datos)` - todas las materias matriculadas ligadas a un profesor, en todos los semestres.
* `obtenerUniversidadesDeProfesor(profesorId, datos)` - universidades donde dio clases un profesor. Devuelve strings vía `plan.universidad.siglas` (fallback a `nombre_completo`), mismo criterio que `calcularPromedioPorSemestreYUniversidad`.
* `obtenerMateriasCompartidasValidas(companero, datos)` - filtra las materias compartidas de un compañero a solo las que siguen apuntando a una matrícula real.
* `obtenerIntentosMateria(materiaId, planEstudioId, datos)` - todos los intentos reales de cursar una materia, cruzando todos los semestres.

*Utilitarios generales de colección:*
* `siguienteOrden(coleccion)` - próximo valor de `orden` para agregar algo al final (drag and drop).
* `reordenarPorArrastre(coleccion, idsEnNuevoOrden)` - reasigna `orden` secuencial tras un drag, sellando timestamp solo en los ítems que cambiaron.

**Si vas a agregar una llave nueva al modelo de datos (JSON de Drive), es acá.**

### core/storage-adjuntos.js
Propósito: orquesta el ciclo de vida completo de un adjunto: referencia liviana en el JSON (vía schema.js) + subida/descarga/borrado del binario real en Drive (vía auth.js), con cola de subida en memoria y limpieza de archivos huérfanos.
Depende de: schema.js, auth.js, storage-sync.js, storage.js
Exporta:
* `adjuntarArchivo(archivo, entidadTipo, entidadId)` - crea la referencia local al instante (con `subidaPendiente:true`) y encola el binario para subir en segundo plano. Devuelve la referencia creada.
* `procesarColaSubidas()` - procesa la cola de subidas pendientes en memoria; se reintenta sola tras adjuntar y al recuperar conexión.
* `descargarAdjunto(adjunto)` - descarga bajo demanda el binario y devuelve un Blob URL (quien llama debe revocarlo).
* `eliminarAdjunto(adjuntoId)` - tumba la referencia en el JSON + intenta borrar el binario real de Drive (best-effort, no crítico si falla).
* `procesarTumbasDriveHuerfanas()` - recorre tumbas de adjuntos buscando archivos de Drive que quedaron huérfanos (borrado registrado pero binario no borrado) y los limpia. Se engancha como hook post-fusión.
* `obtenerAdjuntosDe(entidadTipo, entidadId)` - helper de renderizado: adjuntos vigentes de una entidad puntual.

### core/storage.js
Propósito: estado global compartido (`estado`) por toda la app, caché offline en localStorage, y manejo del access_token de Google (guardar/leer/borrar con expiración).
Depende de: storage-sync.js (`ocultarAvisoReconexion`, `programarRefrescoProactivo` - import circular intencional, ver ARQUITECTURA.md).
Exporta:
* `estado` - objeto mutable compartido globalmente: `token`, `fileId`, `datos`, `pendienteSync`, `conexionDrive`, `ultimoModifiedTimeConocido`, `permisoPortapapeles`, etc.
* `CLAVE_CACHE_LOCAL` / `CLAVE_TOKEN_CACHE` - nombres de llave de localStorage para la caché de datos y de token respectivamente.
* `guardarTokenCache(token, expiresInSegundos)` - cachea `{ token, expiraEn }` en localStorage.
* `leerTokenCacheValido()` - devuelve `{ token, expiraEn }` solo si queda más de 5 min de vida; si no, `null`.
* `borrarTokenCache()` - limpia el token cacheado de localStorage.
* `establecerTokenActivo(token, expiresInSegundos)` - punto único para fijar un token válido: lo guarda en memoria y caché, programa el refresco proactivo, y marca "conexión OK".
* `authListo` - Promise que resuelve una sola vez cuando ya se supo si hay token utilizable (evita condiciones de carrera al iniciar).
* `resolverAuthListo` - función que resuelve la promesa `authListo`.
* `guardarCacheLocal()` - persiste `{ fileId, datos, pendienteSync }` en localStorage.
* `leerCacheLocal()` - lee la caché local; devuelve `null` y la descarta si está corrupta (JSON inválido).

### core/storage-merge.js
Propósito: toda la lógica de fusión (merge) de datos entre dispositivos al sincronizar - resuelve conflictos usando el reloj lógico de Lamport y respeta tumbas de borrado en cada colección anidada del modelo.
Depende de: schema.js (`observarRelojLogico`, `migrarDatosAntiguos`)
Exporta:
* `fusionarDatos(datosLocal, datosRemoto)` - punto de entrada principal; toda lectura de datos externos (Drive o caché) debe pasar por acá antes de aplicarse. También fusiona `tiempo_estudio_materias` junto con sus tumbas de borrado.
* `esMasReciente(a, b)` - compara dos entidades por su contador lógico `_actualizadoEn` (con desempate por `_dispositivoId`).
* `sonValoresEquivalentes(a, b)` - comparación profunda insensible al orden de llaves (evita falsos conflictos).
* `hayConflictoReal(local, remoto)` - determina si dos versiones de una entidad son un conflicto genuino o solo una edición secuencial.
* `marcarConflictoSiCorresponde(entidadLocal, entidadRemota, etiqueta)` *(interna, no exportada - la usan `fusionarColeccion`/`fusionarPlan`/`fusionarSemestre`/`fusionarCriterio`/`fusionarMateriaMatriculada`/`fusionarBloqueHorario`/`fusionarFinanzasSemestre`/`fusionarBloqueUnico`)* - **(2026-09-17, punto 2.1 de la auditoría - "¿el desempate es determinístico?")** antes conservaba SIEMPRE `entidadLocal` como versión "principal" y `entidadRemota` como `_version_alterna`. Como "local/remota" son etiquetas relativas a qué dispositivo está fundiendo, el MISMO choque quedaba espejado en cada dispositivo (cada uno mostraba y subía su propia versión como "la buena"), así que la foto vigente en Drive terminaba dependiendo de quién sincronizó último - justo lo que un conflicto marcado debería evitar. Ahora ordena el par con el mismo criterio determinista que ya usa `esMasReciente` (contador lógico, desempate por `_dispositivoId`), así que los dos dispositivos convergen SIEMPRE al mismo objeto (misma versión principal, misma `_version_alterna`) sin importar el orden de llegada. Las dos versiones siguen enteras - solo cambia cuál queda "arriba" mientras el usuario decide.
* `fusionarColeccion(coleccionLocal, coleccionRemota, tumbas, etiqueta)` - fusiona dos arreglos de entidades con `id` propio, respetando tumbas de ambos lados.
* `fusionarTumbas(tumbasLocal, tumbasRemota)` - unión simple de tumbas (un borrado nunca se pierde).
* `podarTumbasSuperadasPorAltas(tumbas, ...colecciones)` - **(nuevo 2026-09-19, Parte A)** descarta las tumbas que una ALTA posterior dejó obsoletas: una tumba `{ id, eliminadoEn }` se elimina si en cualquiera de las colecciones (local o remota) hay una entrada con ese `id` cuyo `unido_en` (Date.now(), misma escala) sea mayor. Entradas sin `unido_en` (anteriores al fix) nunca superan una tumba - comportamiento de siempre. Al no devolverla, la tumba también se purga de los datos guardados y desaparece de todos los dispositivos. Hoy solo se usa en `fusionarDatos` para `competencias_unidas` (única colección cuyo `id` se REUTILIZA al volver a entrar); el resto de las colecciones tienen ids únicos de por vida y siguen con la regla "la tumba gana siempre".
* `resolverConflicto(entidadConConflicto, cual, sellarTimestampFn)` - aplica la versión elegida por el usuario (local o alterna) y la re-sella limpia.
* `fusionarPlan(planLocal, planRemoto)` - funde un plan de estudios y sus colecciones internas (materias, categorías, etc.). `fusionarPlanesEstudio(local, remoto, tumbas)` *(colección de nivel superior, mismo archivo)* - **(2026-09-17, bug real encontrado en la auditoría, hallazgo no pedido por ningún punto puntual)** era la ÚNICA de sus funciones hermanas (`fusionarSemestres`/`fusionarCriterios`/`fusionarMateriasMatriculadas`/`fusionarBloquesHorario`/`fusionarFinanzasSemestres`, todas en este mismo archivo) que NO llamaba a `observarEntidadRemota()` sobre cada plan remoto - un plan que llegaba solo del lado remoto (`!existente`, ej. creado en otro dispositivo) entraba "gratis" sin que el reloj lógico de este dispositivo se enterara de su contador. Si el usuario editaba ESE plan poco después en este dispositivo, `sellarTimestamp` podía sellar con un contador más bajo que el que el otro dispositivo ya conocía, y esa edición se habría perdido en silencio en el siguiente sync (`esMasReciente` habría preferido al remoto viejo). Arreglado agregando la misma llamada que ya tienen todas sus hermanas.
* `fusionarSemestre(semestreLocal, semestreRemoto)` / `fusionarSemestres(local, remoto, tumbas)` - funden un semestre individual y la colección completa de semestres.
* `fusionarMateriaMatriculada(mmLocal, mmRemoto)` / `fusionarMateriasMatriculadas(local, remoto, tumbas)` - funden una matrícula individual (con sus criterios) y la colección completa.
* `fusionarCriterio(criterioLocal, criterioRemoto)` / `fusionarCriterios(local, remoto, tumbas)` - funden un criterio individual (con sus asignaciones) y la colección completa.
* `fusionarBloqueHorario(bloqueLocal, bloqueRemoto)` / `fusionarBloquesHorario(local, remoto, tumbas)` - funden un bloque de horario individual (con sus excepciones de semana) y la colección completa.
* `fusionarFinanzasSemestre(finSemLocal, finSemRemoto)` / `fusionarFinanzasSemestres(local, remoto, tumbas)` - **Becas y Pagos de Matrícula, Parte A (2026-09-12).** `pagos_matricula`/`ingresos_beca` son arrays anidados dentro de cada `finanzas_semestre` (no colecciones planas de nivel superior como `materias`/`gastos_u`), así que necesitaban el mismo patrón que `fusionarSemestre`/`fusionarPlan`: se funden ítem por ítem vía `fusionarColeccion()` genérica + tumbas propias (`_eliminados_pagos_matricula`/`_eliminados_ingresos_beca`), **excluidas** de la comparación "plana" del resto de campos del registro - si no, dos dispositivos que agregan/editan pagos DISTINTOS de la misma lista sin conexión se pisarían entre sí (todo el registro resolviéndose como un bloque único), en vez de fundirse campo por campo como sí pasa ahora.
* **Regla para entidades nuevas:** cualquier colección nueva con `id` propio se funde con `fusionarColeccion()` genérica - no escribir lógica de fusión nueva por tipo salvo que tenga sub-colecciones anidadas propias (patrón `fusionarPlan`/`fusionarSemestre`/`fusionarCriterio`).

### core/storage-sync.js
Propósito: orquestación completa de sincronización con Drive: refresco de token vía el Worker (`asegurarTokenValido`, sin popups - ver core/auth.js), subida/bajada de cambios, sondeo multi-dispositivo, pull-to-refresh, backup rotativo, indicador visual de estado y conteo de conflictos.
Depende de: config-ajustes.js, config-enlaces.js, main.js, plan-gestionar.js, plan-vista-lista.js, semestres.js, semestres-tarjetas.js, finanzas.js, ui/componentes.js, ui/tema.js, auth.js, schema.js, storage-merge.js, storage.js
> **Nota:** aunque `storage-sync.js` vive en `core/`, importa varios módulos de capas superiores (Plan, Semestres, Finanzas, Config, UI, `main.js`). Es **intencional**, no una violación de capas: son los módulos que hay que re-renderizar después de aplicar datos remotos frescos (`aplicarDatosRemotosFrescos`) o tras un login. Mismo patrón de import circular que `storage.js`↔`storage-sync.js` - ver ARQUITECTURA.md.
Exporta:
* `registrarHookPostFusion(fn)` / `registrarHookPostGuardado(fn)` - permiten a otros módulos (ej. storage-adjuntos.js) engancharse a eventos del ciclo de sync sin acoplar este archivo a ellos. **Fix 2026-09-08 (TDZ / import circular):** las 3 listas de hooks (`hooksPostFusion`, `hooksPostGuardado`, `hooksCierreSesionForzado`) vivían en un `const` de módulo - con imports circulares (este archivo importa `main.js`, que termina cargando transitivamente `horario-amigos.js`, que llama a `registrarHookPostGuardado()` al nivel de módulo, no dentro de una función), esa llamada podía ejecutarse ANTES de que este archivo llegara a correr su propio `const hooksPostGuardado = []` - todos los `import` de un módulo se resuelven antes que su propio código, sin importar en qué línea del archivo esté el `const` - y tirar `ReferenceError: Cannot access 'hooksPostGuardado' before initialization`. Las 3 listas pasaron a vivir como propiedad lazy (`??=`) de su propia función `registrarHook...`: una `function` (a diferencia de `const fn = () => {}`) está completamente disponible desde el primer instante de evaluación del módulo, sin TDZ, así que ya no importa en qué punto del ciclo de imports circulares se la llame.
* `asegurarTokenValido()` - (2026-08-25, reemplaza a `intentarReconexionSilenciosa`/`reconexionEnCurso`) punto único de "conseguir un access_token que sirva" para toda la app: revisa la caché válida, y si no hay, pide uno nuevo vía `refrescarAccessTokenViaWorker` (REST puro contra el Worker, nunca muestra ventana). Si no hay `refresh_token` guardado, o el refresco falla de verdad (revocado/vencido), muestra el aviso de reconexión y devuelve `false`. Deduplica refrescos en paralelo internamente.
* `programarRefrescoProactivo(expiresInSegundos)` - programa el refresco del token antes de que expire.
* `temporizadorRefrescoProactivo` - handle del timer de refresco proactivo.
* `mostrarAvisoReconexion()` / `ocultarAvisoReconexion()` - muestran/ocultan el banner de "reconectando" en la UI.
* `mostrarCargando()` / `ocultarCargando()` - controlan el overlay de carga (con contador anidado).
* `contadorCargando` - contador de llamados anidados a mostrarCargando/ocultarCargando.
* `inicializarPullToRefresh()` - gesto de "deslizar para refrescar" (Pointer Events, funciona en móvil y desktop).
* `conReintentoSi401(operacion)` - envuelve una operación de Drive; si falla con 401, llama a `asegurarTokenValido()` y reintenta una vez.
* `sincronizarAhora()` - sincronización completa "en el sitio": sube pendientes, baja lo último de Drive, repinta la UI sin recargar.
* `aplicarDatosRemotosFrescos(datosFrescos)` - bloque compartido que aplica datos ya descargados y repinta toda la UI.
* `marcarUltimaSincronizacionConfirmada()` - marca en la UI que la última sincronización se confirmó con éxito.
* `sondearCambiosRemotos()` - revisa cada pocos segundos si el archivo cambió en Drive desde otro dispositivo (solo `modifiedTime`, llamada barata).
* `sincronizarAlIniciar()` - sincronización especial al arrancar la app (sube Y baja).
* `inicializarSondeoAlVolver()` - reactiva el sondeo cuando la pestaña vuelve a estar visible/en foco.
* `ejecutarBackupSiToca()` - corre el ciclo de backup rotativo solo si ya toca según la frecuencia elegida; silencioso ante errores.
* `forzarBackupManual()` - corre el backup ignorando el intervalo, para el botón manual; sí propaga errores a la UI.
* `marcarCambioPendiente()` - se llama cada vez que se modifica algo en `estado.datos`; dispara intento de sync. **(2026-09-17, punto 1.1/1.2 de la auditoría)** ahora también incrementa `contadorCambiosLocales` (variable de módulo, no exportada) - es la foto que `ejecutarUnaSincronizacion()` compara antes/después de `guardarDatos()` para saber si algo se editó MIENTRAS la subida estaba en vuelo (ver abajo).
* `intentarSincronizar()` - sube los cambios pendientes a Drive. **(2026-09-17, punto 1.1)** si ya hay un ciclo en curso (`promesaSincronizacionEnCurso`), el disparo nuevo ya NO se descarta tras "subirse" a esa promesa: marca `resincronizarAlTerminar = true` (variable de módulo) para que se corra UN ciclo más apenas termine el actual - antes, un cambio que llegaba después de que el ciclo en vuelo ya hubiera pasado por `guardarDatos()` se perdía en silencio (no viajaba en ese PATCH y quedaba marcado como sincronizado igual).
* `ejecutarUnaSincronizacion()` *(interna)* - **(2026-09-17, punto 1.2, bug real de pérdida de datos)** antes ponía `estado.pendienteSync = false` incondicionalmente tras `guardarDatos()`, aunque hubiera habido una edición local DURANTE el `await` (que no viaja en ese PATCH, porque el body ya se serializó antes). Ahora compara `contadorCambiosLocales` antes/después de la subida: si no cambió, recién ahí limpia `pendienteSync` (y llama a `guardarCacheLocal()`, que antes faltaba acá); si cambió, deja `pendienteSync = true` y `resincronizarAlTerminar = true` para reintentar ya mismo con el estado completo. Al final de un sync exitoso llama a `avisarDatosSubidosAOtrasPestanas()` (punto 1.3).
* `inicializarCanalEntrePestanas()` - **(2026-09-17, puntos 1.3 y 3.4, nueva)** arma un `BroadcastChannel("app_academica_sync")` entre pestañas del mismo navegador (no-op si el navegador no lo soporta). Escucha dos mensajes: `datos-subidos` (otra pestaña sincronizó con éxito → si esta pestaña no tiene cambios propios pendientes, baja y funde vía `refrescarPorAvisoDeOtraPestana()`, sin esperar al próximo `visibilitychange`) y `sesion-cerrada` (otra pestaña cerró sesión → esta deja de sincronizar, ver `sesionCerradaEnOtraPestana` más abajo). Se llama una vez desde `DOMContentLoaded` en main.js.
* `avisarDatosSubidosAOtrasPestanas()` *(interna, no exportada)* - postea `datos-subidos` por el canal tras cada subida exitosa.
* `avisarCierreSesionAOtrasPestanas()` - **(2026-09-17, punto 3.4)** postea `sesion-cerrada` por el canal; la llama `cerrarSesion()` (main.js) ANTES de borrar nada local, para que las demás pestañas de este navegador dejen de reintentar sync con un token que ya está muerto.
* `refrescarPorAvisoDeOtraPestana()` *(interna)* - bajada+fusión disparada por el aviso `datos-subidos`; a diferencia de `sondearCambiosRemotos()`, corre aunque `document.hidden` sea `true` (para eso existe: refrescar justo la pestaña que está en segundo plano).
* `sesionCerradaEnOtraPestana` *(variable de módulo, no exportada)* - **(2026-09-17, punto 3.4)** en `true` corta de entrada tanto `intentarSincronizar()` como `sondearCambiosRemotos()`, para que una pestaña no quede reintentando en loop tras un cierre de sesión hecho en otra.
* `forzarCierreSesionPorFalloDeReconexion()` *(interna)* - **(2026-09-17, punto 3.3, el hallazgo más serio de la auditoría)** este camino dispara el hook de `cerrarSesion()` (main.js), que BORRA la caché local. Antes lo hacía incondicionalmente a los `MAX_INTENTOS_RECONEXION_SEGUIDOS` fallos; si en ese momento había cambios sin sincronizar, se perdían para siempre sin más aviso que un toast. Ahora, con `estado.pendienteSync` en `true`, NO cierra sesión: reinicia el contador de reintentos, deja el aviso de reconexión visible y muestra un toast explicando que no se cerró sesión para no perder los cambios.
* `contadorCambiosLocales` / `resincronizarAlTerminar` *(variables de módulo, no exportadas)* - ver `marcarCambioPendiente()`/`intentarSincronizar()`/`ejecutarUnaSincronizacion()` arriba.
* `forzarSincronizacion()` - fuerza el intento de sync ya, sin esperar el evento `online`.
* `contarConflictosGlobales()` - cuenta todos los conflictos de sync pendientes en cualquier parte de los datos (para el badge ⚠️).
* `actualizarBadgeConflictosGlobales()` - repinta el badge de conflictos con el número actual.
* `actualizarIndicadorSync()` - repinta el indicador visual de estado de sincronización.

### core/utils.js
Propósito: helpers genéricos sin estado propio - conversión de archivos a base64, formato de texto según preferencia del usuario, colores de badges, formato compacto de horas.
Depende de: storage.js (`estado`)
Exporta:
* `convertirArchivoABase64(archivo)` - lee un archivo tal cual (sin comprimir) y lo convierte a base64 (Promise).
* `convertirImagenABase64Comprimida(archivo, maxDimensionPx = 96, calidad = 0.8)` - redimensiona y recomprime una imagen antes de convertir a base64; conserva PNG si el formato tiene transparencia real, si no usa JPEG.
* `obtenerIniciales(texto)` - iniciales (hasta 2 letras) de un texto, en mayúsculas.
* `formatearHoras(materia)` - línea compacta de horas de una materia, iterando las llaves reales de `materia.horas`.
* `formatearHorasCompactoIniciales(materia)` - versión aún más compacta (solo iniciales) para la tarjeta colapsada.
* `aplicarFormatoTexto(texto)` - aplica el formato de nombres elegido en Configuración ("titulo"/"mayusculas"/"oracion"), respetando números romanos siempre en mayúsculas.
* `esTokenNumeroRomano(token)` - valida si una palabra es un número romano válido.
* `transformarPalabraFormato(palabra, formato, esPrimeraPalabra)` - aplica la transformación de una sola palabra según el formato elegido.
* `hexARgba(hex, alpha)` - convierte un color hex a `rgba(...)` con la opacidad indicada.
* `estiloBadgeCategoria(hex)` - string de estilo CSS inline para un badge de categoría (fondo translúcido + borde + texto del color).

### core/notificaciones-calendario.js
Propósito: sincronización de recordatorios de Agenda (tareas, exámenes, eventos) con un Calendario Secundario de Google ("AppAcademica") por usuario - reemplaza por completo a `core/notificaciones-push.js` (Web Push + VAPID + Cron en el Worker, dado de baja el 2026-08-25 por no llegar de forma confiable en Doze Mode/Safari). Habla DIRECTO contra la API de Google Calendar (helpers crudos en auth.js) usando el access_token de la sesión - ya NO usa el Worker de Cloudflare para nada de esto (el Worker solo sigue vivo para `/oauth/exchange`/`/oauth/refresh`). Toda llamada de red es "best-effort": si Calendar no responde, nunca bloquea ni revierte la acción real del usuario en Agenda.
Depende de: schema.js, storage-sync.js, storage.js, utils.js, auth.js, ui/componentes.js
Exporta:
* `sincronizacionCalendarActiva()` - lee el switch de Ajustes (`estado.datos.configuracion.sincronizar_calendario_google`).
* `activarSincronizacionCalendario()` - crea el calendario secundario si hace falta (`asegurarCalendarioSecundario`, interna), prende el switch, y sincroniza en lote todo lo pendiente + el Resumen Diario. Devuelve `true`/`false` según haya quedado activo (falla si no se otorgó el scope de Calendar, o si no se pudo crear el calendario).
* `desactivarSincronizacionCalendario()` - apaga el switch y borra de Calendar todos los eventos espejados + el evento recurrente del Resumen Diario (el calendario secundario en sí NO se borra).
* `sincronizarEventoCalendario(evento)` - (re)crea/actualiza el espejo de un `EventoAgenda` en Calendar vía `events.insert`/`events.update`; si `evento.completada` es `true`, lo elimina en vez de sincronizarlo. **2026-09-21:** también elimina el espejo si `evento.perdida` es `true`; `resincronizarTodaLaAgendaConCalendar` y `generarTextoResumenHoy` (Resumen Diario) también excluyen las perdidas. No hace nada si el switch está apagado o falta el scope de Calendar.
* `eliminarEventoCalendarizado(evento)` - elimina el espejo de un evento en Calendar. A diferencia del viejo `cancelarRecordatorioPush(eventoId)`, necesita el objeto `evento` COMPLETO (con `google_calendar_event_id`), no solo el id.
* `sincronizarResumenDiario()` - crea/actualiza/borra el ÚNICO evento recurrente (`RRULE:FREQ=DAILY`) del Resumen Diario según `configuracion.notificaciones_resumen_diario`; si cambia la hora, actualiza ese mismo evento (nunca crea uno nuevo).
* `ofrecerActivarSincronizacionCalendario()` - diálogo de onboarding (se llama una única vez desde `main.js`, tras el primer login de una cuenta nueva).

> **Integración pendiente de confirmar (2026-08-25):** `agenda.js`/`agenda-modal.js` deben actualizarse para llamar a `sincronizarEventoCalendario`/`eliminarEventoCalendarizado` en vez de las funciones viejas de `notificaciones-push.js` (ver nota de cabecera del archivo); `main.js` debe cambiar su import e invocación de `ofrecerActivarNotificacionesPush` a `ofrecerActivarSincronizacionCalendario`, y su listener de `serviceWorker.addEventListener("message", ...)` (salto a Agenda al tocar una notificación push) ya no aplica - el deep link del Resumen Diario ahora es una URL normal (`?abrir=resumen`) que abre el navegador directo, sin pasar por un Service Worker. Ninguno de los 3 archivos se tocó en esta sesión (no se subieron).

---

## JS - ui

### ui/componentes.js
Propósito: componentes de UI reutilizables en toda la app - modal de confirmación genérico, toasts, long-press, flechas de scroll horizontal, layout responsivo del sidebar/drawer, selector de modalidad de horario.
Depende de: main.js (import circular intencional - ver ARQUITECTURA.md), core/schema.js
Exporta:
* `CLAVE_SIDEBAR_COLAPSADA` - clave de localStorage donde se guarda si el sidebar está colapsado.
* `confirmarUniversidadNoInvertida({ siglas, nombre_completo, nombrePlan })` *(nueva, 2026-09)* - devuelve una Promise con `{ siglas, nombre_completo }` ya revisados. Si las siglas son MAS LARGAS que el nombre completo (señal de que se escribieron al revés) abre un aviso bloqueante (sin X, sin Esc, sin click afuera) cuyos dos botones ("Sí, me equivoqué" = intercambia, "No, así está bien" = deja tal cual) se habilitan recién a los 5 s; si no hay señal, resuelve al instante sin modal. Usada por los 3 formularios que guardan una universidad: `inicializarModalCrearPlan` (plan-esquema.js, bloque "Otra"), `inicializarModalEditarPlanInfo` (plan-gestionar.js) e `inicializarModalCompletarUniversidades` (main.js).
* `abrirConfirmacion({titulo, mensaje, textoConfirmar, claseConfirmar, onConfirmar})` - abre el modal de confirmación reutilizable.
* `abrirDrawerEnlacesMovil()` - abre el drawer móvil de Enlaces rápidos con su propio overlay y bloquea el scroll de fondo.
* `agregarLongPress(el, callback, duracionMs=500)` - ejecuta `callback` al mantener presionado (~500ms) o hacer clic derecho sobre un elemento.
* `callbackConfirmacionActual` - referencia al callback pendiente del modal de confirmación actualmente abierto.
* `cerrarConfirmacion()` - cierra el modal de confirmación y limpia el callback pendiente.
* `cerrarDrawerEnlacesMovil()` - cierra el drawer móvil de Enlaces rápidos.
* `cerrarSidebarMovil()` - cierra el sidebar principal en vista móvil.
* `construirSelectorModalidad(valorInicial, onCambiar)` - arma el selector de modalidad de horario; devuelve `{ elemento, obtenerValor() }`.
* `desplazarYResaltarElemento(selector, intentosRestantes=15)` - hace scroll hasta un elemento y lo resalta, reintentando varios frames si aún no existe en el DOM.
* `envolverConFlechasScroll(elementoScroll)` - agrega flechas de scroll horizontal que solo aparecen cuando el contenido desborda el ancho disponible.
* `inicializarAutoScrollSelectoresEnModales()` - activa el auto-scroll hacia el ítem activo dentro de selectores ubicados en modales.
* `inicializarBotonesCerrarModal()` - engancha los botones "X" de cierre de modales, reutilizando los listeners de "clic afuera cierra" ya existentes.
* `inicializarLayoutResponsivo()` - configura el comportamiento responsivo del sidebar/drawer según el tamaño de pantalla.
* `inicializarModalConfirmacion()` - engancha los listeners del modal de confirmación (botón, clic afuera).
* `inicializarNavegacionBotonesMouse()` - habilita la navegación con los botones de mouse (adelante/atrás) respetando el orden/visibilidad configurados en Ajustes.
* `mostrarPantallaCargaSesion()` / `ocultarPantallaCargaSesion()` - overlay de marca propia (`#overlay-carga-sesion`, ver index.html/design-system.css) que reemplaza a `#pantalla-login` mientras la app decide si puede restaurar la sesión sola, y también mientras `onLoginExitoso` (main.js) trae el archivo de datos de Drive tras un login real. Separado a propósito de `mostrarCargando()`/`ocultarCargando()` (storage-sync.js, los "3 puntitos" genéricos).
* `mostrarToast(mensaje, duracionMs=2400)` - muestra un toast temporal.
* `mostrarToastAccion(mensaje, textoBoton, alConfirmar)` - muestra un toast persistente con un botón de acción, para avisos que requieren confirmación del usuario en vez de desvanecerse solos.
* `restaurarEstadoSidebar()` - restaura el estado colapsado/expandido del sidebar guardado en localStorage.

### ui/paleta-personalizada.js
Propósito: flujo para crear una paleta de colores personalizada ("Crear mi paleta", 15ª opción del selector de paletas), generado 100% por JS (overlay + modal).
Depende de: core/schema.js, core/storage-sync.js, core/storage.js, ui/tema.js
Exporta:
* `iniciarFlujoPaletaPersonalizada({alGuardar}?)` - punto de entrada llamado desde config-ajustes.js; abre el overlay del flujo de creación de paleta. `alGuardar` es un callback (típicamente `renderizarAjustes`) para refrescar el grid de paletas al terminar.

### ui/tema.js
Propósito: aplicación de paletas y modo claro/oscuro sobre `<html>`, más utilidades puras de color (conversión, mezcla, derivación de variables CSS) usadas tanto por el tema fijo como por la paleta personalizada.
Depende de: ninguno (funciones puras + DOM/localStorage)
Exporta:
* `COLORES_PREVIEW_PALETA` - mapa paleta → colores reales, para pintar cada cuadro del selector con su propio color sin depender de la paleta activa.
* `FONDO_PREVIEW_AZUCARADO` - degradado CSS de manchas de color usado como preview de la paleta "azucarado".
* `TEXTO_PREVIEW_PALETA` - mapa paleta → color de texto legible sobre su degradado (ej. "blanco" necesita texto oscuro).
* `aplicarPaleta(paleta, modo, coloresPersonalizados?)` - setea `data-palette`/`data-mode` en `<html>`, persiste en localStorage y aplica o limpia los colores personalizados inline.
* `aplicarTemaGuardadoLocalmente()` - aplica al arranque la paleta/modo guardados en localStorage (incluye colores personalizados si corresponde).
* `obtenerModoTemaLocal()` / `guardarModoTemaLocal(modo)` - leen/guardan `light|dark` por dispositivo; si no hay valor, consultan `prefers-color-scheme` y caen a `dark`.
* `obtenerModoDisenoLocal()` / `guardarModoDisenoLocal(modo)` - leen/guardan `optimizado|fancy` en localStorage por dispositivo; el default es `optimizado`.
* `actualizarThemeColorMeta()` - sincroniza `<meta name="theme-color">` con `--bg-header-solido` de la paleta activa, para que la barra del sistema combine en PWA.
* `hexARgb(hex)` - convierte un color hex a `{r,g,b}`.
* `colorARgb(color)` - convierte hex o `rgb()`/`rgba()` a `{r,g,b}`, ignorando el canal alfa.
* `colorARgba(color)` - igual que `colorARgb` pero conservando el canal alfa real (`{r,g,b,a}`).
* `compositarSobreFondo(colorConAlpha, colorFondoHex)` - compone un color translúcido sobre un fondo dado y devuelve el hex sólido visualmente equivalente.
* `rgbAHex(r, g, b)` - convierte `{r,g,b}` a hex de 6 dígitos.
* `colorAHex(color)` - convierte cualquier color (hex o rgba string) a hex sólido de 6 dígitos.
* `rgbAHsl(r, g, b)` - convierte `{r,g,b}` a `{h,s,l}`.
* `hexAHsl(hex)` - convierte hex a `{h,s,l}`.
* `hslARgb(h, s, l)` - convierte `{h,s,l}` a `{r,g,b}`.
* `hslAHex(h, s, l)` - convierte `{h,s,l}` a hex.
* `mezclarHex(colorA, colorB, factor)` - mezcla lineal entre dos colores hex (factor 0 = colorA puro, 1 = colorB puro).
* `hexARgba(color, alpha)` - convierte un color a string `rgba(...)` con la opacidad indicada.
* `luminanciaRelativa(color)` - calcula la luminancia relativa (WCAG) de un color.
* `esColorClaro(color)` - indica si un color es "claro" según su luminancia relativa (>0.5).
* `calcularGradientesAcento({accent1, accent2, degradado})` - genera las 3 variantes de gradiente de acento (`--gradient-accent`/`-alt`/`-alt2`) según los colores base y el degradado configurado.
* `calcularVariablesDerivadas(colores)` - a partir de los 6 colores elegidos por el usuario, calcula todas las variables CSS derivadas que necesita una paleta (texto, paneles, glows, badges, etc.).
* `aplicarColoresPersonalizadosInline(colores)` - aplica las variables derivadas como estilos inline sobre `:root` (mayor prioridad que las reglas `[data-palette]`).
* `limpiarColoresPersonalizadosInline()` - remueve todas las variables CSS inline de la paleta personalizada, para volver limpio a una paleta fija.

---

## JS - config

### config/config-ajustes.js
Propósito: renderiza la sección de Ajustes (paletas, modo claro/oscuro, escala de notas, nota de aprobación por plan/universidad, formato de texto, backup rotativo a Drive, modo rendimiento, sincronización de recordatorios con Google Calendar, config de días de Horario, clave de API de Gemini para Asistente IA).
Depende de: core/notificaciones-calendario.js, core/schema.js, core/storage-adjuntos.js, core/storage-sync.js, core/storage.js, core/utils.js, plan/plan-vista-lista.js, semestres/semestres.js, ui/componentes.js, ui/tema.js, ui/paleta-personalizada.js
Exporta:
* `renderizarAjustes()` - reconstruye toda la sección de Ajustes (paletas, escalas, moneda, backup, sincronización con Google Calendar, etc.) e inicializa el accordion. Desde 2026-08-22 también llama a `inicializarAsistenteAjustes()` (interna, no exportada) - guarda/reemplaza/borra `configuracion.gemini_api_key`, muestra la clave enmascarada (últimos 4 caracteres) si ya hay una guardada, y llama a `window.aplicarVisibilidadBotonAsistente()` (main.js) al guardar o borrar. También llama a `renderizarNotificacionesRecordatorios()` y `renderizarNotificacionesResumenDiario()` (internas, no exportadas - reconstruidas 2026-08-23 tras perderse en un merge, ver más abajo).
* `renderizarSeccionBackupDrive()` - pinta el bloque de frecuencia/estado del backup rotativo a Drive; solo lee/escribe la preferencia, nunca dispara un backup a mano.
* `renderizarSeccionLiberarEspacio()` - (no documentada hasta ahora) pinta el bloque de Ajustes "Liberar espacio" (borrado en lote de adjuntos) dentro de `#seccion-liberar-espacio`; si `hayAdjuntosGuardados()` (core/storage-adjuntos.js) devuelve `false`, oculta el contenedor entero y no dibuja nada. Dos modos: por semestre (selector, con botones separados para adjuntos de Cronograma vs. de Tareas, más "Borrar todo este semestre") y global para eventos sueltos sin semestre. Cada botón pide confirmación (`abrirConfirmacion`, ui/componentes.js) antes de borrar - es destructivo e irreversible, borra también el archivo real en Drive vía `eliminarAdjuntosDe*` (core/storage-adjuntos.js), no solo la referencia local. Se re-llama a sí misma al terminar un borrado exitoso para refrescar el estado (ej. ocultarse si ya no queda nada). Llamada desde `renderizarAjustes()`.
* `aplicarModoRendimiento(activo)` - aplica/quita el atributo `data-rendimiento` en `<html>`. Desde v3.20.0 el pill Optimizado/Fancy lee y escribe la preferencia local del dispositivo y no modifica `estado.datos.configuracion` ni inicia sincronización.
* `DIAS_SEMANA_CONFIG` - arreglo con id/etiqueta/abreviatura por defecto de cada día de la semana, usado en la config de días de Horario.

Funciones internas (no exportadas) relevantes:
* `renderizarNotificacionesRecordatorios()` - pinta un grupo de chips de selección múltiple (`construirSelectorChipsMultiple`, ui/componentes.js) por cada tipo de evento de Agenda (tarea/examen/evento/feriado, ver `ETIQUETAS_TIPOS_RECORDATORIO_AGENDA`) dentro de `#seccion-notificaciones-recordatorios`. Lee/escribe `configuracion.notificaciones_recordatorios[tipo]` contra `OFFSETS_RECORDATORIO_AGENDA` (core/schema.js). Atenúa y bloquea el bloque completo si el switch general de sincronización con Google Calendar está apagado, sin perder los valores guardados.
* `renderizarNotificacionesResumenDiario()` - switch + selector de hora (mismo patrón visual que `construirSelectCustomAjustes`) para `configuracion.notificaciones_resumen_diario` (`{ activo, hora }`). Cada cambio llama a `sincronizarResumenDiario()` (core/notificaciones-calendario.js, 2026-08-25: antes creaba un evento por día contra el Worker, ahora es un único evento recurrente en Google Calendar). Reconstruida 2026-08-23: se había perdido por completo (junto con `renderizarNotificacionesRecordatorios`) en el merge que integró el Asistente IA a este mismo archivo - la sección "Notificaciones" de Ajustes quedó con el switch general funcionando pero sin recordatorios por tipo ni resumen diario.
* `renderizarSelectorMoneda()` - pinta el selector custom de `configuracion.moneda_preferida` (mismo patrón `.select-custom` que "Escala de notas") a partir de `MONEDAS_DISPONIBLES` (core/schema.js). **v2.9.2 (2026-08-26, pedido explícito):** se agrega un `<input>` de búsqueda por texto (símbolo, nombre o código ISO, sin distinguir mayúsculas/acentos) pegado arriba de la lista desplegable - con ~50 monedas ya no era práctico desplazarse a mano. El buscador se reparenta a `document.body` junto con la lista (antes solo se reparentaba el `<ul>`) para que viajen juntos al abrir/cerrar, y se limpia solo cada vez que el dropdown se abre.

### config/config-baneados.js
Propósito: placeholder vacío reservado para cuando se construya la sección de Baneados. No llenar de código de otra cosa por error.
Depende de: (ninguno)
Exporta: (nada - solo `export {}`)

### config/config-enlaces.js
Propósito: gestiona los Enlaces rápidos de Configuración - listar, agregar, editar, eliminar y renderizarlos en los distintos contenedores donde aparecen (Configuración, panel lateral, drawer móvil).
Depende de: core/schema.js, core/storage-sync.js, core/storage.js, core/utils.js
Exporta:
* `renderizarEnlacesRapidos()` - repinta las 3 listas de enlaces rápidos (Configuración, panel lateral, drawer móvil) y habilita/deshabilita el botón "Agregar" según el límite.
* `renderizarListaEnlacesEn(contenedorId, enlaces, conEditar)` - dibuja una lista de enlaces dentro de un contenedor dado; `conEditar` controla si aparece el lápiz de edición.
* `inicializarModalEnlace()` - engancha los listeners del modal de alta/edición de enlace (pills de tipo de ícono, guardar, cancelar, eliminar).
* `abrirModalEnlace(enlaceId?)` - abre el modal en modo alta o edición (si se pasa un id) y precarga sus datos.
* `cerrarModalEnlace()` - cierra el modal y limpia el id en edición.
* `eliminarEnlaceDesdeModal()` - borra el enlace en edición dejando una tumba en `_eliminados_enlaces` (evita que resucite al sincronizar).
* `guardarEnlaceDesdeModal()` - valida y persiste el enlace (nuevo o editado), sellando timestamp antes de marcar el cambio pendiente.
* `mostrarErrorModalEnlace(mensaje)` - muestra un mensaje de error dentro del modal de enlace.

---

## JS - plan

### plan/plan-categorias.js
Propósito: gestión de categorías personalizadas de materias (crear/editar categorías y asignar/quitar materias de cada una).
Depende de: schema.js, storage-sync.js, storage.js, utils.js, componentes.js, plan-esquema.js, plan-vista-lista.js
Exporta:
* `abrirModalCategoria(categoria, plan)` - abre el modal de crear/editar categoría; precarga nombre y color si `categoria` viene de editar, o lo deja vacío si es nueva.
* `abrirModalCategoriaMaterias(plan, categoria)` - abre el modal de asignación de materias a una categoría, precargando como seleccionadas las que ya pertenecen a ella.
* `construirPanelCategorias()` - construye la sección del panel de categorías del plan activo.
* `inicializarModalCategoria()` - registra los listeners del modal crear/editar categoría (cancelar, eliminar, guardar).
* `inicializarModalCategoriaMaterias()` - registra los listeners del modal de asignación de materias a categoría.
* `renderizarControlesCategoriaMaterias(plan, categoria)` - renderiza el buscador y los controles de orden dentro del modal de materias por categoría.
* `renderizarListaMateriasCheckbox(plan, categoria)` - renderiza la lista de checkboxes de materias disponibles para asignar/desasignar de la categoría.

### plan/plan-detalle.js
Propósito: construye el contenido de detalle de una materia (tarjeta expandida en la lista y modal flotante) - requisitos, correquisitos, historial de intentos y búsqueda inversa ("Desbloquea"). Reutilizado por `semestres/semestres-tarjetas.js` para la tarjeta de materia matriculada.
Depende de: schema.js, storage.js, storage-sync.js, utils.js, componentes.js, plan-esquema.js, plan-vista-lista-tarjetas.js, semestres.js
Exporta:
* `abrirModalDesbloquea(materia, plan)` - abre el modal "Es requisito para:" listando las materias que `materia` desbloquea.
* `abrirModalHistorial(materia, plan)` - abre el modal de historial de intentos de la materia: cruza `materia.id`+`plan.id` contra todos los semestres y muestra cada intento con su estado efectivo.
* `abrirModalRequisito(codigo)` - abre el modal navegable de detalle de una materia a partir de su código.
* `construirBloqueCompletoRequisitos(materia, plan)` - arma el bloque combinado de Requisitos + Correquisitos + etiqueta de cupo original.
* `construirBloqueRequisitos(etiqueta, nodoRaiz, modo)` - construye un bloque individual (Requisitos o Correquisitos) a partir del árbol Y/O; omite el bloque completo si está vacío salvo para "Requisitos".
* `construirBotonesFinalesDetalle(materia, plan, opciones)` - construye la fila de botones finales del detalle (Desbloquea, Historial, etc.), variando según si es modal o tarjeta. Fix 2026-08-21 (bug Mochi): si `opciones.esModal` es true, los botones "Es requisito"/"Historial" cierran `#modal-requisito` antes de abrir Desbloquea/Historial respectivamente (para no dejar dos overlays de modal apilados a la vez) y marcan el flag `volverAModalRequisitoAlCerrar` para que, si Desbloquea/Historial se cierran sin elegir nada, se vuelva a mostrar la tarjeta de origen en vez de quedar sin ningún modal abierto.
* `construirColumnaAccionesTarjeta(materia, plan)` - construye la columna de acciones (badge de categoría y botones) de la tarjeta expandida.
* `construirCuerpoDetalleMateria(materia, plan, opciones)` - punto de entrada que decide si arma el cuerpo en modo "tarjeta" (grid 2 columnas) o "modal" (1 columna).
* `construirCuerpoDetalleModal(materia, plan)` - arma el cuerpo de detalle en el layout de 1 columna usado por el modal flotante.
* `construirCuerpoDetalleTarjeta(materia, plan)` - arma el cuerpo de detalle en el grid de 2 columnas usado por la tarjeta expandida en la lista.
* `construirFilaRequisito(codigo, opciones)` - construye la fila visual de un requisito individual (código de materia + su estado).
* `construirLinea1Materia(materia, plan)` - construye la línea 1 del detalle (Bloque·Código + Categoría).
* `construirLinea2Materia(materia, compacto, plan)` - construye la línea 2 (badge de Estado efectivo + Horas/Créditos), en versión compacta o completa.
* `construirLineaCategoriaMateria(materia, plan)` - construye la línea de badge de categoría de la materia, o `null` si no tiene categoría. Reutilizado por `semestres-tarjetas.js`.
* `construirMetaLineaMateria(materia, plan)` - construye la línea de metadatos "Bloque N · Código" (o "Optativa · Código").
* `construirNodoRequisito(nodo, modo, profundidad)` - renderiza recursivamente un nodo del árbol de requisitos (hoja código, o grupo Y/O).
* `inicializarModalDesbloquea()` - registra los listeners de cierre del modal "Desbloquea".
* `inicializarModalHistorial()` - registra los listeners de cierre del modal de historial.
* `inicializarModalRequisito()` - registra los listeners de "Es requisito", "Historial" y "Cerrar" del modal de requisito.
* `abrirModalAsignarProfesorDesdeHistorial(mm, materia, plan, semestre, onVinculado)` - abre un overlay para asignar/vincular un profesor a un intento del historial de la materia.
* `registrarAbrirAltaProfesorPreseleccionado(fn)` - registra el callback (definido en comunidad.js) que abre el alta de profesor con datos preseleccionados; evita import circular con comunidad.js.

### plan/plan-esquema.js
Propósito: núcleo de acceso a los planes de estudio (plan activo/secundario, materias visibles/optativas/a revisar) y modales para crear plan / añadir materia manual / vincular optativa. Los getters de este archivo son la base que usa casi todo `plan/`.
Depende de: schema.js, storage-sync.js, storage.js, plan-gestionar.js, plan-importacion-csv.js, plan-vista-lista.js
Exporta:
* `EJEMPLOS_PLACEHOLDER_PLAN` - placeholders de ejemplo de Carrera/Código según universidad (TEC, UCR, etc.) para los inputs del modal de crear plan.
* `LIMITE_PLANES_ESTUDIO` - número máximo de planes de estudio que el usuario puede tener (3).
* `abrirModalCrearPlan(paraSecundario, metadatosDetectados)` - abre el modal de crear plan nuevo, marcando si es el plan secundario y precargando metadatos si vienen de una importación. Si `metadatosDetectados.universidad` no mapea a TEC/UCR, precarga el bloque "Otra" con nombre completo + siglas detectadas (`siglas_universidad`, línea `SIGLAS_UNIVERSIDAD:` del CSV - ver `extraerMetadatosImportacion` en plan-importacion.js); el campo "Otra" arranca vacío si no hay nada detectado (2026-08-22: se eliminó la precarga muerta desde `estado.nombreUniversidadImportacion`/`siglasUniversidadImportacion`, que nunca se asignaban).
* `abrirModalMateriaManual(materiaExistente, planDeLaMateria)` - abre el modal "+ Añadir materia" en modo alta o edición, según si se pasa una materia existente.
* `abrirModalVincularOptativa(materiaTemplate, plan, origen)` - abre el modal para vincular una optativa disponible o una materia a revisar; `origen` indica de cuál arreglo especial viene.
* `actualizarFormatoHorasMateriaManual()` - actualiza los campos de horas del modal de materia manual según los `tipos_horas` del plan activo.
* `aplicarDefaultsUniversidad(universidad)` - aplica los valores por defecto (nombre de bloque, semanas, hora de inicio, etc.) de la universidad elegida a los inputs del modal de crear plan.
* `aplicarPlaceholdersAleatoriosPlan(universidad)` - aplica placeholders aleatorios de ejemplo (Carrera/Código) a los inputs del modal según la universidad.
* `buscarMateriaPorCodigoEnPlanes(codigo)` - busca una materia por código entre todas las materias visibles (plan principal + secundario). Devuelve la fila `{materia, plan, origen}` o `null`.
* `elegirPlaceholderPlan(universidad)` - elige al azar un ejemplo de placeholder de `EJEMPLOS_PLACEHOLDER_PLAN` para la universidad dada.
* `filasFiltradas()` - devuelve las materias visibles filtradas por categoría activa y texto de búsqueda actuales.
* `inicializarModalCrearPlan()` - registra los listeners del modal de crear plan (selector de universidad, guardar, cancelar). El handler de guardar arma `universidad` como `{ nombre_completo, siglas }`: TEC/UCR lo arman solos vía `NOMBRES_UNIVERSIDAD_PRESET`; "Otra" exige ambos campos completos (bloquea con error si falta alguno) - 2026-08-22. Desde 2026-09, en "Otra" el guardar pasa por `confirmarUniversidadNoInvertida` (componentes.js) antes de armar `universidad`.
* `inicializarModalMateriaManual()` - registra los listeners del modal de añadir/editar materia manual.
* `inicializarModalVincularOptativa()` - registra los listeners del modal de vincular optativa.
* `mapearUniversidadDetectada(texto)` - infiere el código de universidad (TEC/UCR/Otra) a partir de un texto libre detectado en una importación.
* `obtenerMateriasRevisar()` - devuelve todas las filas `{materia, plan, origen}` de `materias_revisar` del plan principal y secundario.
* `obtenerMateriasVisibles()` - devuelve todas las filas `{materia, plan, origen}` de materias regulares del plan principal y secundario.
* `obtenerOptativasDisponibles()` - devuelve todas las filas `{materia, plan, origen}` de `optativas_disponibles` del plan principal y secundario.
* `obtenerPlanActivo()` - devuelve el plan de estudio marcado como activo (`configuracion.plan_activo_id`), o `null`.
* `obtenerPlanSecundario()` - devuelve el plan secundario de Modo Hardcore.
* **Deuda conocida:** con Modo Hardcore a 3 planes, falta un `obtenerPlanTerciario()` equivalente a `obtenerPlanSecundario()` - hoy nada lo necesita porque `js/semestres/` usa `obtenerPlanesActivos()` de `schema.js` directamente, pero si otro módulo llega a necesitar el plan terciario suelto, falta ese getter acá.

### plan/plan-gestionar.js
Propósito: modal de gestión de planes de estudio (listar, editar info, eliminar, elegir plan secundario/terciario en Modo Hardcore).
Depende de: schema.js, storage-sync.js, storage.js, utils.js, componentes.js, plan-esquema.js, plan-vista-lista.js
Exporta:
* `abrirModalEditarPlanInfo(plan)` - abre el modal de editar info del plan (carrera, universidad, código), precargando los valores actuales.
* `abrirModalGestionPlanes()` - abre el modal de gestión de planes; de paso auto-corrige `plan_activo_secundario_id` si quedó en `null` por datos viejos.
* `eliminarPlanEstudio(planId)` - elimina un plan de estudio del arreglo local y marca la tumba correspondiente para que el borrado se propague por sync.
* `inicializarModalEditarPlanInfo()` - registra los listeners del modal de editar info del plan. Desde 2026-09, el guardar pasa por `confirmarUniversidadNoInvertida` (componentes.js) antes de asignar `plan.universidad`.
* `inicializarModalGestionPlanes()` - registra los listeners del modal de gestión de planes, incluido el cierre que resetea el mini-panel de importación.
* `recalcularPlanesHardcore(cfg)` - recalcula automáticamente cuáles planes participan como secundario/terciario en Modo Hardcore (todos los que no son el principal, en orden de aparición).
* `renderizarListaGestionPlanes()` - renderiza la lista de planes dentro del modal de gestión.
* `renderizarModoHardcore()` - renderiza los controles de Modo Hardcore; siempre relee `estado.datos.configuracion` fresco en cada handler.
* `renderizarSelectorPlan()` - renderiza el selector de plan activo (o el mensaje de "no tienes ningún plan" si la lista está vacía).

### plan/plan-importacion-csv.js
Propósito: parseo del CSV pegado/generado por IA hacia el modelo de plan de estudios, y su contraparte de serialización (árbol de requisitos, tipos de horas).
Depende de: schema.js, storage-sync.js, storage.js, componentes.js, plan-esquema.js, plan-importacion.js, plan-vista-lista.js
Exporta:
* `actualizarEstadoBotonesEnvioImportacion()` - habilita/deshabilita el botón "Enviar a Claude" según si el modo es "link" y el link está vacío.
* `construirMiniPanelImportacion(plan)` - construye el mini-panel de importación de CSV embebido en el modal de gestión de planes.
* `derivarTiposHorasDeHorasColumnas(horasColumnasCrudo)` - convierte el texto crudo de columnas de horas (ej. "Teoría,Práctica") en un arreglo de tipos de horas.
* `importarCSVEnPlan(textoCSV, planDestino)` - parsea un CSV crudo (extrayendo y descartando sus líneas de metadatos) y vuelca su contenido en `planDestino`.
* `manejarClickImportar(textoCSV)` - handler del botón de importar: valida que haya CSV pegado y dispara el flujo de importación, mostrando errores si falla.
* `materiaPareceOptativa(materia)` - determina si una materia es un cupo de electiva/optativa sin llenar, basado únicamente en `materia.sin_definir`.
* `mostrarErroresImportacion(lista)` - muestra u oculta el bloque de errores de importación de CSV con la lista de mensajes dada.
* `obtenerPalabraOptativa(materia)` - devuelve "electiva" u "optativa" según lo que sugiera el nombre de la materia (puramente cosmético, nunca afecta datos).
* `parsearCSVPlanEstudios(textoCrudo, tiposHoras)` - parsea el CSV completo de un plan según sus `tiposHoras`; devuelve `{materias, electivas, paraRevisar, errores}`.
* `parsearLineaCSV(linea)` - parser de una línea CSV que respeta comillas dobles (para nombres con comas).
* `parsearRequisitoArbol(celdaCruda)` - convierte el texto de una celda de Requisitos/Correquisitos en un árbol Y/O de nodos.
* `serializarRequisitoArbol(nodo)` - convierte un árbol Y/O de requisitos de vuelta a su representación de texto (";" para Y, "/" para O).

### plan/plan-importacion.js
Propósito: flujo de importación asistida por IA (capturas → PDF, prompt de importación, instrucciones, extracción de metadatos) y su panel en el modal de gestión de planes.
Depende de: clipboard.js, schema.js, storage-sync.js, storage.js, componentes.js, plan-esquema.js, plan-gestionar.js, plan-importacion-csv.js, plan-vista-lista.js
Exporta:
* `abrirModalCapturasPDF()` - abre el modal de capturas de pantalla → PDF, limpiando la selección de archivos anterior.
* `abrirModalInstruccionesImportacion(modo, textoPrompt)` - abre el modal de instrucciones previas a ir a Claude, guardando el prompt pendiente.
* `abrirVentanaNueva(url)` - abre una URL en una pestaña nueva simulando un click en un `<a target="_blank">`.
* `cerrarModalCapturasPDF()` - cierra el modal de capturas de pantalla → PDF.
* `cerrarModalInstruccionesImportacion()` - cierra el modal de instrucciones y limpia el prompt pendiente.
* `construirColumnasHoras(tiposHoras)` - arma la lista de columnas de horas para el encabezado del CSV.
* `construirEncabezadoCSV(tiposHoras)` - arma la línea completa de encabezado del CSV (Bloque, Código, Nombre, Créditos, columnas de horas, Requisitos, Correquisitos, SinDefinir).
* `construirInputArchivoCSV(textareaDestino)` - construye el input de archivo para cargar un CSV directamente hacia un textarea destino.
* `construirPanelImportacion()` - construye el panel principal de importación (elegir modo, generar prompt, pegar CSV).
* `construirPromptImportacion(modo, link)` - arma el texto del prompt de importación universal a pegarle a la IA, según el modo (link o capturas).
* `construirTextoInstruccionesImportacion()` - arma el texto paso a paso mostrado en el modal de instrucciones antes de ir a Claude.
* `convertirCapturasAPDF(archivos)` - convierte una lista de imágenes capturadas en un único PDF usando jsPDF.
* `enviarPromptAClaude(texto)` - abre claude.ai/new en pestaña nueva y copia el prompt al portapapeles.
* `extraerMetadatosImportacion(textoCrudo)` - extrae los metadatos (carrera, universidad, código, etc.) de las líneas iniciales de un CSV crudo.
* `inicializarModalCapturasPDF()` - registra los listeners del modal de capturas de pantalla → PDF.
* `inicializarModalInstruccionesImportacion()` - registra los listeners del modal de instrucciones previas a la importación.
* `instruccionesImportacionPendiente` (variable exportada, no constante) - guarda el `{textoPrompt}` pendiente mientras el modal de instrucciones está abierto; se limpia al cerrarlo.

### plan/plan-mapa.js
Propósito: la Vista de Mapa interactivo completa - nodos por materia, colores por estado/categoría, zoom (pellizco / `Ctrl`+rueda / botones), camino de desbloqueo con efecto neón, y exportar el mapa como PNG vía `html2canvas`.
Depende de: storage.js, utils.js, plan-detalle.js, plan-vista-lista-tarjetas.js, plan-vista-lista.js
Exporta:
* `COLOR_ESTADO_MAPA` - mapa de color hexadecimal por estado de materia (pendiente/cursando/aprobado/reprobado/retirado) usado en la Vista de Mapa.
* `abrirSelectorDescargaMapa()` - abre el selector de descarga del mapa como imagen (switches de modo claro/oscuro, tema, con/sin fondo).
* `ajustarZoomMapa(delta, etiquetaEl)` - ajusta el nivel de zoom del mapa (clamp 0.5–2), actualiza el porcentaje mostrado en `etiquetaEl`.
* `aplicarZoomMapa()` - aplica el `estado.zoomMapa` actual como transform sobre el track del mapa.
* `colorNodoMapa(materia, plan)` - devuelve el color de un nodo del mapa, según se coloree por categoría o por estado (`COLOR_ESTADO_MAPA`).
* `construirMapaInteractivo(plan)` - construye el SVG del mapa interactivo con únicamente los bloques numerados reales del plan (excluye Optativas y Revisar).
* `construirNodoMapa(materia, plan)` - construye el nodo visual individual de una materia dentro del mapa.
* `construirTarjetaVista(plan)` - construye la tarjeta contenedora de la Vista de Mapa (controles + SVG), guardando referencias vivas del bloque de controles.
* `dibujarCaminoDesbloqueo(plan)` - dibuja/resalta en el SVG el camino de materias que desbloquea la materia seleccionada.
* `exportarMapaComoPNG(opciones)` - exporta el mapa visible como imagen PNG; `opciones` controla modo claro/oscuro, tema y fondo solo durante la captura.
* `recolorearNodosMapa(plan)` - recorre los nodos ya dibujados y actualiza su color según el estado/categoría actual de cada materia, sin re-renderizar todo.
* **Deuda conocida:** hoy el mapa muestra solo el plan **principal** (no combina Modo Hardcore, que soporta hasta 3 planes); y la Simbología usa 4 estados reales (no existe un 5º estado "Retirada" en `ESTADOS_MATERIA`).

### plan/plan-vista-lista-tarjetas.js
Propósito: construcción de las tarjetas de materia dentro de la lista (bloques numerados y optativas), su disponibilidad según requisitos, y resolución de conflictos de sincronización a nivel de materia/entidad.
Depende de: schema.js, storage-merge.js, storage-sync.js, storage.js, utils.js, componentes.js, plan-detalle.js, plan-esquema.js, plan-vista-lista.js
Exporta:
* `ESTADOS_MATERIA` - lista de los 4 estados posibles de una materia (pendiente/cursando/aprobado/reprobado) con su texto y clase de badge. Reutilizado por `semestres/semestres-tarjetas.js` como misma fuente de verdad.
* `abrirMenuRapidoCategoria(materia, plan, anclaEl)` - abre un popover flotante anclado a `anclaEl` para asignar rápido una categoría a la materia.
* `abrirModalResolverConflicto(materia, plan, onResueltoExtra)` - caso particular de `abrirModalResolverConflictoGenerico` para una materia del plan.
* `abrirModalResolverConflictoGenerico({ entidad, plan, titulo, explicacion, onResuelto, obtenerFresca })` - abre el overlay genérico de resolución de conflicto de sync, mostrando los campos en choque entre la versión local y `_version_alterna`.
* `resolverConflictoDirecto({ obtenerFresca, cual })` - aplica directamente la resolución ("local" o "alterna") de un conflicto sobre la entidad viva, sin pasar por el modal; usado por "resolver todos a la vez".
* `agregarIndicadorConflicto(cardEl, onResolver)` - agrega el indicador visual compacto de "choque de versiones" a una tarjeta.
* `construirBloqueOptativas(filasAgregadas, filasDisponibles, esEscritorio, mostrarOrigen)` - construye el bloque especial de Optativas (agregadas + disponibles para vincular).
* `construirContenidoBloques()` - construye el contenido completo de bloques numerados + optativas + revisar de la lista de materias.
* `construirTarjetaMateria(fila, esEscritorio, mostrarOrigen)` - construye la tarjeta de una materia individual, resolviendo disponibilidad y estado de expansión.
* `construirTarjetaOptativaDisponible(materiaTemplate, plan)` - construye la tarjeta de una optativa disponible para vincular (aún no agregada al plan).
* `estaExpandida(codigo, esEscritorio)` - devuelve si una tarjeta está expandida; si no hay estado guardado, usa `esEscritorio` como default.
* `materiaDisponible(materia, materiasDelPlan)` - evalúa si una materia está disponible (sus requisitos se cumplen) contra el árbol Y/O y el estado "aprobado" de las materias del plan.
* `obtenerMateriasQueDesbloquea(materia, plan)` - devuelve las materias del plan cuyo árbol de requisitos o correquisitos contiene el código de `materia`.

### plan/plan-vista-lista.js
Propósito: orquestador de la Vista de Lista del plan de estudios - encabezado, panel de estadísticas, barra de acciones, expandir/contraer, carrusel entre planes, y el render principal `renderizarPlanEstudios`.
Depende de: schema.js, storage-sync.js, storage.js, utils.js, plan-categorias.js, plan-esquema.js, plan-gestionar.js, plan-modo-edicion.js, plan-importacion-csv.js, plan-importacion.js, plan-mapa.js, plan-vista-lista-tarjetas.js
Exporta:
* `construirAnilloDonut(porcentaje, colorProgreso)` - construye el SVG del anillo tipo donut de progreso (créditos aprobados) del panel de estadísticas.
* `construirBarraAcciones()` - construye la barra de acciones (orden, editar materias, añadir materia, etc.) sobre la lista de materias.
* `construirEncabezadoPlan(planPrincipal)` - construye el encabezado del plan (título, carrera, botones de editar/gestionar).
* `construirPanelEstadisticas(plan)` - construye el panel de estadísticas (materias y créditos totales/aprobados) del plan.
* `contraerTodasLasMaterias()` - marca todas las materias visibles como contraídas y re-renderiza.
* `contraerTodosLosBloques()` - colapsa todos los bloques/grupos actuales y re-renderiza.
* `expandirTodasLasMaterias()` - marca todas las materias visibles como expandidas y re-renderiza.
* `expandirTodosLosBloques()` - vacía el set de bloques colapsados (expande todos) y re-renderiza.
* `exportarPlanACSV(planParam)` - exporta el plan completo a CSV (metadatos + todas las materias, incluidos cupos de electiva sin llenar), en el mismo formato que espera el importador.
* `inicializarResponsivoListaPlan()` - registra el listener de resize que detecta el cruce del breakpoint móvil/escritorio (900px) para re-renderizar la lista cuando cambia.
* `navegarPlanCarrusel(delta)` - cambia el plan activo al siguiente/anterior (`delta` ±1) dentro del carrusel de planes.
* `obtenerClavesAgrupacionActuales()` - devuelve el set de claves de agrupación actuales (por categoría o por bloque, según el orden activo) de las materias visibles.
* `renderizarPlanEstudios()` - punto de entrada principal: vacía y reconstruye `#seccion-plan-estudios` completo (encabezado, estadísticas, barra de acciones, contenido de bloques).

### plan/plan-modo-edicion.js
Propósito: botón "Editar plan", badge fijo "Modo edición" y el ícono de lápiz que aparece en cada tarjeta de materia mientras el modo está activo.
Depende de: storage.js, plan-vista-lista.js
Exporta:
* `alternarModoEdicionPlan()` - invierte `estado.modoEdicionPlan` y refresca el badge y las tarjetas del plan.
* `renderizarBadgeModoEdicion()` - muestra u oculta el badge fijo de la esquina inferior derecha según `estado.modoEdicionPlan`.

---

## JS - semestres

### semestres/semestres.js
Propósito: alta/edición/listado de semestres - formulario completo (nombre, fecha, duración, Modo Hardcore, buscador + filtro por estado, checklist de materias), sincronía Matrícula↔Plan, modo edición (editar/borrar semestres), y el render del listado (dashboard + actuales + pasados) con mecanismo anti-scroll-fantasma.
Depende de: core/schema.js, core/storage-sync.js, core/storage.js, core/utils.js, ui/componentes.js, plan/plan-gestionar.js, semestres/semestres-tarjetas.js, semestres/semestres-dashboard.js, main.js (import circular intencional, mismo patrón que ui/componentes.js).
Exporta:
* `abrirModalAltaSemestre(semestreExistente = null)` - abre el modal de alta (o edición, si se pasa un semestre) de un semestre.
* `buscarSemestreVivoPorId(semestreId)` - relee el semestre vigente desde `estado.datos` por id, para evitar referencias huérfanas si un sondeo remoto reemplazó el estado mientras el modal estaba abierto.
* `navegarASemestre(semestreId)` - cambia a la sección Semestres, expande la tarjeta de ese semestre, re-renderiza y hace scroll suave + destello hasta ella.
* `obtenerSemestreAdyacente(semestreId, direccion)` - devuelve el semestre siguiente (`direccion=1`) o anterior (`direccion=-1`) en orden cronológico; envuelve en los extremos. Usado por la navegación con flechas de Horario.
* `obtenerSemestresActuales()` - semestres con estado efectivo `"actual"`, ordenados desc por fecha_inicio.
* `obtenerSemestresOrdenCronologico()` - todos los semestres ordenados asc por fecha_inicio (orden real, sin separar actuales/pasados).
* `obtenerSemestresPasados()` - semestres con estado efectivo `"pasado"`, ordenados desc por fecha_inicio.
* `renderizarSemestres(omitirRestauracionScroll = false)` - punto de entrada de la sección; repinta dashboard académico + listado completo dentro de `#seccion-semestres`.
* `vincularProfesorAMateriaMatriculada(semestreId, mmId, profesorId)` - agrega un profesor a `mm.profesor_ids` de una materia matriculada (usado desde el modal de bloque de Horario).

### semestres/semestres-dashboard.js
Propósito: pestaña contraíble "Historial académico" al inicio de la sección Semestres - estadísticas de aprobación (barra dividida + detalle por estado) y promedio ponderado (por semestre+universidad y por plan/carrera); colapsada por default para no empujar el resto del contenido.
Depende de: core/storage.js, core/utils.js, core/schema.js
Exporta:
* `construirDashboardAcademico(onCambiar)` - arma la tarjeta completa del dashboard (encabezado + selector de vista + vista activa); si está colapsado devuelve solo el encabezado.

### semestres/semestres-tarjetas.js
Propósito: tarjetas de semestre y de materia matriculada - motor de notas completo (criterios, asignaciones, cálculo de nota final en vivo, proyecciones/objetivos, resolución de conflictos de sync) y drag-and-drop de reordenamiento. Es el archivo más grande del proyecto (~3540 líneas); casi todo su contenido son funciones privadas del modal de notas, solo 3 quedan expuestas afuera.
Depende de: core/storage.js, core/utils.js, ui/componentes.js, core/schema.js, core/storage-sync.js, plan/plan-vista-lista-tarjetas.js, plan/plan-detalle.js, plan/plan-vista-lista.js
Exporta:
* `construirTarjetaSemestre(semestre, obtenerPlanPorId, onCambiar, onEditar, onBorrar, anidada = false)` - construye la tarjeta completa de un semestre (encabezado + sus materias matriculadas), expandible/colapsable.
* `abrirModalTodosLosConflictos()` - abre el modal que lista todos los conflictos de sync pendientes de resolver en Semestres (matrícula, criterio, semestre), con botón para resolver cada uno.
* `registrarAbrirTarjetaProfesorFlotante(fn)` - inyecta (desde `comunidad.js`, al inicializar) la función que abre la tarjeta flotante de un profesor con botón "Ir a Comunidad"; evita un ciclo de imports semestres-tarjetas.js↔comunidad.js.
* **Candidato a dividir:** casi 4.5× el límite de 800 líneas del proyecto - considerar partirlo (ej. un archivo aparte para el motor de notas) antes de seguir agregándole funciones.

---

## JS - finanzas

### finanzas/finanzas.js
Propósito: shell de la sección Finanzas - arma las 4 pestañas (Resumen / Semestres / Gastos generales U / Beneficios) y calcula los totales del Resumen; el CRUD de cada pestaña vive en los otros archivos.
Depende de: core/schema.js, core/storage.js, finanzas-graficas.js, finanzas-gastos.js, finanzas-semestres.js
Exporta:
* `calcularTotalesResumenFinanzas()` - suma `calcularTotalPagosMatricula()` de cada registro (todos los semestres) + costo de cada gasto_u de tipo `"gasto"` (recurrentes: solo lo ya pagado a la fecha) vs. `calcularTotalIngresosBeca()` de cada registro + gasto_u de tipo `"ingreso"` (mismo criterio "a la fecha"). **v2.9.2 (2026-08-26):** devuelve `{ totalGastado, totalBecas, totalIngresos, balanceNeto }` (se agrega `totalIngresos`; `balanceNeto = totalBecas + totalIngresos − totalGastado`). **FIX - Becas y Pagos de Matrícula, Parte A (2026-09-12):** todavía sumaba `r.costo_matricula`/`r.beca_monto` directo - campos que la migración de Parte A ya borra del modelo - así que el Resumen habría mostrado ₡0 en ambos lados para todo el mundo tras migrar. Ahora usa `calcularTotalPagosMatricula`/`calcularTotalIngresosBeca` (core/schema.js).
* `formatearFechaLarga(fechaIso)` - convierte `"YYYY-MM-DD"` a `"11 de agosto de 2026"`; vacío/null da `""`.
* `formatearMonto(numero)` - formatea el monto con 2 decimales; para negativos antepone un `"-"` explícito antes del símbolo. **FIX 2026-08-26 (bug reportado por Krys):** el símbolo estaba hardcodeado a `"₡"` y nunca leía la moneda elegida en Ajustes - ahora sale de `obtenerSimboloMonedaActual()`.
* `obtenerSimboloMonedaActual()` - **nuevo (2026-08-26)**, no exportaba antes: resuelve el símbolo de `configuracion.moneda_preferida` contra `MONEDAS_DISPONIBLES` (core/schema.js), con `"₡"` como fallback defensivo. Único punto de verdad del símbolo actual - lo usa también `formatearMontoCompacto` en finanzas-graficas.js.
* `renderizarContenidoFinanzasActivo()` - repinta solo el contenido de la pestaña activa dentro de `#finanzas-contenido`, sin reconstruir el tab bar.
* `renderizarFinanzas()` - punto de entrada de la sección; reconstruye tabs + contenido dentro de `#seccion-finanzas`.

### finanzas/finanzas-graficas.js
Propósito: las 2 gráficas SVG (a mano, sin librería) de la pestaña Resumen - donut "Gastado vs. Disponible" + composición de ingresos (beca vs. ingresos propios) y línea "Por semestre" (Gastos/Beca/Ingresos). **No estaba documentado en una versión anterior de este mapa** pese a existir como archivo propio - se agrega ahora junto con el cambio de ingresos.
Depende de: core/schema.js, core/storage.js, finanzas/finanzas-gastos.js, finanzas/finanzas.js
Exporta:
* `construirGraficasResumenFinanzas(totalBecas, totalIngresos, totalGastado)` - punto de entrada, llamado desde `construirResumenFinanzas` (finanzas.js). **v2.9.2 (2026-08-26):** firma cambia de `(totalBecas, totalGastado)` a `(totalBecas, totalIngresos, totalGastado)` - el donut de 2 segmentos dejó de ser "Beca vs. gastado" para pasar a ser **"Gastado vs. Disponible"** sobre el total de entradas (beca+ingresos) - mezclar entradas y salidas de plata en un solo donut de 3 segmentos no daba una proporción con sentido accionable (decisión tomada junto con el usuario antes de implementar). La composición de esas entradas (cuánto es beca vs. cuánto es ingreso propio) se muestra aparte, como chips de texto, no como otro donut. La línea "Por semestre" gana una 3ra serie ("Ingresos", azul) además de "Gastos" (rojo) y "Beca" (verde, antes llamada "Ingresos" a secas).
* `calcularSerieFinancieraPorSemestre()` (interna, no exportada) - alimenta la línea "Por semestre". **FIX - Becas y Pagos de Matrícula, Parte A (2026-09-12):** mismo bug que `finanzas.js#calcularTotalesResumenFinanzas` - leía `registro.beca_monto`/`registro.costo_matricula` directo, campos retirados por la migración de Parte A. Ahora usa `calcularTotalIngresosBeca`/`calcularTotalPagosMatricula` (core/schema.js). El donut no tenía este bug: recibe los totales ya calculados desde `finanzas.js`, no lee el registro directo.

### finanzas/finanzas-gastos.js
Propósito: CRUD de gastos y, desde v2.9.2, **ingresos** sueltos no vinculados a un semestre (pestaña "Gastos", simples o recurrentes, con vínculo opcional a semestre) + generador de prompt de descuentos estudiantiles (pestaña "Beneficios").
Depende de: core/schema.js, core/storage-sync.js, core/storage.js, core/clipboard.js, ui/componentes.js, plan/plan-esquema.js, semestres/semestres.js, finanzas/finanzas.js
Exporta:
* `renderizarPestanaBeneficios(contenedor)` - pinta la pestaña Beneficios: botón(es) para copiar el prompt de descuentos estudiantiles según la(s) universidad(es) del/los plan(es) activo(s), y abrir claude.ai.
* `renderizarPestanaGastosU(contenedor)` - pinta la lista de gastos/ingresos generales U (simples/recurrentes, con badge de monto o de total pagado a la fecha) + botón para abrir el modal de alta/edición. **v2.9.2:** el modal ahora incluye un selector de Tipo (Gasto/Ingreso, pill-group, default "Gasto"); el badge de un registro `tipo:"ingreso"` se pinta de azul (`#3b82f6`, inline - no existe una clase `.badge-*` azul en el proyecto) con un `"+"` adelante, en vez del gris neutro de un gasto normal, para diferenciarlos a simple vista (pedido explícito de Krys).

### finanzas/finanzas-semestres.js
Propósito: pestaña "Semestres" de Finanzas - lista todos los semestres del historial (actuales + pasados). Cada fila se expande in-place con una flechita para ver/editar el detalle financiero de ese semestre: pagos de matrícula e ingresos de beca, cada uno como su propia tarjeta.

**Becas y Pagos de Matrícula - Parte B (2026-09-12):** reescritura completa del flujo de edición. Se retira el modal único "Editar/Crear registro" (un solo input de costo + un solo input de beca) que existía desde Parte A como parche transicional sobre el modelo nuevo de listas. Ahora:
* La fila de cada semestre gana una flechita ▾/▴ (reemplaza al botón "Editar"/"Crear registro") que expande/colapsa el detalle **ahí mismo**, sin abrir modal - estado de qué semestres están expandidos vive en memoria (`semestresExpandidosFinanzas`, un `Set` a nivel de módulo), no se persiste ni sincroniza.
* El detalle expandido (B.1/B.3) muestra dos columnas - "Pagos de matrícula" / "Ingresos de beca" - en un grid `repeat(auto-fit, minmax(240px,1fr))`: dos columnas lado a lado si el ancho alcanza, una sola apilada si no (pedido explícito: nunca truncar el texto de la descripción, por eso `auto-fit`+`minmax` en vez de un breakpoint fijo con `text-overflow:ellipsis`). Cada columna tiene un total en texto simple arriba ("Total matrícula: ₡X"/"Total beca: ₡X"), la lista de tarjetas, y un botón "+ Agregar".
* Cada tarjeta de pago/beca es una fila compacta (glass-card): descripción (o "Sin descripción" en muted) + fecha chica debajo si tiene, badge de monto (`badge-danger` matrícula / `badge-success` beca) a la derecha, botones ✎ (editar) y ✕ (borrar) discretos, **siempre visibles** - no hay un "modo edición" que los oculte en esta entrega; eso es Parte D, que agrega la capa de confirmación de guardar/descartar por encima de esto, no reemplaza esta edición inline.
* El registro financiero del semestre (`finanzas_semestre`) ya no se crea con un botón aparte ("Crear registro"): se crea vacío, en silencio, la primera vez que se guarda un pago o un ingreso desde el modal chico (`obtenerOCrearRegistroDeSemestre`).
* **B.2 - dividir en partes:** al agregar un pago de matrícula NUEVO (nunca al editar uno existente, y nunca para beca - la beca no tiene equivalente de "cuotas") el modal ofrece, vía `construirPillSwitchBinario` (ui/componentes.js), elegir "Un solo pago" o "Dividir en partes". En modo dividir: monto total + número de partes → genera esa cantidad de `pagos_matricula` de una vez ("Pago 1 de N"…), repartidos lo más parejo posible en centavos (`repartirMontoEnPartes`, mismo algoritmo centavo-seguro que la vieja `repartirMontoEnMeses` de Parte A, ahora sin la noción de "mes" - genera solo montos). Con el checkbox "Personalizar el monto de cada parte" se pueden ajustar los montos individuales antes de crear (siguen sumando lo que el usuario deje, no se fuerza a calzar con el monto total original una vez personalizado). Cada pago generado queda totalmente independiente y editable después, igual que uno cargado a mano.
* Se conserva (como link discreto al pie del panel expandido, solo si ya hay algo cargado) la opción de borrar el registro financiero **completo** del semestre - existía en el modal de Parte A y no se quiso perder esa funcionalidad al rediseñar.
* **Pendiente explícito para Parte C:** los botones rápidos "Pago de matrícula"/"Ingreso de beca" (fuera de un semestre puntual) todavía no existen - reutilizarán `abrirModalItemFinanzas` de este archivo.
* **Pendiente explícito para Parte D:** el modo edición con "no salir sin guardar" + confirmación no está implementado - ver nota en Parte D más abajo sobre por qué `plan-modo-edicion.js` NO se puede reusar tal cual (solo tiene el toggle+badge visual, no un guard de navegación ni detección de cambios pendientes).

Depende de: core/schema.js, core/storage-sync.js, core/storage.js, ui/componentes.js, semestres/semestres.js, finanzas/finanzas.js
Exporta:
* `renderizarPestanaSemestresFinanzas(contenedor)` - pinta la lista de semestres; cada fila expandible in-place con el detalle de pagos/becas descrito arriba.

Funciones internas (no exportadas) relevantes para retomar el trabajo:
* `obtenerOCrearRegistroDeSemestre(semestreId)` - devuelve el registro financiero del semestre, creándolo vacío primero si no existe.
* `abrirModalItemFinanzas({ tipo, semestre, registro, itemExistente, contenedorLista })` - modal chico de alta/edición de UN pago o UN ingreso (con el flujo de dividir en partes cuando aplica). Punto de entrada a reusar para los botones rápidos de Parte C.
* `eliminarItemFinanzas({ tipo, registro, itemId })` - saca el ítem de `pagos_matricula`/`ingresos_beca` y deja tumba en `_eliminados_pagos_matricula`/`_eliminados_ingresos_beca` (formato `{id, eliminadoEn}`, mismo que espera `fusionarFinanzasSemestre` en storage-merge.js).
* `repartirMontoEnPartes(total, cantidadPartes)` - helper centavo-seguro de B.2 (ver arriba).

---

## JS - comunidad

### comunidad/comunidad.js
Propósito: sección Comunidad completa (`#seccion-comunidad`) - profesores (alta/edición, calificación general 1-10, "¿volverías a llevarlo?", vinculación a materias por semestre) y compañeros (alta/edición, materias compartidas, importar contacto vía Contacts Picker API).
Depende de: core/schema.js, core/storage-sync.js, core/storage.js, core/utils.js, ui/componentes.js, core/clipboard.js, semestres/semestres.js, plan/plan-detalle.js, semestres/semestres-tarjetas.js
Exporta:
* `inicializarComunidad()` - se llama una vez al arranque (antes de un posible `mostrarApp()` por caché); registra en plan-detalle.js y semestres-tarjetas.js los callbacks reales de "abrir alta de profesor" y "abrir tarjeta flotante de profesor" para evitar ciclos de import. No crea nodos nuevos (el contenedor ya viene en index.html).
* `renderizarComunidad()` - reconstruye `#seccion-comunidad` completo (pills Profesores/Compañeros + la lista correspondiente); requiere `estado.datos` ya cargado.
* `abrirModalAltaProfesor(profesorExistente?, preseleccionMmId?, onGuardado?)` - abre el modal de alta/edición de profesor. `preseleccionMmId` precarga una materia matriculada a vincular; `onGuardado` se llama justo después de guardar con éxito.
* **Candidato a dividir:** 3948 líneas, muy por encima del límite de 800 líneas por archivo. Considerar partirlo en `comunidad-profesores.js` / `comunidad-companeros.js` antes de seguir agregándole cosas.

---

## JS - horario

### horario/horario.js
Propósito: renderiza el grid semanal de Horario (vista propia, modo conjunto con amigos y vista individual de un amigo), y expone helpers de formato (color/nombre de bloque, modalidad, profesor) reutilizados por Agenda.
Depende de: schema.js, storage.js, storage-sync.js, componentes.js, config-ajustes.js, semestres.js, plan-esquema.js, horario-modal.js, horario-amigos.js
Exporta:
* `inicializarHorario()` - conecta los botones de la sección Horario (semestre anterior/siguiente, agregar bloque, panel de amigos, pantalla completa, descargar imagen).
* `renderizarHorario()` - repinta el grid completo de Horario (delega en el render interno).
* `obtenerSemestreHorarioActual()` - devuelve el semestre que se está mostrando ahora mismo en Horario (usa `estado.horarioSemestreId` o cae al más reciente no pasado).
* `obtenerColorBloque(bloqueEfectivo)` - color a mostrar para un bloque: color propio del bloque, o si no tiene, el de la categoría de la materia asociada, o violeta por default.
* `obtenerNombreBloque(bloqueEfectivo)` - nombre a mostrar: apodo del bloque, o nombre de la materia vinculada, o `"Personalizado"`/`"Materia"` de fallback.
* `obtenerRangoHorasHorario()` - devuelve `{ horaInicio, horaFin }` (0-24) del rango visible del grid según Ajustes → Horario, con validación de rango inválido.
* `obtenerPlanPorId(planId)` - busca un plan de estudios por id en `estado.datos.planes_estudio`.
* `abrirHorarioConjunto()` *(alias de `activarModoConjunto`)* - activa el modo "Horario conjunto" (superpone los horarios de amigos vinculados) dentro del mismo `#horario-grid`.
* `abrirVistaIndividualAmigo(fileId, { pantallaCompleta = true })` *(alias de `activarVistaIndividualAmigo`; segundo parámetro nuevo 2026-09)* - reemplaza el grid propio por la vista de solo lectura del horario de un amigo vinculado puntual. Con `pantallaCompleta: false` la abre "normal" dentro de la página (el header muestra "← Cerrar horario"); es lo que usa el click sobre la tarjeta completa de un amigo. El botón ⛶ de la tarjeta sigue pasando `true`. Fix 2026-09 en el botón de salir de pantalla completa (`anclarBotonSalirFSEnFila`, interna): se perdía porque cada render vacía `#horario-grid` y dejaba el botón desconectado del documento (ahora se guarda en referencias de módulo y se reancla en cada render), y quedaba fuera de pantalla con scroll horizontal (ahora `position:sticky; right` dentro de un ancla que ocupa la fila).
* `abrirTarjetaInfoBloque(semestre, numeroSemana, b)` - abre el overlay con el detalle de una clase efectiva (nombre, semana, modalidad, aula, profesor). Reutilizado por `agenda/agenda-clases.js` para su tarjeta "Mostrar clases".
* `obtenerEmojiModalidad(modalidad)` - emoji para una modalidad (`💻` virtual, `📖` asincrónica, `✖️` sin clase, vacío si presencial).
* `obtenerEtiquetaModalidad(modalidad)` - etiqueta humana para una modalidad, con fallback genérico si el valor no está mapeado.
* `obtenerNombreProfesor(profesorId)` - nombre abreviado del profesor (primer nombre + primer apellido completos, resto a iniciales).
* `fechaLocalDesdeISO(str)` - parsea una fecha `YYYY-MM-DD` a `Date` local (sin desfase de timezone).
* `calcularNumeroSemanaSinAcotarParaFecha(semestre, fecha)` *(re-exportada de `horario-modal.js`, 2026-08-29)* - número de semana del semestre para una fecha puntual, SIN acotar (puede dar <1 o mayor que `duracion_semanas`, a propósito). Reutilizada por `agenda/agenda-clases.js` para su propia `calcularNumeroSemanaParaFecha` (esa sí acotada) y por `asistente/asistente.js` para validar cambios de modalidad por voz/texto. Distinta a propósito de `core/schema.js#calcularNumeroSemanaSemestre` (esa resuelve la semana de HOY únicamente).
* **Candidato a dividir:** 2064 líneas, por encima del límite de 800.

### horario/horario-modal.js
Propósito: modal de alta/edición de un bloque de horario (materia, días/horas, modalidad, color, profesor, notas) y su sub-sección de Cronograma (excepciones puntuales por semana/día).
Depende de: schema.js, storage-sync.js, storage.js, componentes.js, config-ajustes.js, semestres.js, comunidad.js
Exporta:
* `abrirModalBloqueHorario({ semestreId, bloqueId, diaPreseleccionado, horaInicioPreseleccionada, horaFinPreseleccionada, numeroSemanaVista })` - abre el modal para crear un bloque nuevo (con día/hora preseleccionados, ej. desde un click-drag en el grid) o editar uno existente por `bloqueId`.
* `cerrarModalBloqueHorario()` - oculta el modal y limpia el contexto de edición en curso.
* `construirZonaCronograma(semestre, bloque, { semanaInicial })` - arma el bloque colapsable "📅 Cronograma de clases" dentro del formulario, para editar la modalidad de un día puntual de una semana específica sin afectar la plantilla base del bloque.
* `aplicarModalidadDia(bloqueId, semestreId, numeroSemana, diaCodigo, nuevaModalidad)` *(exportada 2026-08-29, antes solo interna)* - aplica o revierte el ajuste puntual de modalidad de un día/semana concreto de un bloque (con tumba real si vuelve a coincidir con la plantilla). La sigue usando `construirZonaCronograma` internamente; ahora también la reutiliza `asistente/asistente.js` para aplicar cambios de modalidad pedidos por voz/texto - mismo camino real, sin lógica duplicada.
* `calcularNumeroSemanaSinAcotarParaFecha(semestre, fechaObjetivo)` *(nueva, 2026-08-29)* - inverso de la fecha-por-semana interna del archivo: dada una fecha real, devuelve su número de semana del semestre, anclado al día real de `fecha_inicio` (no asume que caiga lunes) y SIN acotar entre 1 y `duracion_semanas` a propósito, para poder distinguir "cae fuera del semestre" de "cae en la semana 1/última". Re-exportada vía `horario.js` para `agenda/agenda-clases.js` (que sí necesita la versión acotada, y ahora delega el cálculo crudo acá) y usada directo por `asistente/asistente.js` para resolver cambios de modalidad. **No confundir con `agenda/agenda-clases.js#calcularNumeroSemanaParaFecha`** - incluso siendo casi el mismo nombre, esa acota el resultado y esta no; nunca son intercambiables.
* **Candidato a dividir:** 1038 líneas, por encima del límite de 800.

### horario/horario-amigos.js
Propósito: "Horario entre amigos" del lado de la app con sesión - generar/revocar enlaces compartidos del propio horario, vincular horarios de amigos vía enlace público, y mantener en caché sus snapshots para el modo conjunto y la vista individual.
Depende de: storage.js, storage-sync.js, schema.js, auth.js, componentes.js, clipboard.js, horario.js (helpers de formato/color y las funciones de modo conjunto/vista individual)
Exporta:
* `inicializarHorarioAmigos()` - inicializa el panel de Amigos y pinta la lista de enlaces compartidos propios (corre en el primer `DOMContentLoaded`, antes de tener `estado.token`).
* `abrirPanelAmigos()` - muestra el modal del panel de Amigos y repinta la lista de vinculados.
* `renderizarListaEnlacesCompartidos()` - pinta la lista de enlaces que el usuario generó de su propio horario, ordenados por fecha de creación descendente.
* `procesarAsociacionPendienteDeAmigo()` - lee el pendiente dejado en `localStorage` por horario-amigos-publico.js, lo descarta si expiró o ya estaba vinculado, y si no, dispara la confirmación de vínculo.
* `asignarColorAmigo(semilla)` - color determinístico (hash de la semilla, ej. el file_id) tomado de una paleta fija, para pintar de forma estable el horario de cada amigo.
* `iniciarRefrescoPeriodicoAmigos()` - refresca los snapshots de amigos al llamar y arma un intervalo de 5 min mientras la pestaña siga abierta.
* `obtenerBloquesAmigosPorDia(fecha, diaCodigo)` - bloques de todos los amigos visibles (no ocultos) que caen en un día/fecha real, resolviendo excepciones de cronograma de esa semana.
* `renderizarListaAmigosVinculados()` - pinta la lista de amigos vinculados en el panel; muestra/oculta el botón "Horario conjunto" según si hay al menos uno. Desde 2026-09, un click en CUALQUIER parte de la tarjeta de un amigo (salvo sus botones ⛶, ✎, el switch y Desvincular) abre su horario individual SIN pantalla completa; el botón ⛶ sigue abriéndolo en pantalla completa.
* `obtenerListaAmigosParaDiaConjunto(fecha, diaCodigo)` - para cada amigo vinculado, sus bloques de ese día/fecha ya resueltos (o marca "caída" si su enlace fue revocado). Usado por el modo conjunto en horario.js. Desde 2026-09 cada bloque trae `universidad` como texto (siglas, con fallback a nombre completo) y `universidadNombreCompleto` como aclaración (tooltip en la tarjeta), resueltos con `resolverUniversidadParaMostrar` (interna) que acepta el objeto `{ nombre_completo, siglas }`, un string viejo o vacío, para no volver a mostrar "[object Object]".
* `refrescarSnapshotsAmigos()` - vuelve a descargar el snapshot público de cada amigo vinculado, actualiza el caché y repinta Horario y la lista de amigos.
* `obtenerSnapshotAmigoPorId(fileId)` - snapshot crudo (+ estado "caída") de un amigo puntual, tal cual está en caché ahora. Usado por la vista individual en horario.js.
* `obtenerDiasConClaseAmigosVinculados()` - unión de todos los códigos de día en los que cualquier amigo vinculado tiene al menos un bloque, sin filtrar por visibilidad. Usado para armar los días navegables del Horario conjunto.
* `calcularNumeroSemanaAmigo(snapshot, fecha)` - número de semana del semestre del amigo para una fecha real dada (`null` si cae fuera de su rango de semestre).

### horario/horario-amigos-publico.js
Propósito: script standalone de `amigos.html` (vista pública del horario compartido, sin sesión). A propósito NO importa nada de `js/core` ni de `js/horario` - porta en modo solo-lectura su propia copia de la matemática del grid (horas, días, clases efectivas por semana) para no cargar el stack de auth/sync/schema. No es un módulo ES: se auto-ejecuta llamando a `iniciar()` al final del archivo.
Depende de: nada del resto de la app (fetch directo a Drive API vía `API_KEY` pública restringida por dominio).
Nota: no exporta nada - todo el archivo es el punto de entrada de la página. Funciones internas relevantes (no exportadas): `iniciar()`, `obtenerFileIdDesdeHash()`, `obtenerSnapshotPublico(fileId)`, `renderizarGridPublico(snapshot)`, `inicializarFlujoAsociar(fileId, snapshot)`, `inicializarPantallaCompletaPublico()`. Desde 2026-09: la universidad de cada tarjeta se muestra como siglas con el nombre completo como tooltip (`obtenerTextoUniversidad` / `obtenerNombreCompletoUniversidad`, escapadas), y el botón de salir de pantalla completa vive en la fila de título del header sticky (`#amigos-fila-titulo-fs`, visible solo en pantalla completa) con `position:sticky`, para que no se pierda al scrollear.

---

## JS - agenda

### agenda/agenda.js
Propósito: núcleo de la Agenda - vista Lista (cronológica, agrupada por día), header, filtros (Semanal/Todo, mostrar materias, mostrar días vacíos), selector de semestres, y el despacho entre Lista y Calendario.
Depende de: core/notificaciones-push.js **(2026-08-25: pendiente de cambiar a core/notificaciones-calendario.js - no actualizado en esta sesión, archivo no subido)**, core/schema.js, core/storage-sync.js, core/storage.js, core/utils.js, ui/componentes.js, semestres/semestres.js, agenda/agenda-calendario.js, agenda/agenda-clases.js, agenda/agenda-modal.js, agenda/agenda-utils.js, tiempo-estudio/tiempo-estudio.js (`obtenerEstudioParaHoy`, `irADetalleMateriaTiempoEstudio`, `formatearHorasMin` - **2026-09-22, nuevo**, ver abajo)
Exporta:
* `inicializarAgenda()` - wiring inicial: modal de evento, botón "+", pills de vista, selector de semestres, filtros.
* `abrirCronogramaAgenda(semestreId?, materiaMatriculadaId?)` - selecciona Agenda > Cronograma, opcionalmente limita el semestre y enfoca una matrícula concreta; se invoca desde “Ir a Cronograma” en la tarjeta de información de Horario.
* `renderizarAgenda()` - entrypoint de render: decide Lista vs Calendario y dispara el render correspondiente. También expuesta como `window.renderizarAgenda` (evita import circular con `agenda-modal.js` y `agenda-calendario.js`).
* `construirItemEvento(evento)` *(2026-09-21)* - el círculo tiene 3 estados visuales (pendiente / `.marcada` azul / `.perdida` rojo oscuro con ✕) y delega el toque en `manejarToqueCheckTarea` (agenda-modal.js); el nombre se tacha con raya roja y sin atenuar si es perdida. Ver también el filtro por estado: 7 badges (`Clase`, `Completado`, `Perdida`, `Pendiente`, `Examen`, `Evento`, `Feriado`).
* `construirBloqueDia` *(interna, fix 2026-09-21)* - resuelve la sección de clases ANTES de decidir si el día está vacío: un día solo se descarta (con "días vacíos" apagado) si no tiene ni eventos ni clases visibles. Arregla "filtrar solo por Clase no muestra nada".
* `construirTarjetaEstudioHoy(item)` / `asegurarEstilosEstudioHoyAgenda()` *(internas, 2026-09-22 - "Estudio para hoy" de vuelta, con barra de progreso)* - `construirBloqueDia` las usa para meter, primero de todo dentro del bloque de HOY (antes de Materias/clases), una tarjeta por materia con nombre + barra de progreso (hecho vs. meta de hoy, clamp a 100%, color propio de la materia vía `--eh-color` con `color-mix()` contra `--bg-panel` - mismo patrón que `.timer-indicador*`, funciona en las 13 paletas y claro/oscuro) + cifras "20 min de 1 h 30 min" (o "Meta de hoy cumplida ✅" si la meta de hoy ya está en 0, repartida por `calcularMetaDiariaMateria`). Solo aparece si `configuracion.mostrar_tiempo_estudio_en_agenda === true` y `obtenerEstudioParaHoy()` (de `tiempo-estudio.js`, sin cambios ahí) devuelve algo; tocar una tarjeta lleva al detalle de esa materia (`irADetalleMateriaTiempoEstudio`). El CSS se inyecta una sola vez (guard por id `estilos-agenda-estudio-hoy`), mismo criterio que el resto de piezas autosuficientes del proyecto.

### agenda/agenda-utils.js
Propósito: helpers puros sin DOM, compartidos entre `agenda.js`, `agenda-clases.js` y `agenda-modal.js` - resolución de semestres seleccionados, cálculo de fechas de semana, formateo y estilos de eventos.
Depende de: config/config-ajustes.js, core/storage.js, core/utils.js, horario/horario.js, semestres/semestres.js
Exporta:
* `esHoyFecha(fecha)` - true si `fecha` es hoy (año/mes/día locales).
* `esTareaVencida(evento)` - true si es tarea sin completar **y sin perder** con fecha pasada (o con hora ya pasada hoy). Una tarea `perdida` nunca es vencida (2026-09-21).
* `formatearFechaISO(fecha)` - Date → "YYYY-MM-DD".
* `formatearHoraAmPm(horaStr)` - "HH:MM" (24h) → "h:mm a.m./p.m.".
* `formatearRangoSemanaAgenda(dias)` - texto compacto del rango de una semana, ej. "12 - 18 ago.".
* `formatearTiempoRestanteHoy(fechaISO)` - texto "Xh Ymin restantes" hasta las 23:59:59 de esa fecha.
* `obtenerCodigoDiaSemana(fecha)` - código canónico de día ("L".."D") de una fecha cualquiera.
* `obtenerDiasSemanaAgenda(offsetSemanas)` - los 7 días de la semana (offset respecto a hoy) con su fecha real resuelta.
* `obtenerDiasSemanaOrdenAgenda()` - `DIAS_SEMANA_CONFIG` rotado según `dia_inicio_semana`, sin filtrar por "días visibles" (Agenda siempre muestra los 7).
* `obtenerEstiloEvento(evento)` - `{etiqueta, claseBadge, colorBorde}` según tipo/estado del evento. **(2026-09-21)** una tarea `perdida` devuelve `{ etiqueta: "Perdida", claseBadge: "badge-perdida", colorBorde: "#7f1d1d", esPerdida: true }` (gana sobre `completada`).
* `obtenerFechaInicioSemanaAgenda(offsetSemanas)` - fecha del primer día de la semana mostrada, anclada a hoy.
* `obtenerInicioSemanaQueContiene(fecha)` - primer día de la semana que contiene una fecha arbitraria (usado por Calendario).
* `obtenerMateriasVinculablesAgenda()` - materias matriculadas de los semestres seleccionados, con nombre desambiguado si hay duplicados.
* `obtenerOffsetSemanaParaFecha(fecha)` - cuántas semanas de offset hay que aplicar a Lista para mostrar la semana de `fecha`.
* `obtenerRangoDiasAgendaTodo(semestre, diasAtras)` - array de días desde hoy (menos `diasAtras`) hasta fin de semestre +2 semanas (u 8 semanas fijas si no hay semestre).
* `obtenerSemestreActivoAgenda()` - el más reciente de los semestres seleccionados (para "Semana N" y fin de rango de "Todo").
* `obtenerSemestresSeleccionadosAgenda()` - resuelve el conjunto de semestres que Agenda muestra (automático o selección explícita), ordenado cronológico ASC.
* `tareaVenceHoy(evento)` - true si es tarea sin completar ni perder que vence hoy y todavía no pasó su hora.
* `formatearFechaRelativa(fecha, hoy = new Date())` - **(nueva, 2026-09-21)** texto relativo a hoy, única fuente de la escala en toda la app: futuro `Hoy` / `Mañana` / `Pasado Mañana` / `En N días`; pasado `Ayer` / `Antier` / `Hace N días` (N real desde 3, sin tope). Acepta `"YYYY-MM-DD"` o `Date`; devuelve `""` si es inválida. Compara días de calendario locales (`Date.UTC` de los componentes locales), no horas, así que ni medianoche ni un cambio de horario de verano la corren un día. `hoy` es solo para probarla con fecha fija. La usa `resumen/resumen.js` en todos sus bloques con fecha.
* `COLOR_PERDIDA_RAYA` - **(nueva, 2026-09-21)** `"#dc2626"`, color de la raya del tachado de una tarea perdida (el borde/badge usan `#7f1d1d`, más oscuro).

### agenda/agenda-clases.js
Propósito: sección "Materias" inline por día - qué clases del Horario caen ese día, mostradas junto a eventos/tareas/exámenes.
Depende de: core/schema.js, core/storage.js, horario/horario.js, agenda/agenda-utils.js
Exporta:
* `calcularNumeroSemanaParaFecha(semestre, fecha)` - número de semana del semestre para una fecha puntual (no depende de "hoy"), ACOTADA entre 1 y `duracion_semanas` a propósito (el header/detalle de Agenda-Calendario siempre necesita un número válido que mostrar). Revisada 2026-08-29: antes tenía su propia fórmula de "días desde `fecha_inicio` / 7" con `new Date(string)` directo (mismo tipo de bug de zona horaria ya resuelto en `horario.js`, sin el anclaje-a-lunes que ese cálculo sí usa) - ahora delega el cálculo crudo en `horario/horario.js#calcularNumeroSemanaSinAcotarParaFecha` y solo agrega el acotado. **No es la misma función** que esa (ver nota cruzada en `horario-modal.js`): mismo nombre corto por casualidad de historia, comportamiento distinto a propósito.
* `construirSeccionMateriasDia(semestres, fecha, diaCodigo)` - arma el bloque DOM "Materias" del día para varios semestres a la vez; devuelve `null` si no hay nada que mostrar.
* `contarClasesDelDia(semestres, fecha, diaCodigo)` - conteo liviano (sin DOM) de clases ese día, sumado entre todos los semestres - lo usa el Calendario para el indicador 📚.

### agenda/agenda-modal.js
Propósito: modal de alta/edición de EventoAgenda (evento/tarea/examen) y tarjeta de info al tocar un ítem de la lista. Al guardar/borrar/completar, sincroniza el espejo del evento en Google Calendar (ver core/notificaciones-calendario.js).
Depende de: core/notificaciones-push.js **(2026-08-25: pendiente de cambiar a core/notificaciones-calendario.js - llamar a sincronizarEventoCalendario/eliminarEventoCalendarizado en vez de las funciones viejas; no actualizado en esta sesión, archivo no subido)**, core/schema.js, core/storage-sync.js, core/storage.js, core/utils.js, ui/componentes.js, horario/horario.js, agenda/agenda-utils.js
Exporta:
* `abrirModalEventoAgenda({eventoId, fechaDefault, datosIniciales})` - abre el formulario para crear (si `eventoId` es null) o editar un evento. `datosIniciales` (2026-08-22, Asistente IA) precarga un borrador `{tipo, nombre, fecha, hora, notas}` que todavía NO es un evento real (no vive en `estado.datos.agenda`, sin id) - usado por asistente/asistente.js para mostrar lo que Gemini extrajo, editable, antes de confirmar. Nunca coexiste con `eventoId` (una edición real siempre gana); al guardar se crea un evento nuevo normal, el borrador nunca se persiste como tal.
* `abrirTarjetaInfoEventoAgenda(eventoId)` - abre la tarjeta de solo-info de un evento (paso previo al editor).
* `inicializarModalAgendaEvento()` - wiring de ambos modales (pills de tipo, checkbox "todo el día", dropdown de materia, botones guardar/borrar/cerrar).
* `manejarToqueCheckTarea(eventoId, alTerminar?)` - **(nueva, 2026-09-21, reemplaza al toggle de solo-completada; antes `alternarCompletadaEvento` en agenda.js y `alternarCompletadaDesdeInfo` acá)** único punto que resuelve el toque sobre el círculo de una tarea, compartido por la lista/Resumen (`agenda.js`) y la tarjeta de info. Ya completada o perdida → pendiente; pendiente y vencida → ventanita `Completado / Perdido` (interna `abrirSelectorEstadoTareaVencida`, dinámica, se cierra con Cancelar, tocar afuera o Escape sin cambiar nada); pendiente no vencida → completada directo. Internamente pasa por `aplicarEstadoTarea` (escribe `completada`/`perdida` siempre juntas para que nunca queden ambas en `true`, relee la entidad viva por id, sella, `marcarCambioPendiente`, refresca Agenda y Resumen y sincroniza Calendar). `alTerminar(viva)` es opcional; la tarjeta de info lo usa para repintarse.
* `guardarEventoAgenda` revalida `perdida` al editar: si el tipo deja de ser tarea o la fecha/hora ya no está vencida, la tarea vuelve a pendiente con un toast; mover la fecha a otra fecha igualmente pasada la deja perdida.

### agenda/agenda-calendario.js
Propósito: vista Calendario (mensual/semanal) - grid de 7 columnas con vistazo rápido de eventos por día; tocar un día salta a la vista Lista.
Depende de: core/storage.js, ui/componentes.js, agenda/agenda-clases.js, agenda/agenda.js, agenda/agenda-utils.js
Exporta:
* `renderizarCalendarioAgenda()` - entrypoint de render de la vista Calendario (subheader + grid mensual o semanal).

---

## JS - asistente

### asistente/asistente.js
Propósito: chat del Asistente IA (Gemini, clave propia por usuario), con personalidad ("Wapper", 2026-08-29) - convierte lenguaje natural en tareas/exámenes/eventos de Agenda, y desde 2026-08-29 también puede editar la modalidad de una clase puntual en Horario ("cambia mi clase de anatomía del jueves a virtual"), siempre con tarjeta de confirmación explícita antes de aplicar. Incluye dictado por voz (Web Speech API nativa, con fallback a transcripción por Gemini vía MediaRecorder en navegadores sin backend de voz confiable) e historial de conversación en `localStorage` del dispositivo, vigente 1 hora. Todo el texto de interfaz usa tú (nunca vos/usted).
Depende de: core/storage.js, core/schema.js, core/storage-sync.js, core/notificaciones-push.js, ui/componentes.js, agenda/agenda-modal.js, agenda/agenda-utils.js, horario/horario.js, horario/horario-modal.js, config/config-ajustes.js
Exporta:
* `renderizarAsistente()` - entrypoint de render (llamado por `mostrarSeccion("asistente")`, main.js); si no hay clave de Gemini guardada, muestra aviso con acceso directo a Ajustes en vez del chat. Si hay una conversación reciente (<1h) guardada en este dispositivo, ofrece continuarla o arrancar una nueva.
* `transcribirBase64ConGemini(base64, mimeType)` - transcribe audio ya en base64 a texto plano vía Gemini (mismo modelo/clave que la extracción). Reutilizada por `asistente-bandeja.js` (audio recibido directo del Worker, sin Blob real del que partir).
* `extraerEventosDeTexto(texto)` - variante STATELESS de la extracción (sin leer/tocar el historial visible del chat en pantalla): un solo turno aislado, para procesar ítems de la Bandeja pendiente sin mezclarlos con una conversación en vivo.
* `guardarItemExtraidoComoEvento(item)` - crea el `EventoAgenda` real (`core/schema.js#crearEventoAgenda`) a partir de un ítem ya extraído por Gemini, con recordatorio push si aplica.
* `mensajeParaError(e)` - texto de error listo para UI según `e.tipoError` ("clave" | "limite" | "red" | desconocido).

**Reintento ante 503/UNAVAILABLE de Gemini (2026-08-31, no exportado - bug real: preguntas de consulta caían en el genérico "Algo salió mal de mi lado" con la consola mostrando 503 de `generativelanguage.googleapis.com`, servidor de Gemini saturado momentáneamente):**
* `ejecutarGeneracionGemini(contents)` (interno) - antes de convertir un error HTTP en `tipoError: "desconocido"`, si el código es 503/500 o `error.status === "UNAVAILABLE"` (transitorio por definición) reintenta hasta 2 veces con pausa corta (700ms/1400ms) antes de rendirse. Un error real (clave inválida, límite 429, etc.) nunca entra en este camino - sigue fallando de inmediato con su `tipoError` correspondiente.
* `resolverMateriaVinculada(nombreMateria)` - match EXACTO contra `obtenerMateriasVinculablesAgenda()` primero (como siempre); si no hay exacto, fallback normalizado (sin acentos, minúsculas, espacios colapsados) SOLO si resuelve a una única materia sin ambigüedad - cubre diferencias menores entre lo que Gemini devuelve y el nombre guardado (espacio de más, acento, mayúscula).
* **Iniciales calculadas + red de seguridad por alias (2026-08-31, bug real: "administracion de proyectos"/"segundo parcial de ap2" devolvían de más sin filtrar por materia, "examen 2 administracion de proyectos" devolvía 0 - el prompt YA le decía a Gemini desde siempre que podía reconocer "(iniciales: AP2)" junto a cada materia, pero esa anotación nunca se generaba de verdad, solo apodos; Gemini terminaba adivinando la abreviatura por su cuenta, con resultados inconsistentes):**
  * `calcularInicialesMateria(nombreOficial)` - primera letra de cada palabra significativa del nombre oficial (ignora conectores: de/del/la/el/los/las/y/e/en/a/al/con/por/para/un/una), convirtiendo un número romano final a dígito en vez de tomar su inicial (ej. "Administración De Proyectos II" → "AP2", "Comportamiento Organizacional Y Talento Humano" → "COTH", ambos verificados). Devuelve "" si el resultado queda en menos de 2 caracteres (una sola letra no sirve como abreviatura reconocible). Ahora se muestra de verdad en la lista de materias del prompt (`construirListaMateriasConApodos`, junto a los apodos si tiene) - antes esa lista solo mostraba apodos.
  * `construirAvisoInicialesDuplicadas(materiasVinculables)` - mismo patrón que `construirAvisoApodosDuplicados`: si dos materias calculan a las mismas iniciales, agrega una advertencia al prompt para que Gemini pregunte en vez de adivinar cuál es.
  * `calcularAliasesMateria(materiaVinculada, apodosPorMateria)` - set de alias reales de una materia (nombre oficial normalizado + apodos + iniciales calculadas), usado tanto para mostrarle la lista a Gemini como por la red de seguridad de abajo.
  * `intentarResolverMateriaPorTexto(texto, materiasVinculables)` - red de seguridad en JS para cuando Gemini deja `materia` en null y el nombre de la materia quedó suelto en otro campo de texto libre (típicamente `consulta.palabrasClave`). Dos caminos: (1) nombre oficial PARCIAL - NO exige que todas las palabras del texto matcheen (puede traer ruido de más, ej. "examen"/números que Gemini no separó bien), alcanza con que 2+ palabras reales del nombre coincidan (o la única, si el nombre tiene una sola palabra significativa); (2) apodo o iniciales - coincidencia de TOKEN completo, incluyendo las iniciales SIN el dígito final ("ap" además de "ap2", regla explícita del prompt: el usuario puede omitir el número de nivel). Si matchean 2+ materias candidatas, no adivina (devuelve null, mismo criterio anti-ambigüedad de siempre). Verificado con casos reales: "administracion de proyectos", "ap2", "ap", "coth", "segundo parcial de ap2", "examen 2 administracion de proyectos" → resuelven bien; palabras genéricas sueltas ("examen", "tarea", "parcial", "laboratorio 5") → correctamente `null`, sin falso positivo.
  * `resolverMateriaConRedDeSeguridad(materiaTexto, materiasVinculables)` - envoltorio: intenta `resolverMateriaVinculada` primero (match estricto de siempre), y SOLO si falla intenta `intentarResolverMateriaPorTexto` sobre el mismo texto. Reemplaza la llamada directa a `resolverMateriaVinculada` en `resolverConsultaTareasEventos` y `resolverBusquedaEvento` para el campo `consulta.materia`.
  * En `resolverBusquedaEvento` específicamente: si `consulta.materia` vino vacío, además se intenta `intentarResolverMateriaPorTexto` sobre `consulta.palabrasClave` - y si encuentra una materia ahí, se sacan del filtro de palabras clave los tokens que en realidad eran el nombre de la materia (por `calcularAliasesMateria`), para no exigir que el TÍTULO del evento también los contenga literalmente (nunca iba a matchear "administracion"/"proyectos" contra "Segundo Parcial").

**Filtrado "suave" progresivo en `resolverBusquedaEvento` (2026-08-31, bug real: "Cuánto falta para II Parcial de Derecho Informático Y Mercantil" devolvía 0 resultados aunque la materia existe y tiene justo un "II Parcial" - los 4 filtros (materia + tipoItem + palabrasClave + numeroOrdinal) se aplicaban antes todos en un solo `.filter().filter().filter()` con AND; bastaba con que Gemini errara en UNO (ej. dejara "ii"/"parcial" en `palabrasClave` sin que calzaran exacto contra el título real) para que el combinado diera 0, aunque materia+ordinal solos ya apuntaban sin duda a un único evento):**
* Ahora cada filtro se prueba y SOLO se comete si no vacía el resultado - mismo criterio que YA usaba `numeroOrdinal` (no es nuevo el patrón, se extendió a los otros dos). Orden: materia primero (ÚNICA excepción que se aplica siempre aunque vacíe - es la señal más confiable; si no hay nada de esa materia, no hay nada que mostrar) → `tipoItem` (suave) → `palabrasClave` (suave) → `numeroOrdinal` (suave, como ya era). Si un filtro deja 0, se descarta ESE filtro puntual y se sigue con el conjunto anterior más ancho, no con el vacío.

**Forzado de "proximo" por regex, independiente de Gemini (2026-08-31, bug real: "cuando es el proximo examen?" seguía devolviendo TODOS los exámenes en vez de acotar - ni siquiera con la instrucción de prompt ya corregida para el caso sin materia, Gemini marcaba `"proximo": true` de forma confiable):**
* `mostrarResultadoConsultaEnChat(resultado, turno, textoUsuario)` - ahora recibe también `textoUsuario` (el mensaje TAL CUAL lo escribió el usuario, ya lo tenía disponible `mostrarResultadoEnChat`/`manejarEnvioMensaje`, solo faltaba pasarlo un nivel más). Si `consulta.tipo === "buscar_evento"`, Gemini no marcó `proximo` y no hay `numeroOrdinal` puntual, se revisa el texto original directo contra `/\bpr[oó]xim[oa]\b|\bsiguiente\b/i` y si matchea se fuerza `consulta.proximo = true` ahí mismo, antes de resolver - mucho más confiable que depender de que un booleano generado por el modelo salga bien siempre. No aplica a `modalidad_clase` (que sí puede decir "próxima clase" con otro sentido, sin relación a este concepto) ni al reconstruir desde historial (`textoUsuario` no viene ahí, no hace falta - `consulta` ya quedó resuelta la primera vez).

**Detección de "¿qué puedes hacer?"/saludo tolerante a errores de tipeo (2026-08-31, bug real: `"Que puedes hacer'"` - comilla simple de más al final en vez de "?" - no matcheaba el patrón y caía al fallback genérico de extracción):**
* `esPreguntaCapacidades(texto)`/`esSaludoSimple(texto)` - la limpieza de puntuación antes de probar los patrones ahora también saca comillas simples/dobles y tildes sueltas (`'"´\``), no solo `¡!¿?.,;:` como antes. Mismo criterio en ambas funciones (comparten el mismo enfoque de regex sobre texto normalizado).

**Nombre personalizado / trato cercano (2026-08-29, no exportado - pedido: Wapper se dirige al usuario por su nombre, como el que ya se ve en el sidebar, con posibilidad de cambiarlo por un apodo permanente SOLO para el Asistente):**
* **Confirmado con `main.js`** (2026-08-29, bug real reportado: "cómo me llamo?" respondía que no tenía acceso al nombre - la asunción original `estado.perfil.nombre` estaba mal, siempre devolvía `null`): el nombre de la cuenta de Google vive en `estado.datos.perfil.nombre` (dentro del JSON sincronizado, lo pone `obtenerPerfilGoogle`/`auth.js` justo después del login), NO en un campo de sesión aparte. Ya corregido en `obtenerNombrePerfilGoogleCrudo()`.
* `obtenerNombreParaDirigirse()` - el nombre/apodo con el que Wapper se dirige al usuario ahora mismo: primero `estado.datos.configuracion.asistente_nombre_preferido` (si el usuario pidió un apodo, PERMANENTE hasta que lo cambie de nuevo - nunca toca `estado.perfil`, el nombre del sidebar sigue intacto), si no el primer nombre de `estado.perfil.nombre` (`obtenerPrimerNombre`), si no `null` (Wapper sigue funcionando igual sin nombre).
* `resolverActualizacionNombre(nombreNuevo)` - accion nueva `"actualizar_nombre"` (ej. "llámame Fer"): guarda el apodo en `estado.datos.configuracion.asistente_nombre_preferido` y llama a `marcarCambioPendiente()` (mismo mecanismo de persistencia que ya usa `guardarItemExtraidoComoEvento`). Se aplica de inmediato, sin tarjeta de confirmación - decisión de diseño: es un cambio de bajo riesgo y fácil de revertir (pedir otro nombre), a diferencia de `editar_modalidad` que si tiene confirmación por tocar el Horario real.
* `construirPromptPersonalidadWapper()` - arma `PROMPT_PERSONALIDAD_WAPPER` + una frase con el nombre actual (si hay uno), instruyendo humor liviano si el nombre suena a apodo/meme y qué contestar si preguntan de dónde salió ("de tu cuenta de Google"). Se arma de nuevo en cada llamada a `generarRespuestaConversacionalWapper` (nunca se cachea, para que un apodo recién pedido se use ya en la siguiente respuesta). El prompt de extracción (`construirSystemInstruction`) sigue intacto - la personalización NO se mete ahí, mismo principio ya establecido de separar personalidad de extracción.
* `construirMensajeBienvenidaWapper()`/`construirMensajeSaludoWapper()` - mismo saludo de siempre, con el nombre al inicio si se conoce.

**Editar modalidad por voz/texto (2026-08-29, no exportado - funciones internas):**
* El prompt de extracción (`construirSystemInstruction`) ahora pide a Gemini un campo discriminador `accion: "crear_eventos" | "editar_modalidad" | "consultar"`, con `cambioModalidad {materia, dia, modalidadNueva}` o `consulta {tipo, semana, materia, dia}` según corresponda. Gemini nunca decide materia/día por sí solo si hay ambigüedad - devuelve `aclaracion` en vez de adivinar (mismo principio anti-alucinación que ya usaba la extracción de eventos).
* `resolverCambioModalidad(cambioModalidad)` - revalida lo que devolvió Gemini contra datos reales de Horario (materia vinculada, día con clase real, modalidad válida) y calcula la próxima fecha/semana real del cambio vía `horario/horario-modal.js#calcularNumeroSemanaSinAcotarParaFecha`.
* `crearTarjetaConfirmacionModalidad(resuelto, estadoInicial, onDecision)` - tarjeta "Aplicar cambio"/"Cancelar"; el cambio real (`horario/horario-modal.js#aplicarModalidadDia`) solo se dispara al tocar "Aplicar cambio", nunca antes.
* El estado de esa decisión (`pendiente`/`aplicado`/`cancelado`) y la resolución ya congelada (`cambioModalidadResuelto`, serializada sin objetos `Date`) se guardan en el turno del historial local, para que reabrir el chat dentro de la 1h de vigencia muestre la tarjeta en su estado real sin poder re-aplicarla.

**Consultar (2026-08-29, no exportado - corrige bug real: preguntas de solo lectura, ej. "qué tareas tengo esta semana"/"qué modalidad es mi próxima clase de bd"/"cuándo es el tercer parcial de cálculo"/"cuántos días faltan para el 3 parcial de cálculo"/"qué exámenes tengo para semana 8", caían al fallback conversacional de Wapper sin acceso real a los datos, o devolvían de más):**
* `resolverRangoConsulta(numeroSemana)` - rango de fechas (inicio/fin, ambos inclusive) para una consulta de tareas/eventos GENERAL (tipo `tareas_eventos`): si `numeroSemana` viene explícito, calcula sobre el semestre activo (`obtenerSemestreActivoAgenda`, agenda-utils.js) con la misma fórmula que usa la tabla de semanas del prompt; si no, usa `obtenerFechaInicioSemanaAgenda(0)` (agenda-utils.js) - la MISMA noción de "esta semana" que ya usa la vista Lista de Agenda.
* `resolverConsultaTareasEventos(consulta)` - filtra `estado.datos.agenda` por el rango de `resolverRangoConsulta`, opcionalmente por materia (`resolverMateriaVinculada`) y opcionalmente por tipo de ítem (`tipoItem`: "examen"/"tarea"/"evento" - 2026-08-29: antes mezclaba tareas y exámenes sin distinguir aunque el usuario pidiera explícitamente "exámenes"). **`consulta.alcance === "todo"` (2026-08-31, bug real: "de todo el semestre" seguía devolviendo solo la semana actual - el campo ya estaba en el schema/prompt pero este resolver nunca lo leía):** cuando aplica, `resolverRangoConsulta` NUNCA se llama - se trae TODO `estado.datos.agenda` sin filtro de fecha (pasado y futuro incluidos, tal cual pide el usuario), materia/tipo siguen filtrando igual, y el `rango` devuelto usa `inicio`/`fin` en `null` con etiqueta fija "todo el semestre" (el caller arma el encabezado sin rango de fechas cuando detecta `inicio: null`).
* `resolverBusquedaEvento(consulta)` - tipo `buscar_evento`: busca UN ítem puntual por nombre (ej. "el tercer parcial de cálculo", "el laboratorio 4 de bd") en TODO `estado.datos.agenda`, sin límite de fecha - filtra por materia/tipo de ítem/palabras clave del título, y usa `numeroOrdinal` (convertido de dígito, palabra ordinal o número romano - "Parcial I" = "primer parcial" = "Parcial 1", ver `nombreEventoMencionaNumero`/`PALABRAS_ORDINALES_A_NUMERO`/`NUMEROS_A_ROMANO`) SOLO para desempatar si hay más de un candidato, nunca para descartar el único resultado que ya matcheó por texto. **Red de seguridad (2026-08-29, bug real: "el primer parcial de derecho" devolvió 8 resultados de materias/tipos no relacionados cuando Gemini no resolvió bien materia/número en ese turno):** si aun con los filtros el resultado supera `LIMITE_RESULTADOS_BUSQUEDA_EVENTO` (3), NO se listan todos - se le pide al usuario precisar materia o nombre/número exacto. **`consulta.proximo === true` (2026-08-31, bug real: "cuánto falta para el próximo examen de AP2" devolvía TODOS los exámenes de AP2, pasados incluidos - mismo caso que `alcance`, el campo estaba en el schema/prompt pero sin conectar):** después de ordenar por fecha, se descartan los eventos con `fecha` anterior a hoy (`obtenerContextoFechaHoy().iso` - hoy mismo SÍ cuenta como "el próximo") y se devuelve solo el más cercano. El "cuánto falta" en sí lo sigue mostrando `formatearDiasFaltantes` sobre ese único resultado (mismo mecanismo que ya usaba cualquier búsqueda de un solo ítem, sin cambios). Devuelve también `tipoItem`/`materiaVinculada`/`proximo` (antes solo `materiaVinculada`/`eventos`) para que el caller arme un mensaje de "vacío" correcto cuando `proximo` no encuentra nada - ver `mostrarResultadoConsultaEnChat` más abajo.
  * **Ajuste de prompt (2026-08-31):** la instrucción original de `"proximo"` solo daba ejemplos con materia mencionada ("el próximo examen de AP2"), lo que en la práctica hacía que "cuánto falta para el próximo examen" (SIN materia) cayera a búsqueda por palabra clave ("examen") en vez de `proximo: true` - devolvía de más (8 coincidencias) en vez de acotar al más cercano. Se reescribió la instrucción para cubrir explícitamente el caso sin materia, y se agregaron `"alcance"`/`"proximo"` al bloque de "forma exacta" del JSON que ve Gemini (antes solo estaban en el `responseSchema` forzado, no en el ejemplo textual - inconsistencia que podía reforzar que Gemini los tratara como inexistentes).
  * **Mensaje vacío diferenciado (2026-08-31):** cuando `proximo` no encuentra ningún ítem futuro, `mostrarResultadoConsultaEnChat` ya NO muestra el genérico "No encontré nada con ese nombre… revisa si está escrito distinto" (engañoso: la pregunta se entendió bien, simplemente no queda ninguno pendiente) - muestra en su lugar "¡Buenas noticias! No tienes más [exámenes/tareas/eventos] [de MATERIA] pendientes 🎉", mismo tono positivo que ya usaba el caso vacío de `tareas_eventos`. `turno.consultaBusquedaProximo`/`turno.consultaMateriaNombre` (nuevos, junto a `turno.consultaTipoItem` ya existente) cargan estos datos y persisten en el historial local igual que `consultaEventoIds` (mismo patrón: se escriben una vez en el turno real y sobreviven la reconstrucción del chat).
* `fraseDemostrativaTipoItem`/`FRASES_TIPO_ITEM`/`FRASES_TIPO_ITEM_PLURAL` - frases naturales con género correcto ("estos exámenes"/"esta tarea") para el encabezado de `tareas_eventos` cuando se filtró por tipo; si no hay resultados y sí se filtró por tipo, mensaje cálido acorde a la personalidad Wapper (ej. "¡Buenas noticias! No tienes exámenes para esa semana 🎉") en vez del genérico "no tienes nada guardado".
* `resolverConsultaModalidad(materiaNombre, diaNombreOpcional)` - SOLO LECTURA: busca la próxima clase real de esa materia y su modalidad EFECTIVA de esa semana puntual (`obtenerClasesEfectivasSemana`, `core/schema.js` - fusiona la plantilla con la excepción de Cronograma, importada directo en `asistente.js` igual que ya hace `horario.js`; `numeroSemana` calculado con la misma `calcularNumeroSemanaSinAcotarParaFecha` que ya usa `resolverCambioModalidad`).
* `formatearDiasFaltantes(fechaEventoIso)` - "faltan N días" / "es hoy" / "fue hace N días", recalculado en vivo (nunca congelado) contra la fecha real de hoy. Se agrega SIEMPRE que `buscar_evento` devuelve un único resultado.
* `mostrarResultadoConsultaEnChat(resultado, turno)` - para `tareas_eventos`/`buscar_evento`, pinta los eventos encontrados con `crearTarjetaEventoGuardado` (la MISMA tarjeta editable/borrable que usa la creación); para `modalidad_clase`, un texto corto de solo lectura. Los ids/resolución quedan congelados en `turno.consultaEventoIds` / `turno.consultaEsBusqueda` / `turno.consultaTipoItem` / `turno.consultaRangoTexto` / `turno.consultaBusquedaDemasiados` / `turno.consultaModalidadResuelto` (mismo patrón que `eventosGuardados`/`cambioModalidadResuelto`) para que reabrir el chat no vuelva a llamar a Gemini - excepto `formatearDiasFaltantes`, que se recalcula cada vez que se pinta.


**Personalidad "Wapper" (2026-08-29, no exportado - funciones/constantes internas):** ajuste de tono, separado A PROPÓSITO del prompt de extracción (`construirSystemInstruction`/`ESQUEMA_RESPUESTA_GEMINI`, que sigue frío y preciso, sin tocar).
* `MENSAJE_BIENVENIDA_WAPPER` + `mostrarSaludoInicial()` - texto fijo de bienvenida ("Soy Wapper…") más UNA sola línea "Ejemplo: …" en texto plano (sin botón), elegida al azar por `elegirEjemploBienvenidaAlAzar()` entre las 12 plantillas de `PLANTILLAS_EJEMPLOS_BIENVENIDA_WAPPER` ya resueltas por `construirEjemplosBienvenida()`. Corrección 2026-08-29: reemplaza la fila de chips clicables (`crearChipEjemplo`, eliminada) - nunca se muestran las 12 a la vez ni son seleccionables. El placeholder del input (`construirEsqueletoAsistente`) usa la misma función para su propio random independiente, reemplazando el placeholder fijo anterior. 2026-08-31 (punto 8 del brief): se agregó una TERCERA burbuja fija debajo del ejemplo - "¿No sabes por dónde empezar? Pregúntame qué puedo hacer." (tuteo, consistente con el resto de la interfaz) - invitando a `esPreguntaCapacidades`. Es un agregado, no reemplaza el ejemplo random existente.
* `obtenerNombresMateriasParaEjemplosBienvenida()` - reutiliza `obtenerMateriasVinculablesAgenda()` (agenda/agenda-utils.js, la misma fuente que ya usa `construirSystemInstruction` para saber qué materias existen) para las materias reales matriculadas; si no hay ninguna, cae a `MATERIAS_GENERICAS_RESPALDO` (anatomía/historia/química/cálculo/estadística/física). Nunca mezcla ambas listas a medias - si hay menos materias reales que huecos, se repiten rotando.
* `esSaludoSimple(texto)` - detecta un saludo SIN contenido adicional (regex sobre texto normalizado); si matchea, `manejarEnvioMensaje` responde con `MENSAJE_SALUDO_WAPPER` sin llamar a Gemini en absoluto (punto 4 del brief: "sin intentar extraer nada").
* `CAPACIDADES_WAPPER` (2026-08-31, punto 7 del brief) - arreglo mantenible `{descripcion, ejemplo}[]` con las capacidades REALES ya implementadas de Wapper, en orden lógico (crear → consultar tareas/exámenes → buscar ítem puntual → consultar modalidad → cambiar modalidad → cambiar apodo; las dos entradas de modalidad quedan juntas a propósito, ajuste 2026-08-31). `ejemplo` es un string fijo solo para "Consultar qué tareas o exámenes tienes en una semana puntual" (no depende de materia); las demás son FUNCIONES que se resuelven al vuelo en cada llamada a `construirMensajeCapacidadesWapper` - las de materia vía `elegirNombreMateriaEjemplo()` (2026-08-31, pedido explícito: antes usaban nombres genéricos inventados como "anatomía"/"física" sin importar si el usuario los llevaba - ver abajo), la de apodo vía `APODOS_EJEMPLO_CAPACIDADES` al azar. Agregar una capacidad nueva en el futuro (ej. Tiempo de Estudio) es sumar una entrada acá - no hay texto de la lista enterrado en ningún prompt de Gemini ni hardcodeado aparte.
* `elegirNombreMateriaEjemplo()` (2026-08-31) - reutiliza `obtenerNombresMateriasParaEjemplosBienvenida()` (mismo helper que ya usaba la pantalla de bienvenida, ver abajo) para que los ejemplos de `CAPACIDADES_WAPPER` y de `construirMensajeFallbackWapper()` nombren una materia REAL matriculada del usuario al azar, en vez de "anatomía"/"física"/"cálculo"/"bases de datos" fijas. Cae a `MATERIAS_GENERICAS_RESPALDO` solo si el usuario no tiene ninguna materia matriculada - caso raro, no el default.
* `APODOS_EJEMPLO_CAPACIDADES` (2026-08-31) - 22 apodos de ejemplo (Fer, Pipo, Juanjo, Male, Santi, Naty, Pao, Andy, Nacho, Kike, Meli, Fabi, Caro, Tavo, Rodri, Gaby, Chepe, Mafe, Toño, Vicky, Lalo, Beto) usados SOLO para el ejemplo de "actualizar_nombre" en la lista de capacidades - no tiene relación con `asistente_nombre_preferido` real del usuario ni con `resolverActualizacionNombre`. "Vale" sacado a propósito de esta lista (regla explícita: no puede aparecer).
* `construirMensajeCapacidadesWapper()` - arma el texto "Puedo ayudarte con: • descripcion.\nEjemplo: "..."" (viñeta "•", ejemplo en su propia línea debajo de la descripción - ajuste 2026-08-31) recorriendo `CAPACIDADES_WAPPER` en vivo (nunca cacheado); si `c.ejemplo` es función la ejecuta en el momento, mismo principio que `construirEjemplosBienvenida`.
* `esPreguntaCapacidades(texto)` (punto 7) - mismo mecanismo que `esSaludoSimple` (regex sobre texto normalizado, acepta voseo y tuteo en la ENTRADA del usuario): detecta preguntas tipo "¿qué podés hacer?"/"¿para qué servís?"/"ayuda" que sean (casi) todo el mensaje, para no interceptar un mensaje real que solo menciona "ayuda" de paso. Si matchea, `manejarEnvioMensaje` responde con `construirMensajeCapacidadesWapper()` sin llamar a Gemini - mismo atajo que el saludo simple, con marcador local `accion: "capacidades"` (nunca lo devuelve Gemini; agregado también a la rama de reconstrucción de `reconstruirChatDesdeHistorial`).
* `PROMPT_PERSONALIDAD_WAPPER` + `generarRespuestaConversacionalWapper(textoUsuario)` - llamada APARTE a Gemini (mismo modelo/clave, sin `responseSchema`, texto libre) usada SOLO cuando la extracción normal devuelve `items: []` sin `aclaracion` (charla suelta sin tarea/examen/evento reconocible). El resultado se congela en `turno.respuestaConversacional` (mismo patrón que `eventosGuardados`/`cambioModalidadResuelto`) para que reconstruir el historial no vuelva a llamar a Gemini; si la llamada falla o no hay `textoUsuario` (reconstrucción), cae a `construirMensajeFallbackWapper()` (2026-08-31: ahora función, no string fijo - mismo motivo que `CAPACIDADES_WAPPER`, para que su ejemplo también nombre una materia real vía `elegirNombreMateriaEjemplo()` en vez de "anatomía" fija).
* `mostrarResultadoEnChat`/`mostrarResultadoEventosEnChat` - ahora `async`, con una rama nueva `accion === "saludo"` (marcador puramente local, nunca lo devuelve Gemini - el schema solo admite `crear_eventos`/`editar_modalidad`) y la rama de fallback reescrita para usar `generarRespuestaConversacionalWapper` en vez de un mensaje estático fijo.

**Nota histórica reemplazada por la revisión v3.20.0:** la Bandeja está en `core/asistente-bandeja.js`; su único export es `sincronizarBandejaPendiente()` (documentado en `core/asistente-bandeja.js` arriba).

---

## JS - Tiempo de Estudio

### tiempo-estudio/tiempo-estudio.js
Propósito: núcleo de la sección Tiempo de Estudio - vista principal (filtro "Esta semana / Todas", tarjetas por materia matriculada con barra de meta + acceso al timer), pantalla de detalle por materia (timer + progreso en vivo) y el modal de ajustes globales de la sección. Las materias disponibles son SIEMPRE las de `obtenerSemestresActuales()` (`semestres/semestres.js`) - cada materia matriculada (`mm`) tiene su propia config/sesiones, aunque sea una repetición de la misma materia del Plan.
Depende de: core/storage.js, core/storage-sync.js, core/utils.js, core/schema.js (`COLOR_TIEMPO_ESTUDIO_DEFAULT`), ui/componentes.js, ui/buscar-materia.js, semestres/semestres.js, main.js, tiempo-estudio/tiempo-estudio-config.js, tiempo-estudio/tiempo-estudio-timer.js, tiempo-estudio/tiempo-estudio-registro.js (Parte 3), tiempo-estudio/tiempo-estudio-estadisticas.js (Parte 3), tiempo-estudio/tiempo-estudio-racha-ui.js (racha, 2026-09-19)
Exporta:
* `inicializarTiempoEstudio()` - cablea el badge/click de acceso rápido y, **desde la Parte 2**, llama primero que nada a `revisarSesionOlvidadaAlAbrir()` (tiempo-estudio-timer.js) - el chequeo del salvavidas corre una sola vez al arrancar la app, antes de que exista ningún DOM propio de esta sección (no depende del badge).
  * **(2026-09-19, rediseño del indicador)** el cableado del badge viejo se reemplazó por `montarIndicadoresTimer(...)` (tiempo-estudio-indicador.js) alimentando los 3 contenedores a la vez, suscrito con `suscribirseATimer`. Funciones nuevas *(internas)* en `tiempo-estudio.js`: `resolverInfoIndicadorTimer(activo)` - busca la materia entre las de los semestres ACTUALES y devuelve `{ nombre: nombreMateriaCorto (sin código), color: obtenerColorMateria(...), tiempo: formatearDuracion(segundosTranscurridos()) (la FASE en curso, lo mismo que ve el detalle), pausado, estado: "En pausa" | "Descanso" | "" }`; si la materia ya no está entre las actuales cae a "Materia" + color por defecto en vez de romper. `irAMateriaDelTimerActivo()` - abre el detalle de la materia del timer (`materiaDetalleActivaId` + `mostrarSeccion("tiempo-estudio")`), y solo fuerza `renderizarTiempoEstudio()` si no se estaba ya en ese detalle (para no perder lo que se esté haciendo ahí).
  * **(2026-09-19, tiempo extra)** `construirPantallaDetalle` suma el renglón `.te-timer-extra` bajo el cronómetro (oculto sin Pomodoro; con Pomodoro reserva su alto), el botón "☕ Descanso" y el "Terminar/Saltar descanso" dinámico; la etiqueta de fase agrega "completado". `resolverInfoIndicadorTimer` muestra `40:00 +05:12` con estado "Extra" / "Descanso" (el `estado` no cambia con los segundos, para no mover la etiqueta accesible).
  * **(2026-09-19, Parte B)** registra UNA vez (bandera `_hookRepintadoTiempoRegistrado`) un hook con `registrarHookPostFusion` (storage-sync.js) que llama a `renderizarTiempoEstudio()` tras CADA fusión remota (sondeo ~9 s, pull-to-refresh, otra pestaña vía BroadcastChannel, login, y también los syncs propios). Antes `aplicarDatosRemotosFrescos` repintaba Semestres/Finanzas/Plan pero nunca Tiempo: una sesión guardada en otro dispositivo llegaba a `estado.datos` pero el DOM quedaba congelado. El scroll lo protege la reafirmación del propio lote de `aplicarDatosRemotosFrescos`. Costo conocido: estando en la vista Competencias, cada sync vuelve a pedir el marcador.
  * **(2026-09-19, Parte B)** `construirTarjetaMateria`: una materia SIN meta ahora muestra `"1 h 30 min esta semana · sin meta"` si hay tiempo estudiado esta semana (antes solo "Sin meta configurada"); sin sesiones sigue igual, con meta el formato no cambia. No se dibuja barra sin meta.
* **2026-09-07 (pausa + historial editable + estadísticas individuales):** la pantalla de detalle y la tarjeta de materia ahora muestran, mientras el timer de ESA materia está activo, 2 controles separados en vez de un solo botón: play/pause (`pausarTimerEstudio`/`reanudarTimerEstudio` de `tiempo-estudio-timer.js`, pausa sin cerrar la sesión) y "Detener sesión" (cierra y guarda, el mismo `manejarBotonIniciarDetener` de siempre). Además, `construirPantallaDetalle` ahora llama, después del panel de progreso: `construirEstadisticasMateria()` (tiempo-estudio-estadisticas.js - resumen de metas en líneas, tendencia de horas trabajadas en barras del color propio de la materia, y el bloque final de totales/día más productivo/sesiones/promedio) y `construirListaSesiones()` (tiempo-estudio-registro.js - historial de sesiones de ESA matrícula puntual, cada una editable con fecha+hora de inicio/fin por separado -admite cruzar medianoche/varios días- y borrable con tumba).
* `renderizarTiempoEstudio()` - entrypoint de render de toda la sección; también expuesta como `window.renderizarTiempoEstudio` para refrescar desde otros módulos sin import circular (mismo patrón que Agenda/Horario). **Parte 3**: ahora arma encabezado + pill "Materias/Estadísticas" UNA sola vez (`construirEncabezado`/`construirPillVistaSeccion`) y despacha a `construirVistaPrincipal` (existente) o a `construirVistaEstadisticas` (tiempo-estudio-estadisticas.js) según `vistaSeccionTE` - ninguna de las 2 pantallas dibuja su propio encabezado. Ese pill NUNCA aparece dentro del detalle de una materia puntual (Estadísticas es un agregado de todas las materias).
* **2026-09-17 (Partes 1.4 y 2):** el texto visible de la sección pasó de "Tiempo de Estudio" a **"Tiempo"** (`construirEncabezado` → `titulo.textContent`, el `title`/`aria-label` del engranaje, y el `<h2>` del modal de ajustes). **No se renombró ningún archivo ni carpeta** - los imports siguen apuntando a `js/tiempo-estudio/*`, y en los comentarios internos el nombre viejo quedó tal cual. `construirPantallaDetalle` suma un botón **"⏭ Saltar descanso"** (`saltarDescansoPomodoro` de tiempo-estudio-timer.js) a la fila de acción, que `pintar()` muestra únicamente cuando el timer activo es de ESTA materia Y está en una fase de descanso de Pomodoro. `abrirModalAjustesTiempoEstudio` perdió su handler de "clic afuera cierra" (pedido 2.2).
* `obtenerEstudioParaHoy()` - usado por el switch "¿Mostrar tiempos de estudio en Agenda?" (`configuracion.mostrar_tiempo_estudio_en_agenda`) para la sección "Estudio para hoy" de Agenda Lista. **"Entrega 3" (2026-09-09, ya no PROVISIONAL):** dejó de repartir la meta semanal parejo entre los 7 días - ahora usa `calcularMetaDiariaMateria()` (importada de `tiempo-estudio-estadisticas.js`, misma cuenta exacta que ve el usuario en "Resumen de metas", para que Agenda y Tiempo de Estudio nunca muestren dos números distintos para "cuánto toca hoy"). Además, las materias cuyo `dias_estudio` no incluye el día de hoy quedan **afuera del todo** de la lista devuelta - antes entraban todas las materias con meta configurada sin importar el día. Como Agenda solo lee esta función, automáticamente terminó mostrando nada más que las materias que corresponde estudiar hoy, sin que `agenda.js` necesitara ningún cambio de su lado.
* `irADetalleMateriaTiempoEstudio(materiaMatriculadaId)` - navega directo a la pantalla de detalle de una materia; es la acción de la opción "Tiempo de Estudio" en el menú `abrirBuscarMateriaEn()` (`ui/buscar-materia.js`).
* `formatearHorasMin(minutosTotales)` - helper de formato ("2h 15m" / "45m") reusado por las tarjetas, el detalle y el panel de progreso en vivo.
* `obtenerMateriasParaTiempoEstudio()` - **(exportada desde Parte 3)** recorre las materias matriculadas de los semestres actuales y las materias independientes propias de Tiempo, resolviendo sus datos sin incorporarlas a Plan/Semestres; ya existía como helper privado, se exporta para que `tiempo-estudio-registro.js` reciba la lista sin recalcularla ni crear un import circular de 3 puntas. Incluye las entradas independientes creadas en Tiempo.

Cambios de comportamiento de la Parte 2 (sin tocar el diseño ya aprobado):
* `manejarBotonIniciarDetener` distingue el toast según lo que devuelva `detenerTimerEstudio()` - `"Sesión guardada"` si cerró un bloque de trabajo/timer simple, `"Descanso descartado (no se guardó nada)"` si lo que se detuvo fue un descanso de Pomodoro (los descansos nunca generan sesión).
* `construirPantallaDetalle` agrega una etiqueta de fase de Pomodoro ("Bloque 2 de 4 · Descanso corto", misma clase `.muted` ya usada en el resto del panel) y convierte el panel de progreso en una función `pintarProgreso()` que se repinta en cada tick del timer (antes se calculaba una sola vez al entrar a la pantalla) - necesario para el excedente en vivo sobre la meta semanal mientras el timer sigue corriendo.

Cambios de la Parte 3:
* `construirEncabezado` suma un botón "＋" (mismo tamaño que el engranaje) que abre `abrirModalRegistroManual()` - único punto de entrada al registro manual, visible sin importar en qué pill estés parado.
* `construirPillVistaSeccion(cont)` (nueva, privada) - dibuja el pill "Materias/Estadísticas"; al cambiar, solo actualiza `vistaSeccionTE` (módulo-nivel, no persistida) y llama a `renderizarTiempoEstudio()`.
* `construirVistaPrincipal` ya NO dibuja su propio encabezado (se movió a `renderizarTiempoEstudio`, compartido con Estadísticas) - si algo llamaba a esta función esperando que también dibujara el encabezado, hay que revisar (no debería haber otros llamadores fuera de este archivo).

* **2026-09-19 (racha de estudio):** `construirEncabezado` agrupa título + chip 🔥 en `.te-encabezado-titulo-grupo` (por la misma razón que el grupo de botones: `row-between` reparte el espacio entre TODOS sus hijos) y llama a `construirChipRacha()`. `inicializarTiempoEstudio` suma `inicializarRacha()`, un 2.º hook post-fusión (`alFusionarDatosRacha`, DESPUÉS del de repintado) y un respaldo `setTimeout(alFusionarDatosRacha, 12000)` para quien abre la app sin conexión (sin fusión no hay hook; es idempotente). Ver `tiempo-estudio-racha-ui.js`.

### tiempo-estudio/tiempo-estudio-config.js
Propósito: modales de configuración de Tiempo de Estudio - meta de horas + días de estudio + Pomodoro opcional por materia matriculada, y el Pomodoro predeterminado global de la cuenta (semilla para la primera vez que cualquier materia activa Pomodoro). Modales 100% construidos en JS (mismo patrón que el alta/edición de semestre), sin markup fijo en `index.html`.
Depende de: core/schema.js (`COLOR_TIEMPO_ESTUDIO_DEFAULT`, `crearConfigPomodoroDefault`, `sellarTimestamp`), core/storage-sync.js, core/storage.js, ui/componentes.js (`mostrarToast`), config/config-ajustes.js (`DIAS_SEMANA_CONFIG`, 2026-09-09)
Exporta:
* `abrirModalConfigTiempoEstudio(mm, nombreMateria, onGuardar)` - modal por materia: meta semanal, **días de estudio** (2026-09-09, "Entrega 3" - ver `construirPillDiasEstudio` abajo), color, y toggle "Usar Pomodoro" con sus 4 campos (duración de bloque, cantidad de bloques, descanso corto, descanso largo). La primera vez que una materia activa el toggle, precarga el Pomodoro predeterminado de la cuenta (`obtenerPomodoroPredeterminado()`); de ahí en más cada materia guarda su propia copia independiente. `onGuardar` se llama sin argumentos después de guardar, para que quien abrió el modal re-renderice sin que este archivo necesite saber de tarjetas/detalle.
* `construirPillDiasEstudio(diasEstudioActuales)` *(interna)* - **(2026-09-09, nuevo)** pill-group de 7 botones (mismo código de día y mismo orden L→D que `DIAS_SEMANA_CONFIG`, usado también por "Días visibles" en Ajustes de Horario) para marcar/desmarcar días de estudio; `null` arranca con los 7 marcados. Mismo criterio que "Días visibles": no se permite destildar el último día activo (una meta sin ningún día para cumplirla no tiene sentido). Guarda el `Set` vivo de seleccionados como `cont._seleccionados` para que el handler de "Guardar" del modal lo lea directo, sin reconstruirlo recorriendo clases `active`.
* **2026-09-17 (pedido 2.2):** los 2 modales de este archivo perdieron su handler de "clic afuera cierra" - se cierran únicamente con su botón "Cancelar". Mismo cambio en los 2 de `tiempo-estudio-registro.js`, en el de ajustes de `tiempo-estudio.js`, en el salvavidas de `tiempo-estudio-timer.js` y en todos los de Competencias (vía `construirCajaModal`).
* `abrirModalPomodoroPredeterminado(onGuardar)` - modal del Pomodoro "de fábrica" de la cuenta (Ajustes → Ajustar pomodoro predeterminado). Sin meta/color/toggle: siempre está "activo" porque ES el default. Cambiarlo acá no afecta materias que ya tienen su propia config guardada.

### tiempo-estudio/tiempo-estudio-timer.js
Propósito: motor del timer - **único punto de entrada real** para iniciar/detener un timer de estudio; la regla de "una sola sesión activa a la vez" se hace cumplir acá adentro, no confiando en que cada botón de la UI la respete por su cuenta. Parte 1 fue un cronómetro simple en memoria; **Parte 2 (2026-09-04)** agregó el motor de Pomodoro, las alertas de cambio de bloque, el excedente en vivo sobre la meta semanal y el salvavidas de sesión olvidada.
Depende de: core/schema.js (`crearSesionEstudio`, `sellarTimestamp`), core/storage-sync.js (`marcarCambioPendiente`), core/storage.js (`estado`), ui/componentes.js (`mostrarToast`)
Exporta:
* `hayTimerActivo()` - `true`/`false` según si hay un timer corriendo (para cualquier materia). **(2026-09-17, punto 3.5 de la auditoría)** también se cuelga de `window.hayTimerActivo` para que `pedirConfirmacionCerrarSesion()` (main.js) pueda consultarlo sin crear un import nuevo entre ambos archivos - mismo patrón que `window.renderizarTiempoEstudio`/`window.renderizarHorario`.
* `obtenerTimerActivo()` - el objeto `timerActivo` completo o `null`: `{ materiaMatriculadaId, origen: "timer"|"pomodoro", sesionInicio, inicioFase, pomodoro: null|{config, bloqueActual, fase}, metaAlarmaDisparada }`. `fase` es `"trabajo"|"descanso_corto"|"descanso_largo"`.
* `segundosTranscurridos()` - segundos transcurridos de la **fase actual** (para timer simple es toda la sesión, igual que en Parte 1; para Pomodoro es el avance del bloque/descanso en curso - cambio de comportamiento respecto a Parte 1, que medía siempre la sesión completa). **(2026-09-19)** sigue devolviendo el transcurrido SIN topar; para pintar usar `tiempoDeFase()`.
* `tiempoDeFase()` - **(2026-09-19, nuevo)** `{ transcurridos, extra, duracion, completa }` de la fase actual, en segundos. `transcurridos` está **topado a la duración configurada** (el principal llega a 40:00 y se queda), `extra` es lo que se pasó, `completa` = la fase ya cumplió su tiempo. En timer simple: `extra` 0 y `completa` false (no hay duración fija). Respeta la pausa. Lo consumen la pantalla de detalle y el indicador.
* `iniciarDescansoPomodoro()` - **(2026-09-19, nuevo)** botón "☕ Descanso": cierra el bloque de trabajo y arranca el descanso (corto, o largo si era el último bloque). Guarda la sesión de `inicioFase` hasta ahora, **incluido el tiempo extra**; si estaba pausado, fin = instante de la pausa y el descanso arranca corriendo. Solo procede si el bloque ya cumplió su duración (`false` si no; para cortar antes está "Detener sesión"). Es lo único que saca a un bloque de la fase de trabajo.
* `iniciarTimerEstudio(materiaMatriculadaId)` - arranca un timer; `false` sin hacer nada si ya hay uno corriendo (de cualquier materia). Resuelve internamente si la materia tiene Pomodoro configurado (`mm.tiempo_estudio.pomodoro`) buscando la `mm` en `estado.datos.semestres` - la UI no necesita pasar esa config. Si arranca en modo Pomodoro, pide permiso de `Notification` si todavía está en `"default"` (aprovechando que el click que dispara esto ya es el gesto de usuario que el navegador exige).
* `detenerTimerEstudio()` - detiene el timer activo. Solo crea y guarda una `sesion_estudio` si la fase en curso "cuenta" como estudio real (timer simple siempre; Pomodoro solo si estaba en fase de trabajo) - detener a mitad de un descanso no genera sesión, igual que si ese descanso hubiera terminado solo. Devuelve la sesión creada o `null`.
* `cambiarTimerEstudio(materiaMatriculadaIdNueva)` - detiene el timer actual (guardando lo que corresponda) y arranca uno nuevo para la materia indicada; usado por el diálogo "ya hay un timer activo en \<Materia X\>, ¿querés cambiar?".
* `suscribirseATimer(cb)` - se suscribe a cada tick (1/seg mientras hay timer activo, más un valor inmediato al suscribirse); devuelve función para darse de baja. Usado por el indicador persistente y la pantalla de detalle - un solo `setInterval` compartido, nunca 2+ en paralelo por tener 2 pantallas abiertas.
* `formatearDuracion(segundosTotales)` - `"MM:SS"` si dura menos de una hora, `"H:MM:SS"` si dura una hora o más.
* `revisarSesionOlvidadaAlAbrir()` - **(Parte 2; REESCRITA 2026-09-17, era la causa del bug de pérdida de tiempo)** se llama una sola vez al arrancar la app (`inicializarTiempoEstudio()`). Lee el snapshot local (`localStorage`, clave `te_timer_activo_v1`, NUNCA sincronizado a Drive) y **restaura el timer** con `restaurarTimerDesdeSnapshot()`. Si la fase venía corriendo hace `SALVAVIDAS_HORAS_LIMITE` (3 h) o más, lo restaura CONGELADO (pausado en el instante de reapertura, para que `tick()` no avance 40 fases de Pomodoro de golpe creando sesiones falsas) y abre el modal de corrección, ahora con 3 salidas: "Seguir contando" (no toca nada, el timer ya está vivo), "Guardar y detener" (con la duración real **precargada**, no 0h 0m), y "Descartar" (con confirmación aparte). No se cierra al tocar fuera.

  **Bug que arreglaba (reporte: "inicié a las 2pm, detuve a las 10pm, estuve 2 h afuera de la app, contó solo 3 h"):** el snapshot se guardaba bien pero NUNCA se leía para volver a levantar el timer en memoria - no existía la operación inversa en ningún lado. Los 3 caminos de esta función terminaban en `localStorage.removeItem()`: fase de menos de 3 h → se borraba en silencio (recarga de pestaña, o el navegador móvil descartando la pestaña por memoria) y la sesión desaparecía entera; fase de 3+ h → modal precargado en 0h 0m, sin restaurar el timer, y tocar fuera del modal también descartaba; descanso de Pomodoro → descartado en silencio. **Ojo con la expectativa del reporte:** bajo el modelo timestamp inicio/fin (el diseñado, y el que exige el punto 1.2) estar fuera de la app NO descuenta - 2pm→10pm son 8 h, no 6. Lo que había que arreglar era la pérdida total, no el descuento.
* `restaurarTimerDesdeSnapshot(snapshot, { congelarAhora })` *(interna, 2026-09-17)* - vuelve a poner en memoria el timer tal cual estaba (mismos `sesionInicio`/`inicioFase`/fase/pausa), reengancha el intervalo y notifica a los suscriptores. `congelarAhora` lo deja pausado en el instante actual, ver arriba.
* `revisarFelicitacionMeta(materiaMatriculadaId)` - **(Parte 3)** revisa si la materia cruzó su meta semanal (sumando sus sesiones YA guardadas de la semana) y, si es así y todavía no se felicitó por la semana actual (`mm.tiempo_estudio.ultima_semana_felicitada`, epoch ms del lunes de la semana ya felicitada), muestra un overlay de felicitación (🎉, auto-cierra a los 3.5s o al tocar) y sella el campo para que no se repita en cada apertura de la app esa misma semana. Se llama SIEMPRE después de guardar la sesión que podría haber cruzado el umbral (nunca antes): desde `detenerTimerEstudio`, `iniciarDescansoPomodoro`, `abrirAvisoSesionOlvidada`, y desde `tiempo-estudio-registro.js` tras un registro manual o una edición de sesión.
* `notificarSesionesEstudioActualizadas()` - **(2026-09-19, nuevo)** dispara `te:sesiones-actualizadas` en `window`; único aviso de "cambió `sesiones_estudio` o el timer dejó de aportar su tramo en curso". Se llama DESPUÉS de dejar el estado final (timer ya vaciado / ya en descanso), nunca entre el `push` de la sesión y el cambio del timer. La llaman `detenerTimerEstudio`, `iniciarDescansoPomodoro`, las 2 salidas del salvavidas ("Guardar y detener" y "Descartar") y `tiempo-estudio-registro.js` (registro manual, editar, borrar). Vive acá (no en estadísticas) porque registro ya importa de este archivo. Estadísticas la re-exporta.
* `saltarDescansoPomodoro()` - **(2026-09-17, punto 1.4)** corta el descanso en curso y vuelve YA al bloque de trabajo siguiente. Hace exactamente el conteo de bloques y el reinicio del ciclo tras un descanso largo, solo que sin esperar a que venza el reloj. Si estaba pausado a mitad del descanso, se reanuda solo (corriendo `sesionInicio` como hace `reanudarTimerEstudio`). Nunca genera sesión de estudio: un descanso no suma, se salte o no. `false` sin hacer nada si no hay timer, si no es Pomodoro, o si ya está en fase de trabajo. Lo consume el botón "⏭ Saltar descanso" de `construirPantallaDetalle` (tiempo-estudio.js), visible SOLO durante un descanso de esa misma materia. **(2026-09-19)** Desde que el descanso ya no termina solo, es TAMBIÉN el "Terminar descanso": la única salida de un descanso cumplido (toast "Descanso terminado" en vez de "salteado").
* `pausarTimerEstudio()` / `reanudarTimerEstudio()` - **(2026-09-07, pedido explícito: "botón tipo play/pause y botón de detener sesión aparte")** pausan/reanudan el timer activo (simple o Pomodoro) SIN cerrar la sesión - eso lo sigue haciendo únicamente `detenerTimerEstudio`. Mecánica: al pausar se guarda `msPausaInicio`; al reanudar, `inicioFase` y `sesionInicio` se corren hacia adelante exactamente la duración de la pausa, así ningún otro cálculo del archivo (tick, `revisarMetaSimple`, `iniciarDescansoPomodoro`, `detenerTimerEstudio`, el salvavidas) necesita saber que existió una pausa - es como si el tiempo pausado nunca hubiera pasado. Mientras `pausado === true`, `tick()` no avanza fases de Pomodoro ni revisa la meta simple, y `segundosTranscurridos()` se congela en el instante de la pausa en vez de seguir con `Date.now()`. `timerEstaPausado()` - helper de lectura del estado de pausa del timer activo.

Notas internas de implementación (no exportadas, pero relevantes para quien toque este archivo después):
* El timer activo se persiste en `localStorage` (no en `estado.datos`) porque es puramente local al dispositivo - igual que `CLAVE_SIDEBAR_COLAPSADA` en `ui/componentes.js` o `CLAVE_FILTRO_VISTA_TE` en `tiempo-estudio.js`. Es lo único que permite detectar, al reabrir la app, que quedó una sesión sin detener.
* Las alertas (`dispararAlerta`) combinan un beep generado con Web Audio (`OscillatorNode`, sin archivo de audio propio que mantener), un `mostrarToast()`, y una `Notification` local (nunca push/Worker). **Reforzadas 2026-09-17 (reporte: "el aviso de fin de bloque solo me funciona con la app adelante"), 4 cambios:** 1) el permiso de `Notification` se pide al iniciar CUALQUIER timer, no solo con Pomodoro - antes, quien nunca hubiera usado Pomodoro jamás podía recibir el aviso de meta en 2do plano; 2) la notificación se dispara si la pestaña está oculta **o simplemente sin foco** (`document.hasFocus()`), que es el caso real de "la ventana se ve pero estoy en otra app"; 3) se prioriza `registration.showNotification()` del Service Worker sobre `new Notification(...)`, porque **en Chrome de Android el constructor directo tira excepción** y en celular la notificación nunca llegaba aunque hubiera permiso (el `new Notification` quedó solo de fallback de escritorio); 4) `programarAlarmaFase()`, un `setTimeout` único apuntado al instante exacto de fin de fase que sobrevive mucho mejor al estrangulamiento de temporizadores en 2do plano que el `setInterval(tick, 1000)` (Chrome lo lleva a 1/minuto tras unos minutos ocultos) - se reprograma desde `guardarSnapshotLocal()`, que es el único punto por el que pasan los 6 cambios de estado del timer.
* **Limitación real de plataforma, documentada a propósito en vez de prometer algo que no se cumple:** todo lo anterior depende de que el proceso del navegador siga vivo. Mientras la pestaña esté nada más que en 2do plano, funciona. Si el SO **suspende o descarta** la pestaña (iOS/Safari bastante rápido al minimizar, Android bajo presión de memoria) no corre JS ninguno y ningún aviso local puede salir a tiempo - la única garantía sería Web Push con un backend que programe el envío, justo lo que se le sacó al Worker en 2026-08-25. Lo que sí está garantizado es que **no se pierde tiempo**: al volver, el listener de `visibilitychange` fuerza un `tick()` que procesa todas las fases vencidas.
* **Punto 1.2 (a), verificado en toda la app:** no existe ningún listener de `visibilitychange`/`blur`/`pagehide`/`beforeunload` que detenga una sesión activa, ni corte por inactividad. El único listener de visibilidad de la sección es el de catch-up descrito arriba, que nunca detiene ni descarta nada.
* `buscarMateriaMatriculada()` y `calcularMinutosEstaSemana()` duplican lógica equivalente que ya existe en `tiempo-estudio.js` - duplicado a propósito para no crear un import circular (`tiempo-estudio.js` ya importa de este archivo). Si el criterio de "inicio de semana" llega a cambiar (ej. día configurable), hay que actualizar los dos lugares a la vez.
* El ciclo de Pomodoro es indefinido: trabajo(1) → descanso corto → trabajo(2) → ... → trabajo(N) → descanso largo → trabajo(1) de nuevo, hasta que el usuario detiene el timer a mano - no se corta solo al completar `cantidad_bloques` una vez. **(2026-09-19)** Y cada cambio de fase es a toque de la persona (Descanso / Terminar descanso); nunca automático. El aviso de fin de fase (`avisarFinDeFaseSiCorresponde`, interna) suena una sola vez por fase y `avisoFaseDisparado` va en el snapshot para que recargar no lo repita; al restaurar una fase ya vencida NO suena aviso al reabrir.

### tiempo-estudio/tiempo-estudio-registro.js
Propósito: **(Parte 3, punto 1, nuevo)** modal de registro manual de un bloque de estudio ya pasado - materia (matriculada, semestre actual) + fecha + hora de inicio + duración (horas y minutos, en vez de hora de fin, para no lidiar con "¿cruzó medianoche?"). Al guardar crea una `sesion_estudio` con `origen: "manual"`, que cuenta EXACTAMENTE igual que una sesión en vivo para meta semanal y felicitación. Sin ninguna validación anti-trampa (fecha futura, duplicados, etc. se permiten tal cual) - pedido explícito de Wagner, uso entre amigos de confianza total; lo único que se valida es duración > 0.
Depende de: core/schema.js (`crearSesionEstudio`, `sellarTimestamp`), core/storage-sync.js, core/storage.js, ui/componentes.js (`abrirConfirmacion`, `mostrarToast`), tiempo-estudio/tiempo-estudio-timer.js (`revisarFelicitacionMeta`)
Exporta:
* `abrirModalRegistroManual(items, onGuardar)` - `items` es el mismo arreglo que ya arma `obtenerMateriasParaTiempoEstudio()` (se recibe por parámetro desde `tiempo-estudio.js`, no se recalcula acá, para no crear un import circular de 3 puntas entre este archivo, `tiempo-estudio.js` y `tiempo-estudio-timer.js`). Tras guardar, llama a `revisarFelicitacionMeta()` (mismo chequeo que dispara la felicitación desde el timer en vivo) y a `onGuardar()` sin argumentos para que el llamador re-renderice.
* `construirListaSesiones(cont, materiaMatriculadaId, color, refrescar)` - **(2026-09-07, pedido explícito)** historial de sesiones de ESA matrícula puntual (nunca las de otra repetición de la misma materia), más reciente primero, como tarjetas con franja del color de la materia. Cada tarjeta tiene botón editar (✏️, abre `abrirModalEditarSesion`) y borrar (🗑️, `eliminarSesion` con confirmación vía `abrirConfirmacion`). Se llama desde `construirPantallaDetalle` en `tiempo-estudio.js`, después de `construirEstadisticasMateria`.
* **2026-09-19 (Parte B) - cambios en este archivo:** nueva `construirTimestampLocalReg(fechaStr, horaStr)` *(interna)* - parsea a mano el `<input type="date">` + `<input type="time">` (ignora segundos si vienen) y devuelve `NaN` si algo no es válido; reemplaza a `new Date(`${fecha}T${hora}:00`)`, que con "09:30:00" daba Invalid Date y guardaba la sesión con `inicio = NaN`. El registro manual y `abrirModalEditarSesion` validan `Number.isFinite` antes de guardar (en la edición, con NaN la validación `fin <= inicio` se saltaba). El toast del registro manual ahora dice materia + duración + día ("✓ Registrada: 1 h 30 min de Cálculo · Lun 14 sep · 09:30. La ves en el detalle de la materia."). `construirListaSesiones` ordena tolerando `inicio` inválido (las inválidas van al final y `formatearFechaHoraReg` muestra "fecha inválida", para poder editarlas/borrarlas).
* **2026-09-19 (tiempo real):** los 3 puntos que escriben en `sesiones_estudio` (registro manual, `abrirModalEditarSesion`, `eliminarSesion`) llaman ahora a `notificarSesionesEstudioActualizadas()` (importada de `tiempo-estudio-timer.js`) justo después de `sincronizarHorasCompetencias()`, además del `refrescar`/`onGuardar` de siempre.
* **2026-09-22 (rediseño del registro manual, pedido explícito) - cambios en `abrirModalRegistroManual`:** (1) el `<select>` nativo de materia (se veía gris/roto sobre fondo oscuro) se reemplaza por tarjetas seleccionables de selección única - mismo patrón visual que `.agenda-semestre-tarjeta` (`design-system.css`), un solo click cambia cuál queda `.active`. (2) Nuevo pill-switch **Hora / Tiempo** que cambia qué campos se piden: modo **Hora** (default) pide hora de inicio + hora de fin y calcula la duración solo (comportamiento de siempre); modo **Tiempo** pide horas + minutos directos, con un checkbox opcional "Agregar hora de inicio" (si no se marca, usa la hora actual) - esa hora es puramente informativa, nunca deriva una hora de fin ni afecta la duración cargada. (3) El toast de confirmación pasó de un genérico "Sesión registrada" a nombrar materia + duración + día exactos, para que quede claro que se guardó aunque la tarjeta visible no cambie en nada (sesión fuera de la semana en curso, o materia sin meta).
* `abrirModalEditarSesion(sesion, refrescar)` *(interna, `tiempo-estudio-registro.js`)* - a diferencia del registro manual (que pide fecha única + duración), este modal pide fecha+hora de INICIO y fecha+hora de FIN por separado - es lo que permite editar una sesión que cruza medianoche o varios días (ej. "empezaste 10pm, terminaste 2am") sin ningún truco especial: `fin` simplemente cae en otro día calendario, la resta de timestamps no le importa. **FIX 2026-09-17 (relectura de entidad viva, punto 2.3):** ya no muta el `sesion` del closure directo - al guardar relee `estado.datos.sesiones_estudio.find(s => s.id === sesion.id)` y muta esa referencia fresca; si no la encuentra (borrada o editada en otro lado mientras el modal estaba abierto), avisa con toast y cierra sin guardar. Re-sella el timestamp (`sellarTimestamp`) y llama a `revisarFelicitacionMeta()` por si el ajuste cruzó la meta.
* `eliminarSesion(sesion, refrescar)` *(interna)* - confirma con `abrirConfirmacion`, luego saca la sesión del arreglo vivo y agrega su `id` a la tumba `estado.datos._eliminados_sesiones_estudio` (mismo patrón "Borrado = tumba" del resto de la app), antes de `marcarCambioPendiente()`. **Fix 2026-09-08 (bug real: "borrar una sesión no se mantenía guardado"):** la tumba estaba empujando `sesion.id` **pelado** en vez de un objeto `{ id, eliminadoEn }` - con un valor plano, `t.id` da `undefined` para toda entrada, así que `fusionarColeccion` (`storage-merge.js`) nunca reconocía el id real como borrado (su `Set` de eliminados quedaba lleno de `undefined`) y `fusionarTumbas` directamente descartaba la entrada entera (`t.id === undefined`). Resultado: en cuanto `marcarCambioPendiente()` disparaba el siguiente sync - que primero BAJA lo de Drive y funde antes de subir (`intentarSincronizar`, storage-sync.js) - la sesión, todavía viva del lado remoto, resucitaba en el fusionado. La fusión en sí (`fusionarColeccion`/`fusionarTumbas`) estaba bien escrita; el bug era solo la forma de la tumba acá. Ya usa `{ id: sesion.id, eliminadoEn: Date.now() }`, mismo patrón que `agenda.js` al borrar un evento. **2026-09-09:** ahora también llama `sincronizarHorasCompetencias()` (tiempo-estudio-competencias.js) después de `marcarCambioPendiente()`, junto con los otros 2 puntos de este archivo que tocan `sesiones_estudio` (registro manual y editar sesión) y los 3 de `tiempo-estudio-timer.js` - 6 en total, ver esa función para el porqué.

### tiempo-estudio/tiempo-estudio-competencias-visual.js
Propósito: **(2026-09-21, nuevo - rediseño `prototipo-competencias-v2.html`)** capa puramente visual de Competencias: avatares con foto de Google, filas, podio, tarjeta de competencia, hoja modal deslizable ("bottom sheet"), historial por semana y aviso de posición. No hace ninguna llamada de red ni lee `estado` - recibe datos ya resueltos y devuelve HTML/elementos DOM. Los archivos con lógica (`tiempo-estudio-competencias.js`, `-gestion.js`, `-celebracion.js`) lo importan; este no importa a ninguno de ellos, así que no crea ciclos. Los estilos se inyectan solos (un único `<style id="te-estilos-competencias-visual">`, guard por id, vía `asegurarEstilosCompetenciasVisual()`) y viven en el archivo hermano `tiempo-estudio-competencias-visual-estilos.js`; nada de esto toca `design-system.css`. Todo prefijado para no chocar con el resto de la app: clases `cp-…`, variables `--cp-…` (definidas en `.cp-scope`/`.cp-overlay`, nunca en `:root`), keyframes `cp-…`. Tarjeta y hojas usan su propia paleta oscura "de marca", independiente del tema de la app (con el fix de contraste de más abajo para modo claro); la tipografía se hereda (`font: inherit`).
Depende de: tiempo-estudio/tiempo-estudio-competencias-visual-estilos.js (`CSS_COMPETENCIAS_VISUAL`)
Exporta:
* `ICONO`, `COLOR_PALETA_DEFAULT` - constantes (set de iconos SVG inline usados en hojas/avisos; hex de la paleta violeta default de la app, usado para distinguir "color propio real" de "nunca tocó la paleta" - ver el fix de `obtenerMiColor()` en `tiempo-estudio-competencias.js`).
* `esc(valor)` - escape HTML mínimo (mismo criterio que el resto de la app).
* `fmtHoras(horas)` - horas con decimales (como las guarda el Worker) → texto "1 h 20 min" / "45 min" / "3 h".
* `normalizarFotoUrl(valor)` / `normalizarColorHex(valor)` - saneo antes de poner algo que viene de OTRA persona (foto de Google, color de paleta) en un `<img src>` o un estilo inline: solo https y largo razonable para la foto; solo `#rgb`/`#rrggbb`/`rgb()`/`rgba()` bien formados para el color. Única puerta por la que esos valores llegan a inline.
* `leerColorAcentoActual()` - lee el `--accent-1` computado de la app para pintar el color propio en el podio/tarjeta.
* `leerVistaPreferida()` / `guardarVistaPreferida(vista)` - persisten en `localStorage` (clave `te_comp_vista`) si cada tarjeta se ve como podio o como lista; por dispositivo, no sincroniza por Drive.
* `avatarHTML(persona, tam)` - HTML del avatar: foto de Google si `foto_url` es válida, si no inicial del apodo sobre un color estable derivado del apodo (o del color propio, si lo hay). Tras insertar HTML con avatares hay que llamar a `activarFallbackAvatares(raiz)` (los handlers de error de `<img>` no van inline, por la CSP).
* `activarFallbackAvatares(raiz)` - engancha el fallback a inicial en toda foto que falle al cargar dentro de `raiz` (URLs de Google que vencen, etc.).
* `asegurarEstilosCompetenciasVisual()` - inyecta `CSS_COMPETENCIAS_VISUAL` una sola vez (guard por id).
* `pintarTarjeta(tarjeta, o, cb)` - dibuja la tarjeta completa de una competencia (cabecera con selector podio/lista + trofeo Historial + engranaje Gestionar, y el cuerpo en el modo elegido) dentro del elemento `tarjeta` ya insertado en el DOM. `o` trae nombre/esCreador/apodo/yoId/vista/participantes/estadoCuerpo ("cargando" o ya resuelto); `cb` trae los callbacks `alHistorial`/`alGestionar`/`alCambiarVista`. Llama a `ajustarContrasteCpScope()` al final (fix 2026-09-26, ver abajo).
* `abrirHoja({ icono, tono, titulo, subtitulo, cuerpoHTML })` - abre la hoja modal deslizable genérica (`.cp-overlay`) que usan Historial, Gestionar y el aviso de invitación; devuelve `{ overlay, hoja, cuerpo, cerrar }` (el nombre exacto de las claves puede variar levemente, ver el archivo). También llama a `ajustarContrasteCpScope()`.
* `pintarHistorial(hoja, podios, yoId, abrirEn)` - pinta el historial completo (una entrada desplegable por semana cerrada) dentro de la hoja que abrió `abrirHoja`; `abrirEn` (opcional) es la `semana_cerrada_en` a abrir de entrada, para que el aviso de resultado nuevo lleve directo a esa semana.
* `resumenPosicion(podio, yoId)` - texto corto de la propia posición en un podio ya cerrado ("Quedaste 2.º · a 1 h 20 min de Iva"), usado tanto en el aviso de resultado como en el historial.
* `construirAvisoPodio(datos, cb)` - dibuja el banner de "tenés un resultado nuevo" con medalla/posición (reemplaza al aviso de texto plano de antes del rediseño); lo consume `tiempo-estudio-celebracion.js`. También llama a `ajustarContrasteCpScope()`.
* **Internas relevantes (no exportadas) para quien edite este archivo:** `tonosDeColor(color)`/`varsDeColor(tonos)` calculan los tonos claro/oscuro derivados del color propio de cada persona y arman las variables CSS inline (`--cp-uc`, `--cp-uc-d`, …) que leen tanto el JS como el CSS del archivo hermano; `leerLuminosidadFondoHeader()`/`ajustarContrasteCpScope(el)` **(2026-09-26, fix "letra invisible en modo claro")** miden el color real de `--bg-header-solido` y, si es claro, fijan texto oscuro inline sobre el `.cp-scope`/`.cp-overlay` recién pintado (si es oscuro, no tocan nada - quedan los valores claros de siempre); ver la cabecera de `tiempo-estudio-competencias-visual-estilos.js` para el detalle completo de por qué hizo falta.

### tiempo-estudio/tiempo-estudio-competencias-visual-estilos.js
Propósito: **(2026-09-21, nuevo)** solo CSS del rediseño de Competencias, como un string exportado - se separó de `tiempo-estudio-competencias-visual.js` para respetar el límite de 800 líneas por archivo del proyecto. Lo inyecta `asegurarEstilosCompetenciasVisual()` (archivo hermano). Todo prefijado (`.cp-…`, `--cp-…`, `@keyframes cp-…`), variables solo en `.cp-scope`/`.cp-overlay`, nunca en `:root`. Generado a partir de `prototipo-competencias-v2.html`.
Depende de: nada (string puro, sin imports).
Exporta:
* `CSS_COMPETENCIAS_VISUAL` - el string de CSS completo.
Notas: **saga de bugs de color, 2026-09-22 a 2026-09-26**, todos ya resueltos - útil tenerlos presentes si algo del podio vuelve a salir "morado" para todos: (1) 2026-09-22, la rayita superior del pedestal quedaba oro/plata/bronce fijo aunque el pedestal ya estuviera teñido con el color propio - ahora usa `--cp-uc-d`; (2) 2026-09-22 (2), el halo de "esta posición sos vos" tenía el violeta de marca escrito directo en `rgba()` en vez de leer `--cp-uc` - por eso el anillo/fondo de "sos vos" salía violeta sin importar el color real de cada quien; (3) 2026-09-22 (3), el bug más extendido: la TARJETA en sí (fondo, aro detrás de avatares, selector de vista, chip de acento del modal, tarjeta de Gestionar, botón "Copiar link"...) tenía el violeta de marca escrito en un montón de reglas sueltas en vez de leer `--accent-1`/`--accent-2` de la paleta de cada usuario; (4) 2026-09-23, el destacado del 1er lugar en la tarjeta y en el aviso "¡Ganaste la semana!" tenían el mismo problema puntual; (5) 2026-09-24/25/26, tres intentos seguidos de "adaptar al modo claro" (el segundo intento, fijar el fondo, había quedado MAL) hasta confirmar que el fondo (`--cp-bg`) ya se adapta bien solo y que lo que faltaba de verdad era el contraste del TEXTO - resuelto del lado del JS con `ajustarContrasteCpScope()` (ver `tiempo-estudio-competencias-visual.js`), no tocando más este archivo.

### tiempo-estudio/tiempo-estudio-competencias.js
Propósito: **(2026-09-09, "Competencias" Parte 1+2 - contrato reescrito de cero contra el Worker real)** una "competencia" es un grupo de horas-de-estudio TOTALES (todas las materias juntas) compartido entre varias personas - a diferencia de TODO lo demás en la app, no vive en el Drive de cada usuario: vive del lado del Worker (`worker-notificaciones-agenda`, mismo que ya usa `core/auth.js` para OAuth - ver `worker-notificaciones-agenda/src/index.js`, "Competencias, Parte 4/4 del spec de Tiempo de Estudio", con 3 tablas D1 nuevas - `competencias`, `participantes`, `historial_ganadores` - y un cron por hora que cierra la semana de cada una anclado a la hora local de su creador). Lo único que sincroniza por Drive (como el resto de `estado.datos`) es la lista LOCAL de "a qué competencias estoy unido" (`estado.datos.competencias_unidas`, ver core/schema.js) - un puntero por competencia (id + mi apodo ahí + mi participante_id), no el contenido de la competencia en sí (eso vive 100% en D1 y se pide siempre fresco por HTTP).

**⚠️ Corrección 2026-09-09 sobre la ronda anterior:** la primera versión de este archivo había ADIVINADO un contrato (`POST /competencias/crear`, `Authorization: Bearer <token>`, camelCase) antes de que el Worker existiera de verdad. Al subir el código real del Worker, el contrato correcto resultó bastante distinto - reescrito de cero:
- **Sin `Authorization: Bearer`** - el Worker no valida el access_token de Google para nada, solo el header `Origin` (CORS gate contra `https://waxyflounder971.github.io`). La "identidad" para todo es `identificador_usuario` (el correo de Google, `estado.datos.perfil.correo`) viajando en el BODY de cada request - decisión explícita del dueño del proyecto ("entre amigos, no es un riesgo"). Ningún endpoint GET lo devuelve de vuelta.
- Rutas y campos en **snake_case** tal cual la tabla D1: `POST /competencias` (no `/competencias/crear`), `token_creador`, `participante_id`, `horas_semana_actual`, `offset_minutos_utc`.
- `POST /competencias/:id/unirse` NO devuelve `nombre` en la respuesta - hay que pedirlo aparte con un `GET /competencias/:id` (se hace automático dentro de `abrirModalUnirseCompetencia`, antes de cerrar el modal).
- `offset_minutos_utc` ancla el corte semanal a la hora LOCAL del creador (no de cada participante - evita ambigüedad entre husos horarios distintos). El cliente lo calcula con `obtenerOffsetMinutosUtc()` (signo invertido respecto a `Date.prototype.getTimezoneOffset()` de JS, que usa la convención opuesta) y lo manda tanto al crear como al unirse (cada participante guarda el suyo en D1, pero solo el del creador decide cuándo cierra la semana).

Depende de: core/storage.js, core/schema.js (`sellarTimestamp`), core/storage-sync.js (`marcarCambioPendiente`, `intentarSincronizar`), core/auth.js (`URL_WORKER_OAUTH`), ui/componentes.js (`mostrarToast`, `abrirConfirmacion`), core/clipboard.js (`copiarAlPortapapelesBlindado`, `abrirModalCopiaManualPortapapeles`), tiempo-estudio/tiempo-estudio-estadisticas.js (`calcularMinutosTotalesEnRango`, `obtenerRangoSemana`), tiempo-estudio/tiempo-estudio-competencias-gestion.js (`abrirModalGestionCompetencia`, `construirRegistroCompetencias` - import circular intencional), tiempo-estudio/tiempo-estudio-celebracion.js (`construirAvisosResultados`, `construirBotonesSimulacion`, `mostrarAnimacionUnirseCompetencia`), tiempo-estudio/tiempo-estudio-competencias-visual.js (`esc`, `normalizarFotoUrl`, `normalizarColorHex`, `leerColorAcentoActual`, `leerVistaPreferida`, `guardarVistaPreferida`, `pintarTarjeta`, `abrirHoja`, `pintarHistorial`, `avatarHTML`, `activarFallbackAvatares`, `asegurarEstilosCompetenciasVisual`, `COLOR_PALETA_DEFAULT` - **2026-09-21, nuevo**, ver esa sección: todo lo que se dibuja del rediseño vive ahí, acá queda la lógica de red/estado/permisos)
Exporta:
* `construirVistaCompetencias(cont, refrescar)` - punto de entrada, llamado desde `tiempo-estudio.js` cuando el pill superior está en "Competencias". **2026-09-21 (rediseño):** cada tarjeta ahora la dibuja el módulo visual (`pintarTarjeta`) - marcador con podio O lista (selector, se recuerda por dispositivo), avatares con foto de Google, dos íconos en la cabecera (🏆 Historial, ⚙️ Gestionar). `cargarMarcadorEnTarjeta` *(interna)* la pinta primero "cargando" y luego con los datos de `GET /competencias/:id`, sin bloquear el render de toda la lista esperando a las N competencias a la vez. **2026-09-26:** las piezas (encabezado, avisos, lista, registro) cuelgan de un `envoltorio` propio en vez de directo de `cont`, para controlar el espaciado interno sin heredar el gap de la clase compartida "stack". Botón "Enlace" suelto y "Borrar"/"Salir" sueltos por tarjeta: **ya no existen** desde el rediseño - viven dentro del menú ⚙️ Gestionar.
* `revisarLinkInvitacionAlCargar(refrescar)` - **(2026-09-21, nuevo)** invitación por deep link: si la URL trae `?comp=<id>` (el mismo link que arma `construirLinkInvitacion`), lo saca con `history.replaceState` (así un F5 posterior no reabre nada ni reintenta unirse solo) y abre `abrirModalInvitacionRecibida(id, refrescar)` *(interna)* - trae nombre + marcador con `GET /competencias/:id` ANTES de decidir, y solo pide el apodo para unirse (`POST /unirse` de siempre). Si ya es parte de esa competencia, solo avisa con un toast. Debe llamarse UNA sola vez desde el `DOMContentLoaded` de `main.js`, después de que `estado` termine de cargar (usa `estado.datos.perfil.correo` y `competencias_unidas`) - **En v3.20.0**, `main.js` llama esta función después de cargar el estado, de modo que el deep link se procesa al abrir la app. Al unirse (por acá o por `abrirModalUnirseCompetencia`) dispara `mostrarAnimacionUnirseCompetencia()` de `tiempo-estudio-celebracion.js`.
* `construirLinkInvitacion(competenciaId)` / `copiarLinkInvitacion(competencia)` - **(exportados desde el rediseño 2026-09-21, ya existían internos)** arman el link `?comp=<id>` y lo copian con `copiarAlPortapapelesBlindado`/`abrirModalCopiaManualPortapapeles`; el botón que los dispara vive ahora dentro del menú Gestionar (`tiempo-estudio-competencias-gestion.js`), de ahí que salieran a la lista de exports.
* `abrirModalHistorial(competencia, abrirEn)` - **(2026-09-21, cambia de contrato)** el trofeo de la tarjeta ahora trae el PODIO COMPLETO de cada semana cerrada vía `GET /competencias/:id/podios` (antes: solo el ganador, `GET /historial`) y lo pinta con `pintarHistorial` en hojas desplegables. `abrirEn` (opcional, nuevo) = `semana_cerrada_en` a desplegar de entrada, para que el aviso de resultado nuevo (`construirAvisosResultados`) abra directo la semana que anuncia.
* `obtenerMiColor()` *(interna)* - **fix 2026-09-22:** antes mandaba siempre el `--accent-1` computado, incluso con la paleta default de la app sin tocar - alguien que nunca eligió un color de competencia terminaba con el podio pintado de violeta igual, indistinguible de quien sí configuró ese tono a propósito. Ahora la paleta default se trata como "sin color propio" (`null`, mismo camino que ya usaba `tonosDeColor()` para oro/plata/bronce) - el violeta de marca solo se ve cuando alguien de verdad la eligió entre las otras 12 paletas o la personalizada.
* **2026-09-23:** los 2 botones de simulación de resultado (`construirBotonesSimulacion`, de `tiempo-estudio-celebracion.js`) dejan de mostrarse por defecto en `construirVistaCompetencias` - quedan detrás de un easter egg (`activarLongPress` sobre el título "Competencias", 5s).
* **2026-09-17 (Partes 3/4/5) - cambios en este archivo:** `construirVistaCompetencias` ahora pinta, en este orden, los avisos de resultado nuevo (`construirAvisosResultados`), los 2 botones temporales de prueba (`construirBotonesSimulacion`), la lista de competencias **activas**, y al final la sección "Registro de competencias" (`construirRegistroCompetencias`) con las finalizadas. `construirFilaBotonesCompetencia` pasó de 4/3 botones a **3 fijos - Enlace / Clasificación / Gestionar**: "Borrar" y "Salir" dejaron de ser botones sueltos y viven adentro del menú Gestionar (punto 4.1), lo que permitió borrar toda la gimnasia de medición con `scrollWidth`/`ResizeObserver` y los modos `data-cols="2x2"`/`data-compacto` del CSS. `construirCajaModal` perdió su parámetro `bloquearClickAfuera` (ahora TODOS bloquean, pedido 2.2) y gana una "✕" propia en la esquina con un `MutationObserver` que la re-inserta cuando el modal repinta su contenido tras un fetch - hacía falta porque varios de estos modales tienen estados "Cargando…" que, sin clic afuera, quedarían sin ninguna salida (los modales fijos de index.html ya la tienen vía `inicializarBotonesCerrarModal()` de ui/componentes.js, que a estos no los alcanza por ser dinámicos). `cargarMarcadorEnTarjeta` además **reconcilia el caché local** de `nombre`/`estado` contra lo que devuelve el Worker y re-renderiza una sola vez si algo cambió (así se entera de un renombre o una finalización hecha desde otro dispositivo). Las 3 altas locales de `competencias_unidas` siembran `estado: "activa"`.
* **Exports de uso interno del grupo, no API de la sección** (los consume `tiempo-estudio-competencias-gestion.js`): `construirCajaModal`, `fetchConTimeout`, `formatearHoras`, `leerTokenCreador`, `salirDeCompetencia`, `borrarCompetenciaEntera`, más `construirLinkInvitacion`/`copiarLinkInvitacion`/`abrirModalHistorial` (agregados en el rediseño 2026-09-21, ver arriba). Import circular intencional con ese archivo (allá se importa `abrirModalGestionCompetencia`/`construirRegistroCompetencias` de vuelta), seguro porque ningún nombre importado se usa en el nivel superior.
* `sincronizarHorasCompetencias()` - envía a CADA competencia unida el total de horas estudiadas ESTA semana (`calcularMinutosTotalesEnRango` + `obtenerRangoSemana(0)`, TODAS las materias sumadas - una competencia es sobre el total, no una materia puntual). Best-effort a propósito (mismo criterio que documenta el propio Worker en `manejarActualizarHoras`): nunca bloquea ni muestra error si falla, y manda el TOTAL recalculado (no un delta), así que un fallo de red puntual se autocorrige solo con la próxima sesión guardada. Se llama desde 6 puntos: `tiempo-estudio-timer.js` (bloque de trabajo Pomodoro completado, `detenerTimerEstudio`, salvavidas de sesión olvidada) y `tiempo-estudio-registro.js` (registro manual, editar sesión, borrar sesión) - siempre justo después de `marcarCambioPendiente()`/`revisarFelicitacionMeta()` en cada uno. **2026-09-17:** filtra las competencias con `estado === "finalizada"` antes de mandar nada (el Worker igual las ignora, pero así se evitan N requests inútiles por cada sesión guardada).
* **2026-09-19 (Parte A, estado fantasma) - cambios en este archivo:** (1) las 3 altas (`abrirModalCrearCompetencia`, `abrirModalUnirseCompetencia`, `abrirModalInvitacionRecibida`) sellan `unido_en: Date.now()` en la entrada de `competencias_unidas` - es lo que permite a `podarTumbasSuperadasPorAltas` (storage-merge.js) distinguir una membresía nueva de una tumba vieja con el mismo `id`. (2) `ejecutarSalidaCompetencia` *(interna)* ahora **devuelve `true`/`false`** y solo saca la competencia de la lista local (y deja tumba) si el Worker confirmó (2xx o 404); ante red caída, timeout o 5xx deja todo como estaba y avisa con toast - antes se tragaba el error y dejaba una fila fantasma en D1. (3) En `abrirModalDelegarAntesDeSalir`, si la delegación funcionó pero la salida falló, se marca `es_creador = false` en la entrada viva (relectura por id + `sellarTimestamp`), porque el token ya se limpió y sin esto el reintento chocaba con "no se encontró el permiso de borrado". (4) Los handlers de 409 en unirse quedan por compatibilidad, pero con el Worker nuevo el 409 ya no ocurre para la misma persona.

### tiempo-estudio/tiempo-estudio-celebracion.js
Propósito: **(2026-09-17, Parte 3 - nuevo)** las celebraciones de resultado de una competencia: el **aviso** discreto, la **pantalla** completa con confeti/animación, y el **audio** con carga segura. El orden de disparo es lo importante del diseño: **nada suena automáticamente al abrir la app** (punto 3.3). Un resultado nuevo deja primero un banner ("Tenés un resultado nuevo de \<competencia\>, tocá para verlo - trae sonido") y recién al tocarlo se abre el overlay con confeti y sonido. Ese toque cumple además el gesto de usuario que los navegadores exigen para poder reproducir audio, así que el aviso resuelve las dos cosas a la vez.

Cómo detecta un resultado real: el Worker cierra la semana solo (cron por hora) y deja una fila en `historial_ganadores`; este archivo pide `GET /competencias/:id/historial`, mira la entrada más reciente y la compara contra el último `semana_cerrada_en` que ESTE dispositivo ya mostró (`localStorage`, clave `te_comp_resultado_visto_<id>`). Si avanzó, hay resultado nuevo: **victoria** si el `participante_id` ganador es el propio, **derrota** si no. El "ya lo vi" es local al dispositivo a propósito (mismo criterio que el snapshot del timer): que avise en el celular y en la notebook es lo esperable. La primera vez que un dispositivo mira una competencia que YA tiene historial viejo, se sella sin avisar - si no, estrenar teléfono dispararía una celebración de una semana de hace un mes.

**2026-09-21 (rediseño, `prototipo-competencias-v2.html`):** el aviso ya no es un banner de texto plano - ahora lo dibuja `construirAvisoPodio` del módulo visual, con medalla y posición ("Quedaste 2.º · a 1 h 20 min de Iva"). Para saber el puesto se pide primero `GET /competencias/:id/podios` (podio completo de la semana); si el Worker no lo tiene o no hay datos de este usuario en esa semana, cae al aviso de siempre armado con `/historial`. La pantalla de celebración a pantalla completa NO cambió de diseño; gana un segundo parámetro opcional `alCerrar` que, si se pasa, se llama al tocar "Listo" (lo usa el aviso para poder abrir el podio de esa semana justo después de cerrar la celebración).
Depende de: core/storage.js, core/auth.js (`URL_WORKER_OAUTH`), tiempo-estudio/tiempo-estudio-competencias-visual.js (`construirAvisoPodio`, `resumenPosicion`, `avatarHTML`, `activarFallbackAvatares`, `asegurarEstilosCompetenciasVisual` - **2026-09-21, nuevo**, sin ciclos: ese módulo no importa nada de este archivo). Duplica `fetchConTimeout` (10 líneas) en vez de importarlo de tiempo-estudio-competencias.js, para no crear un ciclo - ese archivo sí importa de este.
Exporta:
* `mostrarCelebracionResultado(resultado, alCerrar)` - overlay a pantalla completa. `resultado` = `{ tipo: "victoria"|"derrota", nombreCompetencia, apodoGanador, horas, esPrueba }`. Confeti en `<canvas>` sin ninguna librería (mismo criterio que las gráficas SVG a mano de Estadísticas): victoria = cañones que disparan piezas con rotación (`lanzarCanones`); derrota = lluvia lenta y apagada que no para (`lanzarLluvia`). Se le suma la animación de entrada de la tarjeta por CSS y el emoji con latido. Al cerrar ("Listo", único botón - no se cierra tocando el fondo) se corta el `requestAnimationFrame`, el audio, y se llama `alCerrar()` si se pasó **(parámetro nuevo, 2026-09-21)**.
* `construirAvisosResultados(cont, refrescar, abrirHistorial)` - dibuja los avisos pendientes arriba de la lista de competencias, ahora con el diseño de podio (`construirAvisoPodio`) - `abrirHistorial` **(parámetro nuevo, 2026-09-21)** es el callback que abre el historial de esa competencia en esa semana puntual (`tiempo-estudio-competencias.js` le pasa `abrirModalHistorial`). Los de prueba se pintan sincrónicos (ya están en memoria); los reales se agregan después, sin bloquear el render, primero probando `/podios` y cayendo a `/historial` si hace falta (`detectarResultadoNuevo`, interna). Si el Worker no responde, no hay aviso y listo - no se le muestra ningún error al usuario por esto.
* `construirBotonesSimulacion(cont, refrescar)` - **BOTONES TEMPORALES DE PRUEBA** (punto 3.1), marcados como tales de 3 formas para que nadie los confunda con funcionalidad real: estilo *outline* punteado naranja propio (no usa `.btn` del resto de la app), la etiqueta "(prueba)" en el texto, y el comentario `// BOTÓN TEMPORAL DE PRUEBA - remover cuando el diseño de celebración esté aprobado` en cada punto del código. **Respetan el punto 3.3 igual que un resultado real**: no abren la celebración de una, encolan un aviso - así se prueba el flujo completo (aviso + celebración), no solo el confeti. **Sigue existiendo tal cual**, pero desde el 2026-09-23 `construirVistaCompetencias` ya no la llama por defecto - queda detrás del easter egg del long-press en el título "Competencias" (ver la sección de `tiempo-estudio-competencias.js`); el interruptor interno `MOSTRAR_BOTONES_PRUEBA` sigue en `true`.
* `simularResultado(tipo, nombreCompetencia)` - encola el resultado falso (ahora con un podio de prueba inventado, `podioDePrueba`, para poder probar el diseño con medalla/posición). La cola (`resultadosDePrueba`) vive en memoria a nivel de módulo, no en `localStorage` ni en `estado.datos`: sobrevive a un re-render de la sección pero se va sola al recargar la página, que es lo que se quiere de algo temporal.
* `marcarResultadoVisto(competenciaId, semanaCerradaEn)` - sella el "ya lo vi" de ese dispositivo.
* `mostrarAnimacionUnirseCompetencia(datos)` - **(2026-09-22, nuevo, pedido "algo que motive" al unirse)** pantalla corta de "¡Sumado al equipo!" con chips de avatar de quienes ya estaban en la competencia entrando uno a uno (`avatarHTML`) y el propio cerrando la fila. `datos` = `{ nombreCompetencia, apodoPropio, miFoto, miColor, participantes }` (participantes = la lista cruda de `GET /competencias/:id`). Se dispara desde los 2 puntos reales donde alguien se une en `tiempo-estudio-competencias.js` (`abrirModalUnirseCompetencia` y `abrirModalInvitacionRecibida`, deep link), justo después del `POST /unirse` exitoso - a propósito NO al CREAR una competencia (`abrirModalCrearCompetencia`, que sigue con su toast + copiar invitación de siempre, porque ahí no hay ningún equipo al que sumarse todavía). **Sin confeti a propósito:** el cañón de `lanzarConfeti` está pensado para durar toda la pantalla de resultado (guion de tiempos largo) y reusarlo acá cortado a mitad de guion causaba que, si el tab perdía foco justo en el corte, el canvas quedara congelado en el último cuadro al volver.
* `reproducirAudioSeguro(ruta)` *(interna)* - **punto 3.2**. Intenta `audio/ganador.mp3` (victoria) o `audio/perdedor.mp3` (derrota) y devuelve SIEMPRE un objeto con `detener()`, aunque no haya sonado nada. Se traga los 3 modos de falla posibles -archivo inexistente (404), formato no soportado, autoplay bloqueado- y en los 3 **la celebración se muestra igual, completa, sin sonido y sin ningún mensaje de error**. Cada caso deja un `console.warn` para diagnóstico y nada más, así que se puede desplegar sin los mp3 y agregarlos después sin tocar código (ver `audio/README.md`).

### tiempo-estudio/tiempo-estudio-indicador.js
Propósito: **(2026-09-19 - nuevo)** el indicador persistente de "hay una sesión de estudio corriendo": lucecita parpadeante del color de la materia + nombre sin código + tiempo (+ "En pausa"/"Descanso"). **DOM puro: no importa nada del resto de la app** - quién es la materia, su color y el texto del tiempo se los da `tiempo-estudio.js` por `resolverInfo`. Eso permite probarlo con el `design-system.css` real en un navegador sin levantar la app, y tolera un `index.html` viejo (los contenedores que no existan se ignoran).
Depende de: nada (solo `document`).
Exporta:
* `montarIndicadoresTimer({ elementos, resolverInfo, alTocar })` - recibe los 3 contenedores (`#timer-tarjeta-lateral`, `#badge-tiempo-estudio`, `#timer-linea-topbar`; los `null` se descartan; si no queda ninguno devuelve `null`), les arma por dentro la MISMA estructura (`.timer-luz` + `.timer-materia` + `.timer-sep` + `.timer-tiempo` + `.timer-estado`; lo que cambia entre presentaciones es solo el CSS) y engancha `alTocar` al clic. Devuelve `pintar(activo)` para pasarle a `suscribirseATimer`: con `null` oculta los 3 (clase `oculto`), con un timer activo llama a `resolverInfo(activo)` y pinta los 3. El tick de cada segundo solo cambia `textContent` y solo si cambió (nunca reconstruye nodos); `--te-color` y `aria-label` solo se reescriben cuando cambian. El color se valida como hex; cualquier otra cosa cae a `var(--accent-1)`.
* `esColorHex(valor)` - helper de validación (`#rgb` / `#rrggbb`).
Notas: el CSS (`design-system.css`, sección "Tiempo de Estudio", bloque `.timer-indicador*`) hace fondo y borde con `color-mix()` de `--te-color` contra `--bg-panel`, así que funciona en las 13 paletas y en claro/oscuro; en `[data-rendimiento="reducido"]` se apaga el blur y se pone un fondo opaco (mismo patrón que `.glass-panel`). `prefers-reduced-motion` ya apaga la animación de la luz por la regla global.

### tiempo-estudio/tiempo-estudio-competencias-gestion.js
Propósito: **(2026-09-17, Partes 4 y 5 - nuevo)** administrar una competencia (menú "Gestionar" con sus 6 acciones) y el archivo muerto ("Registro de competencias" finalizadas). Se separó de `tiempo-estudio-competencias.js` porque ese archivo ya estaba en ~1100 líneas, bastante por encima del límite de 800 del proyecto. El corte es por responsabilidad, no por tamaño arbitrario: allá queda **participar** (crear / unirse / marcador / historial / enviar horas), acá queda **administrar**.
Depende de: core/storage.js, core/schema.js (`sellarTimestamp`), core/storage-sync.js, core/auth.js (`URL_WORKER_OAUTH`), ui/componentes.js (`mostrarToast`, `abrirConfirmacion`), tiempo-estudio/tiempo-estudio-competencias.js (`construirCajaModal`, `fetchConTimeout`, `formatearHoras`, `leerTokenCreador`, `salirDeCompetencia`, `borrarCompetenciaEntera`) - import circular intencional, seguro porque nada se usa en el nivel superior del archivo.
Exporta:
* `abrirModalGestionCompetencia(competencia, refrescar)` - **(punto 4.1)** el menú único que reemplazó a los botones sueltos de "Borrar" y "Salir" en cada tarjeta. Cada opción cierra el menú y abre su propio sub-modal (o dispara la acción directo).
* `construirRegistroCompetencias(cont, refrescar)` - **(Parte 5; 2026-09-22 cambia de comportamiento)** sección "Registro de competencias", **colapsada por default** (`registroAbierto`, módulo-nivel, no persistida - mismo criterio que `vistaSeccionTE`). Una tarjeta chica por cada competencia `finalizada`, con solo su nombre. **Ahora SIEMPRE se dibuja** (antes: nada si no había ninguna finalizada localmente) porque, abierta, también trae el botón "🔄 Recuperar historial" - si dependiera de ya tener algo local, quien perdió el puntero nunca vería cómo recuperarlo. Sin finalizadas localmente muestra "No hay ninguna localmente." en vez de la lista.

No exportada, pero interna al módulo:
* `recuperarCompetenciasPerdidas(refrescar)` *(interna, 2026-09-22)* - llama a `GET /identificadores/:correo/competencias` (Worker, nuevo) con `estado.datos.perfil.correo`, y agrega a `competencias_unidas` cualquier competencia que el Worker devuelva y no esté ya localmente por `id` - **solo aditivo**, nunca pisa ni borra nada existente. Pensado para cuando el puntero local se pierde (tumba vieja de antes del fix de `podarTumbasSuperadasPorAltas`, dispositivo nuevo, bug de sync) y la competencia desaparece del Registro sin forma de volver a verla, porque todo el resto de endpoints pide un `id` puntual que el cliente ya no tiene. Las recuperadas entran con `es_creador: false` (el endpoint no expone `token_creador`). Sella cada alta con `sellarTimestamp` + `marcarCambioPendiente`, como el resto de mutaciones del archivo. **Trade-off aceptado:** no distingue "salí a propósito" de "se perdió el puntero" (el Worker no lo guarda), así que en teoría podría resucitar una competencia de la que alguien salió hace mucho - aceptable frente al costo de perder historial real.
* `opcionesGestion(competencia)` *(interna)* - **el único lugar donde se deciden los permisos (punto 4.2)**, para no tener el criterio repartido por 6 handlers. Solo el creador: Renombrar · Sacar usuario · Finalizar/Reactivar · Borrar. Cualquiera (incluido el creador): Cambiar mi apodo · Salir. Además filtra por estado (4.3): si la competencia está `finalizada`, "Finalizar" se reemplaza por **"Reactivar"** y "Sacar usuario" desaparece. "Creador" acá es `competencia.es_creador` + tener el `token_creador` en ESTE dispositivo; el Worker vuelve a validar el token en cada endpoint, así que esconder botones es comodidad, **nunca** la seguridad real.
* `abrirModalRenombrarCompetencia` / `abrirModalCambiarApodo` / `confirmarCambioDeEstado` + `cambiarEstadoCompetencia` / `abrirModalSacarUsuario` *(internas)* - los 4 flujos nuevos contra el Worker. Todos siguen el patrón de **relectura de entidad viva** antes de mutar (se vuelve a buscar la competencia por id en `estado.datos.competencias_unidas`, nunca se muta el objeto que venía del closure del modal) y sellan con `sellarTimestamp` + `marcarCambioPendiente`. "Sacar usuario" reusa el endpoint de "salir" (`DELETE .../participantes/:pid`) mandando `token_creador` en vez de `identificador_usuario`, en lugar de abrir una ruta nueva que borre participantes.
* `abrirModalCompetenciaFinalizada(competencia, refrescar)` *(interna)* - la ficha de una competencia archivada, en **un solo viaje** (`GET /competencias/:id/resumen`): todas las semanas que tuvo con su ganador (ordenadas de la primera a la última, que es cómo se lee una historia - el Worker las devuelve DESC), y abajo el resumen general armado con `construirHtmlResumenGeneral`: semanas jugadas, quién ganó más (con empate contemplado), horas totales de TODOS los participantes en toda la vida de la competencia, la mejor semana de la historia, cantidad de participantes y cuánto duró.

### tiempo-estudio/tiempo-estudio-estadisticas.js
Propósito: **(Parte 3, punto 2, nuevo)** vista de Estadísticas - 2 visualizaciones en SVG a mano, mismo lenguaje visual que `finanzas/finanzas-graficas.js` (se miró ese archivo como referencia de estilo antes de escribir esto, nunca se importa directo - su lógica es 100% específica de Finanzas): 1) donut "Horas por proyecto" (= por materia) del corte elegido (Día con selector de fecha puntual / Semana con navegador `‹ ›` / Semestre con navegador `‹ ›` sobre TODOS los semestres del historial, no solo los actuales) + lista debajo con una barra por materia normalizada contra la de más minutos, horas ancladas a la derecha; 2) gráfica de barras de tendencia (pill propio Semana/Semestre, sin Día) - en semana, eje X = los 7 días de esa semana; en semestre, eje X = los meses de ese semestre - valor agregado de TODAS las materias juntas (a diferencia del donut, acá no se separa por materia).

A propósito NO restringido a `obtenerSemestresActuales()`: una sesión vieja de un semestre que ya no es "actual" tiene que poder seguir viéndose acá (mismo criterio que `finanzas-graficas.js`) - `resolverInfoMateria()` y el navegador de semestre recorren `estado.datos.semestres` completo.
Depende de: core/storage.js, core/utils.js (`aplicarFormatoTexto`), core/schema.js (`COLOR_TIEMPO_ESTUDIO_DEFAULT`), agenda/agenda-clases.js (`calcularNumeroSemanaParaFecha`, 2026-09-08), config/config-ajustes.js (`DIAS_SEMANA_CONFIG`, 2026-09-09)
Exporta:
* `construirVistaEstadisticas(cont, refrescar)` - punto de entrada, llamado desde `tiempo-estudio.js` cuando el pill superior está en "Estadísticas". `refrescar` es un callback sin argumentos (`renderizarTiempoEstudio` del archivo que llama) que se dispara ante cualquier cambio de pill/navegador - este archivo no re-renderiza su propio contenido en aislado, deja que el padre reconstruya toda la sección (mismo patrón que ya usaba el filtro Todo/Activos).
* **2026-09-19 (Parte C) - `construirSeccionHorasTotales(cont)`** *(interna)* - primera sección de `construirVistaEstadisticas` (aislada con try/catch como las demás): 4 tarjetitas en grid 2x2, mismo estilo que `construirSeccionResumenFinal` (etiqueta muted + valor `strong` + una línea de detalle: "Mié 16 Sep", "14 Sep – 20 Sep", "Sep 2026", "2026"). Hoy / Esta semana (lunes-domingo) / Este mes / Este año calendario, TODAS las materias sumadas (incluye sesiones de materias borradas o semestres pasados, igual que Tendencia). Cada sesión cuenta entera en el período donde EMPIEZA (`inicio`, hora local).
* `calcularHorasTotalesPeriodos(ahora = new Date())` - **(nueva, exportada)** devuelve `{ hoy, semana, mes, anio }`, cada uno `{ minutos, inicio, fin }`. `ahora` existe para poder probarla. Usa `obtenerRangoDia`, `obtenerRangoSemana(0, ahora)`, `obtenerRangoMes`, `obtenerRangoAnio` (estas dos, nuevas e internas) y `calcularMinutosTotalesEnRango`.
* **2026-09-19 (semestre por semanas, color por materia, tiempo real) - cambios en este archivo:** `construirSeccionBarras` ("Tendencia") y `construirSeccionBarrasMateria` ("Horas estudiadas") en corte Semestre muestran S1…S-actual clasificando cada sesión con `calcularNumeroSemanaSegura` (envoltura de `calcularNumeroSemanaParaFecha`, nunca una ventana de fechas propia). "Tendencia" usa `construirGraficaBarrasApiladas` / `construirGraficaBarrasApiladasDesplazable` (un tramo por materia) + `construirLeyendaMaterias`. **Tiempo real:** `obtenerSesionEnCursoVirtual()` (la sesión que lleva el timer, solo fases de estudio, congelada en pausa) + `obtenerSesionesParaGraficas()` (guardadas + virtual, descartando la virtual si ya existe la guardada con mismo `inicio`); `montarSeccion` / `repintarSeccionesEnVivo` reemplazan cada sección en el sitio; un único suscriptor (`suscribirseATimer`) repinta al cambiar el minuto entero y el listener de `te:sesiones-actualizadas` repinta al guardar. `calcularMinutosTotalesEnRango(inicio, fin, incluirEnCurso = false)` y `calcularHorasTotalesPeriodos(ahora, incluirEnCurso = false)` ganan el parámetro opt-in (la pantalla pasa `true`; competencias nunca). `notificarSesionesEstudioActualizadas` se sigue exportando (re-export desde el timer). `calcularMetaDiariaMateria` ahora ve la sesión en curso del día de hoy (vía `calcularMinutosMateriaEnRango`), lo que no altera el reparto de la meta (solo cuentan los días ya pasados).
* **`obtenerRangoSemana(offsetSemanas, ahora = new Date())`** - se le agregó el 2º parámetro opcional (compatible: los llamadores existentes pasan solo el offset).
* **`fechaLocalStr(fecha = new Date())`** *(interna, 2026-09-19, Parte B)* - "YYYY-MM-DD" en hora LOCAL. Reemplaza los 2 usos de `new Date().toISOString().slice(0, 10)` (UTC: en Costa Rica desde las 6 pm devolvía MAÑANA) en `obtenerIndiceSemestreVigente` y en el corte "Día" del donut. Además `fechaDiaDonut` ya no queda fija desde la primera apertura: solo se respeta si la persona la eligió a mano (`fechaDiaDonutElegidaAMano`); si no, cada render usa hoy.
* `construirEstadisticasMateria(cont, mm, color, refrescar)` - **(2026-09-07, pedido explícito; 2026-09-08 se le sacó el parámetro `semestre` que había tenido brevemente, ver nota más abajo)** a diferencia de `construirVistaEstadisticas` (agregado de TODAS las materias, vive en el pill superior "Estadísticas"), esto se llama desde `construirPantallaDetalle` de `tiempo-estudio.js`, DENTRO del detalle de una materia puntual, siempre filtrado por `materia_matriculada_id` - cada repetición de una materia tiene sus propias gráficas. `color` es el mismo color efectivo que ya resuelve `obtenerColorMateria()` en `tiempo-estudio.js` (propio > categoría > default), recibido por parámetro para no duplicar esa cadena de fallbacks acá. **Fix 2026-09-08 (aislamiento de errores):** las 3 secciones (`construirSeccionResumenMetas`, `construirSeccionBarrasMateria`, `construirSeccionResumenFinal`) ahora corren cada una en su propio `try/catch` - antes, si una tiraba una excepción, el `refrescar()` entero se cortaba ahí y las secciones siguientes (con sus pills/navegadores) se quedaban con handlers viejos sin re-atachar, dando la sensación de pantalla "congelada". Ahora, si una sección falla, las otras 2 se renderizan igual y la que falló muestra un aviso en vez de dejar todo a medio actualizar.
  1) `construirSeccionResumenMetas` - gráfica de líneas (`construirGraficaLineas`, generaliza a N series el mismo sistema de coordenadas de `construirGraficaBarras`) con el trabajado de cada día de ESTA semana en el color propio de la materia, contra la meta diaria en línea gris punteada, más 2 contadores debajo con el total semanal de cada línea; si la materia no tiene meta configurada, muestra un mensaje en vez de la gráfica. **2026-09-08:** ganó su propio navegador `‹ Semana N / rango ›` (antes solo mostraba la semana actual fija, sin forma de navegar - `offsetSemanaMetas`, mismo patrón que ya tenía "Horas trabajadas"). **2026-09-09 ("Entrega 3"):** la línea de meta diaria dejó de ser `meta_horas_semana / 7` pareja - ver `calcularMetaDiariaMateria()` más abajo.
  2) `construirSeccionBarrasMateria` - mismo pill Semana/Semestre y navegador `‹ ›` que la tendencia global, pero filtrada a esta materia (`calcularMinutosMateriaEnRango`) y con las barras pintadas del color propio de la materia (antes solo existía en violeta genérico, agregado de todas las materias).
  3) `construirSeccionResumenFinal` - 4 totales de siempre (todo el historial, sin filtrar por período): horas totales estudiadas, día más productivo (agrupa sesiones por fecha local y toma el máximo), sesiones totales, sesión promedio.
* `calcularMetaDiariaMateria(mm, offsetSemana = 0)` - **(nuevo, 2026-09-09, "Entrega 3")** calcula el reparto REAL de la meta diaria de una materia para la semana de `offsetSemana`: respeta `mm.tiempo_estudio.dias_estudio` (`null` = todos los días) y pone en 0 la meta de los días que ya pasaron (antes de hoy) - lo que falta de la meta semanal (total menos lo YA trabajado en días pasados, cuenten o no como "día de estudio") se reparte en partes iguales entre los días de estudio pendientes (hoy inclusive). Devuelve `null` si la materia no tiene meta configurada. Exportada para que `obtenerEstudioParaHoy()` de `tiempo-estudio.js` (el widget que alimenta a Agenda) use EXACTAMENTE el mismo número que "Resumen de metas" - sin esto, Agenda y Tiempo de Estudio podrían mostrar dos metas distintas para el mismo día. Reemplaza el reparto plano `meta_horas_semana / 7` que usaban antes tanto esta sección como `obtenerEstudioParaHoy()`.
* `construirGraficaBarras(puntos, color)` - **(firma ampliada 2026-09-07)** el segundo parámetro es opcional (cae a `COLOR_BARRA_TOTAL` si no se pasa, mismo comportamiento de antes para la tendencia global) - lo usa `construirSeccionBarrasMateria` para pintar las barras del color propio de cada materia en vez del violeta genérico.

**Semana del semestre, no semana ISO del año (2026-09-08, 2 vueltas):** el navegador `‹ Semana N / rango ›` (donut, barras globales, y las 2 secciones por materia) originalmente calculaba "Semana N" con una fórmula ISO-8601 propia (semana del AÑO calendario, ej. "Semana 36") - pedido explícito: tiene que ser la semana DENTRO del semestre, como ya muestran Horario y Agenda. Primer intento: se le agregó un parámetro `fechaReferencia` a `calcularNumeroSemanaSemestre()` (core/schema.js, hasta entonces solo resolvía "hoy") - **este intento quedó revertido en la misma ronda**, porque duplicaba (con el mismo bug de zona horaria ya resuelto ahí) una función que YA existía para justo este caso: `calcularNumeroSemanaParaFecha(semestre, fecha)` de `agenda/agenda-clases.js` (fecha arbitraria, acotada 1..duracion_semanas, delega el cálculo crudo en `horario/horario.js#calcularNumeroSemanaSinAcotarParaFecha`, que ya tiene resuelto el anclaje correcto al día real de `fecha_inicio`, no asume que caiga lunes). Se usa esa en cambio. `core/schema.js#calcularNumeroSemanaSemestre(semestre)` quedó tal cual estaba (solo resuelve "hoy", sin el parámetro agregado).

Segundo bug relacionado, distinto (mismo día): por materia puntual, "Semana N" daba un número incorrecto y quedaba pegado en ese número sin importar cuánto se navegara para atrás - root cause: `construirSeccionResumenMetas`/`construirSeccionBarrasMateria` le pasaban a `calcularNumeroSemanaParaFecha` el `semestre` específico al que esa `mm` está matriculada (`item.semestre`, resuelto en `tiempo-estudio.js` vía `obtenerSemestresActuales()`), en vez del semestre VIGENTE por fecha (que es lo que ya usaban, y ya andaba bien, el donut y "Horas trabajadas" global vía `obtenerSemestreVigenteParaSemana()`, interno de este archivo). Si la matrícula queda ligada a un objeto de semestre distinto del vigente, la cuenta se hacía bien pero contra el `fecha_inicio` equivocado. Fix: las 2 secciones por materia pasaron a usar también `obtenerSemestreVigenteParaSemana()` - un solo criterio de "qué semestre es este" para las 4 secciones, sin depender de a qué matrícula quedó ligada cada materia. Se sacó por completo el parámetro `semestre` que se le había agregado de paso a `construirEstadisticasMateria`/`construirSeccionResumenMetas`/`construirSeccionBarrasMateria` (y el threading correspondiente en `tiempo-estudio.js`) - ya no hacía falta.

**Mejora 2026-09-07 (diagnóstico del reporte "en modo semestre no muestra nada"):** revisando el código no había ningún bug de renderizado - el motivo real es que una gráfica con TODAS las barras en 0 minutos (semestre vigente sin sesiones cargadas todavía) se ve indistinguible de "no carga nada". Se agregó: 1) un guard en `construirGraficaBarras`/`construirGraficaLineas` contra `puntos.length === 0` (antes producía coordenadas `NaN` y un SVG invisible si alguna vez llegaba un arreglo vacío - no debería pasar con datos de semestre válidos, pero ahora cae a un mensaje en vez de romperse en silencio); 2) un aviso explícito ("Sin sesiones registradas en este período - probá con el navegador ‹ › de arriba") debajo de la gráfica de barras (global y por materia) cuando el período elegido no tiene ninguna sesión, para que quede claro que la gráfica funciona pero está vacía, no rota.

Notas internas (no exportadas): el estado de qué corte/período está seleccionado (`corteDonut`, `offsetSemanaDonut`, `fechaDiaDonut`, `indiceSemestreDonut`, y su equivalente `*Barras` para la 2ª gráfica) vive a nivel de módulo, igual que `materiaDetalleActivaId` en `tiempo-estudio.js` - no se persiste ni sincroniza, es solo "qué estás mirando ahora". El bloque nuevo por materia tiene su propio estado de módulo aparte (`corteBarrasMateria`, `offsetSemanaBarrasMateria`, `indiceSemestreBarrasMateria`) para no pisar el de la vista global si las dos llegaran a estar montadas (no pasa hoy, pero evita un bug si el detalle alguna vez convive con la vista de Estadísticas). `construirDonutHorasPorMateria` generaliza a N segmentos el truco de `stroke-dasharray`/`stroke-dashoffset` acumulado que `finanzas-graficas.js` usa para 2; `construirGraficaBarras`/`construirGraficaLineas` reutilizan el mismo algoritmo de "nice numbers" del eje Y (`calcularEscalaAgradable`) que ese archivo, adaptado a minutos en vez de montos.

---

### tiempo-estudio/tiempo-estudio-racha.js
Propósito: **(2026-09-19 - nuevo)** el CÁLCULO de la racha de estudio. **Puro a propósito: sin imports** (ni `estado`, ni DOM); recibe las sesiones y el "ahora" por parámetro, así se prueba en Node con cualquier fecha (mismo criterio que `calcularHorasTotalesPeriodos`). La racha es DERIVADA: no se guarda ningún número, se recalcula desde `sesiones_estudio`, así editar/borrar/cargar una sesión vieja nunca la desincroniza.
Depende de: nada.
Exporta:
* `calcularRacha(sesiones, { ahora = Date.now(), desdeIdx = null })` - devuelve `{ racha, activa, hoyIdx, hoyCumplido, minutosHoy, inicioIdx, inicioISO, descansosUsados, descansosRestantes, enRiesgo, restauradaHoy, recuperable }`. Una pasada O(N) agrupa minutos por día LOCAL (sesión entera en el día donde EMPIEZA, igual que "Horas totales"; ignora `inicio` corrupto, duración ≤0 y sesiones "del futuro") y luego recorre día por día desde el primero con datos hasta hoy. `enRiesgo` = ya gastó los 2 descansos de la semana y hoy no cumplió. `recuperable` = `{ longitud, minutosNecesarios, minutosHoy }` solo el día siguiente a la ruptura de una racha ≥30.
* `minutosSemana(sesiones, { ahora })` - **(seguimiento)** los 7 días lun→dom de la semana de `ahora` como `[{ idx, min }]` (futuros = 0); alimenta la tira de la ventana de detalle. Lee las sesiones con las MISMAS reglas que `calcularRacha` (ambas usan `agruparMinutosPorDia`, interna).
* `MINUTOS_DIA_CUMPLIDO` (30), `DESCANSOS_POR_SEMANA` (2), `MINUTOS_PARA_RECUPERAR` (90), `RACHA_MINIMA_RECUPERABLE` (30), `RACHA_DESDE_ISO` (`"2026-09-14"`, ver changelog).
* `indiceDia(ms)` / `indiceDesdeISO(iso)` / `isoDesdeIndice(idx)` - el día se maneja como ENTERO (días desde 1970 del calendario LOCAL, vía `Date.UTC(año, mes, día)`), nunca con `toISOString()` (bug UTC de Costa Rica) ni con restas de milisegundos (cambio de horario de verano).
Notas: **consecuencia conocida** de "2 descansos por SEMANA": 2 al final de una semana y 2 al inicio de la siguiente (4 seguidos) no rompen la racha; si se quisiera evitar, se agrega un tope de días seguidos en el recorrido. Semana = lun–dom fijo (igual que el resto de Tiempo, no respeta todavía el "día de inicio de semana" configurable de Horario).

### tiempo-estudio/tiempo-estudio-racha-ui.js
Propósito: **(2026-09-19 - nuevo)** interfaz y coordinación de la racha: lee `estado.datos.sesiones_estudio`, dibuja el chip del encabezado, los 3 avisos a pantalla completa y decide CUÁNDO celebrar.
Depende de: core/storage.js, core/storage-sync.js (`marcarCambioPendiente`), ui/componentes.js (`mostrarToast`), tiempo-estudio/tiempo-estudio-racha.js. **No importa `tiempo-estudio.js`** (repinta vía `window.renderizarTiempoEstudio`, mismo patrón que main.js) - sin import circular.
Exporta:
* `obtenerEstadoRacha(ahora)` - `calcularRacha` con `estado.datos.sesiones_estudio` y el corte `RACHA_DESDE_ISO`.
* `describirEstadoRacha(est)` - frase corta (tooltip, `aria-label` del chip y toast al tocarlo).
* `construirChipRacha()` - botón 🔥 + número (clases `--encendida` / `--pendiente` (+`--riesgo`) / `--apagada` (+`--recuperable`)). Si hay una animación pendiente (`animacionChip`, vence a los 5 s) estrena el pop + odómetro n-1→n. Al tocarlo llama a `abrirDetalleRacha()` (ya no lanza un toast).
* `construirOdometro(desde, hasta, { retrasoMs, giro })` - dígitos como columnas con una tira 0–9 que rueda (solo se mueven las columnas que cambian; una columna nueva, 9→10, entra con fundido; `giro` suma una vuelta). Con "reducir movimiento" salta al valor final.
* `abrirOverlayRacha({ tipo, est, desde, hasta })` - `"detalle"` (al tocar el chip: llama viva si hay racha, número 0→n, semana día por día, filas Hoy / Esta semana / Días de descanso restantes / Racha iniciada; título y texto según el estado; sin racha usa `te-racha-card--apagada`), `"inicio"` (reglas + casilla "No volver a mostrar"), `"recuperada"` y `"oferta"` (llama apagada + barra de avance hacia 90 min). No se cierra tocando fuera (como el resto de modales de Tiempo): botón o Escape; trampa de foco mínima.
* `abrirDetalleRacha()` - **(seguimiento)** handler del chip: si `est.recuperable` abre `"oferta"`, si no `"detalle"`.
* `alFusionarDatosRacha()` - tras CADA fusión: refresca en silencio la referencia (lo que llega de otro dispositivo nunca celebra) y hace UNA revisión al abrir por día. `inicializarRacha()` - cablea `te:sesiones-actualizadas` y `visibilitychange` (app abierta de un día para otro).
Preferencias (en `configuracion`, ver schema.js; **por dispositivo en la práctica**: `fusionarBloqueUnico` funde `configuracion` entera por `_actualizadoEn` y, sin sellar, gana el local en empate - como `mostrar_tiempo_estudio_en_agenda`): `racha_aviso_inicio_oculto`, `racha_ultimo_inicio_avisado` (ISO del día en que empezó la racha ya anunciada → el aviso sale una vez por racha). La oferta de recuperar se limita a 1 vez/día **por dispositivo** con `localStorage` (`te_racha_oferta_dia_v1`), a propósito: no hay nada que sincronizar.
Notas: CSS en `design-system.css`, bloque "Racha de estudio" (`.te-racha-*`, `.te-llama*`, `.te-odo*`); los colores de la LLAMA son hex fijos a propósito, todo lo demás sale de variables de paleta. z-index del overlay: 330.

## JS - resumen

### resumen/resumen.js
Propósito: primera sección de la app - vista de solo lectura que agrega Semana actual, Clases de hoy, Tareas de hoy, Próximas tareas, Exámenes próximos y Próximo evento, leyendo directo de `estado.datos.agenda` y reutilizando funciones ya existentes de `agenda/agenda-utils.js`, `agenda/agenda-clases.js`, `agenda/agenda.js` (`construirItemEvento`) y `core/schema.js`. Sin modelo de datos propio, sin modales, sin edición - tocar un ítem abre la tarjeta de info que ya existe en Agenda (`abrirTarjetaInfoEventoAgenda`, llamada indirectamente vía `construirItemEvento`).
Depende de: core/schema.js, core/storage.js, agenda/agenda.js, agenda/agenda-clases.js, agenda/agenda-utils.js
Exporta:
* `inicializarResumen()` - no-op por ahora (Resumen no tiene controles propios que cablear); se mantiene por consistencia con el patrón `inicializarX()`/`renderizarX()` que `main.js` espera de cada sección.
* `renderizarResumen()` - entrypoint de render (**2026-09-21:** cada ítem de Exámenes próximos / Tareas de hoy / Tareas vencidas / Próximas tareas / Próximo evento lleva antes de su tarjeta una fila con la fecha real a la izquierda y, a la derecha, `formatearFechaRelativa`; ya no hay encabezado de grupo "MAÑANA". Las tareas perdidas no aparecen en ninguna lista; al final hay una tarjeta "Tarea(s) perdida(s)" con el conteo si hay ≥1): reconstruye las 6 secciones de golpe, ocultando cada una si no tiene contenido, y mostrando un mensaje de "día tranquilo" si ninguna tiene nada. También expuesta como `window.renderizarResumen` (mismo patrón que `window.renderizarAgenda`/`window.renderizarHorario`) para que otros módulos puedan refrescar Resumen tras guardar datos, sin import circular.

---

## js/main.js

Propósito: orquestador raíz de la app - arranque, login/logout, navegación entre secciones y renderizado del perfil de usuario. Importa e inicializa el resto de módulos.
Depende de: config-ajustes.js, config-enlaces.js, auth.js, notificaciones-push.js **(2026-08-25: pendiente de cambiar a notificaciones-calendario.js - no actualizado en esta sesión, archivo no subido)**, schema.js, storage-merge.js, storage-sync.js, storage.js, utils.js, comunidad.js, finanzas.js, plan-categorias.js, plan-detalle.js, plan-esquema.js, plan-gestionar.js, plan-importacion.js, plan-vista-lista.js, semestres.js, agenda.js, horario.js, horario-amigos.js, componentes.js, tema.js
Exporta:
* `programarAvisoLoginBloqueado()` - arma un timeout de 6s que muestra el aviso "no se pudo abrir el login" (VPN/bloqueador de anuncios/extensión de privacidad).
* `ocultarAvisoLoginBloqueado()` - cancela ese timeout y oculta tanto el aviso de login bloqueado como el de permiso rechazado.
* `onLoginExitoso(token, expiresIn)` - flujo posterior a un login exitoso de Google: guarda el token activo, resuelve `authListo`, pide almacenamiento persistente al navegador, continúa la carga de datos y, si la cuenta es nueva (`esArchivoNuevo`), ofrece activar la sincronización con Google Calendar (`ofrecerActivarSincronizacionCalendario`, core/notificaciones-calendario.js - 2026-08-25, reemplaza a `ofrecerActivarNotificacionesPush`; este archivo no se tocó en la sesión de la migración, actualizar el import).
* `mostrarApp()` - oculta la pantalla de login y muestra el shell de la app; aplica la paleta guardada y las preferencias locales de tema/diseño, renderiza el selector de plan y, si la URL trae `?abrir=agenda` (llegó de tocar una notificación push), salta directo a la sección Agenda. Al final llama a `revisarUniversidadesIncompletas()`.
* `inicializarModalCompletarUniversidades()` (2026-08-22) - registra el listener del botón "Guardar" de `#modal-completar-universidades`; guarda nombre_completo+siglas de cada plan incompleto y solo se habilita cuando todas las filas tienen ambos campos llenos. Desde 2026-09 revisa cada fila con `confirmarUniversidadNoInvertida` (ui/componentes.js) ANTES de escribir nada en los planes, así el guardado sigue siendo de una sola vez.
* `revisarUniversidadesIncompletas()` (2026-08-22) - recorre `estado.datos.planes_estudio` buscando planes con `universidad.siglas === ""` (dejados así por `migrarDatosAntiguos` al migrar un `universidad` string viejo); si encuentra alguno, arma las filas dinámicas en `#lista-completar-universidades` y abre el modal bloqueante `#modal-completar-universidades` (excluido del cierre automático por "X"/click-afuera en componentes.js - no se puede posponer). Se llama al final de `mostrarApp()`.
* `pedirConfirmacionCerrarSesion()` - cierra el popover de perfil; si hay cambios sin sincronizar pide confirmación antes de cerrar sesión, si no cierra directo. **(2026-09-17, punto 3.5 de la auditoría, bug real)** antes solo miraba `estado.pendienteSync` - una sesión de estudio EN CURSO (timer corriendo) no vive todavía en `estado.datos` (recién la escribe `detenerTimerEstudio`, tiempo-estudio-timer.js), así que se podía cerrar sesión con el cronómetro corriendo sin ningún aviso, perdiendo lo estudiado hasta ese momento. Ahora también consulta `window.hayTimerActivo?.()` (puente por `window`, mismo patrón que `window.renderizarTiempoEstudio` - evita un import nuevo entre main.js y tiempo-estudio-timer.js) y muestra un mensaje distinto según haya cambios pendientes, timer corriendo, o ambos.
* `cerrarSesion()` - limpia token, caché local y estado en memoria; vuelve a mostrar la pantalla de login. **(2026-09-17, punto 3.4)** ahora llama a `avisarCierreSesionAOtrasPestanas()` (storage-sync.js) ANTES de borrar nada, para que otras pestañas de este mismo navegador dejen de reintentar sync con un token que ya está muerto.
* Bloque de arranque en `DOMContentLoaded` *(no exportado)* - **(2026-09-17)** ahora también llama a `inicializarCanalEntrePestanas()` (storage-sync.js) junto con `inicializarSondeoAlVolver()`/`inicializarReconexionAlVolverOnline()`, para dejar armado el `BroadcastChannel` entre pestañas del mismo navegador desde el arranque.
* `CLAVE_SECCION_ACTIVA` - clave de `localStorage` (`"seccion_activa_v1"`) donde se persiste qué sección de navegación quedó activa.
* `inicializarNavegacionSecciones()` - conecta los botones `.btn-nav[data-seccion]` para que llamen a `mostrarSeccion`.
* `mostrarSeccion(nombre, { desdeHistorial })` - cambia y persiste la sección visible; cada cambio de sección agrega un estado con History API (`pushState`) y `popstate` vuelve a la sección anterior. Solo las secciones son pasos del historial; abrir/cerrar modales o detalles no agrega entradas. Entra también en `asistente` llamando a `window.renderizarAsistente?.()`.
* `aplicarVisibilidadBotonAsistente()` (revisado 2026-08-22) - disparador delgado: llama a `aplicarVisibilidadNavegacion()` (recalcula mostrar/ocultar/orden de TODO el nav, incluyendo el gate de existencia de "asistente") y a `window.renderizarNavegacionOculta?.()` (config-ajustes.js, refresca en vivo la lista de switches de Ajustes > Navegación si está montada). Se llama desde `mostrarApp()` y desde `inicializarAsistenteAjustes()` (config-ajustes.js) cada vez que se guarda/borra la clave.
* `aplicarVisibilidadNavegacion()` (main.js, revisado 2026-08-22) - muestra/oculta/reordena todos los `.btn-nav[data-seccion]` según `configuracion.navegacion_oculta`/`navegacion_orden`. "asistente" participa de este mismo sistema como cualquier otra sección (está en `DEFAULT_ORDEN_NAV` y en `SECCIONES_TOGGLEABLES`), con un gate de EXISTENCIA adicional por encima: se oculta sin importar `navegacion_oculta` si no hay `configuracion.gemini_api_key` guardada. La preferencia de orden/visibilidad del usuario para "asistente" nunca se pierde, solo se re-aplica en cuanto vuelve a haber clave.
* `renderizarNavegacionOculta()` (config-ajustes.js, revisado 2026-08-22) - dibuja la lista de switches arrastrables de Ajustes > Navegación a partir de `SECCIONES_TOGGLEABLES` y `window.obtenerOrdenNavegacion()`. Omite la fila de "asistente" si no hay `configuracion.gemini_api_key` (mismo gate de existencia que el nav real), sin tocar su preferencia guardada. Expuesta en `window` para que `aplicarVisibilidadBotonAsistente()` (main.js) la refresque sin import circular.
* **`SECCIONES_TOGGLEABLES` (2026-09-09):** incluye Tiempo (`id: "tiempo-estudio"`), confirmado contra el botón real de navegación de esta versión. No requiere gate de existencia.
* `temporizadorAvisoLogin` - id del `setTimeout` de `programarAvisoLoginBloqueado`, expuesto para que otros módulos puedan limpiarlo (ej. al cerrar sesión).
* `renderizarPerfil()` - pinta nombre, correo, foto/iniciales del perfil en el header y en el popover; maneja el fallback si la foto no carga.
* `togglePerfilPopover(forzarCerrado)` - abre/cierra el popover de perfil (o lo fuerza a cerrado si se pasa `true`).
* Listener de `serviceWorker.addEventListener("message", ...)` - si el service worker avisa que se tocó una notificación push con la app ya abierta en una pestaña, salta a la sección Agenda sin recargar.

---

## Patrones transversales (todo el proyecto)

- **Sincronización:** todo objeto persistido usa reloj de Lamport (`sellarTimestamp`/`observarRelojLogico` en `core/schema.js`) - nunca `Date.now()` directo. `_version_base` guarda el contador previo para que `storage-merge.js` distinga edición secuencial de conflicto real.
- **Borrado = tumba:** nunca se hace `delete` directo - cada colección plana o anidada que se puede borrar tiene su propio arreglo `_eliminados_<coleccion>`, inicializado explícito desde `crearDatosUsuarioNuevo()`. Un borrado se agrega ahí y se filtra del arreglo vivo, para que el merge de sync sepa que fue intencional y no lo "resucite" desde un dispositivo desactualizado.
  - **Excepción documentada (2026-09-19): ids que se REUTILIZAN.** La regla "la tumba gana siempre" da por hecho que cada entidad tiene un `id` único de por vida. `competencias_unidas` la rompe: su `id` es el de la competencia, y salir + volver a unirse lo reutiliza - la tumba vieja mataba la alta nueva en cada sync. Solución: la alta lleva `unido_en` (Date.now()) y `fusionarDatos` aplica `podarTumbasSuperadasPorAltas` a esa colección antes de fusionar. **Si se agrega otra colección cuyo `id` pueda reutilizarse (ej. "seguir a un usuario", "unirse a un grupo"), NO usar el id del objeto externo como `id` de la entrada, o replicar este mecanismo.**
- **Fusión de colecciones:** cualquier colección nueva con `id` propio se funde con `fusionarColeccion()` genérica (`storage-merge.js`) - no escribir lógica de fusión nueva por tipo de entidad salvo que tenga sub-colecciones anidadas propias (ver `fusionarPlan`/`fusionarSemestre`/`fusionarCriterio`/`fusionarBloqueHorario` como patrón a replicar).
- **Relectura de entidad viva:** antes de mutar algo que vino de un closure (ej. abierto en un modal), se vuelve a buscar por id en `estado.datos` - un sondeo remoto puede haber reemplazado el objeto mientras tanto.
- **Límite de 800 líneas por archivo:** si un archivo se acerca al límite, se separa por responsabilidad (ver cómo se partió Agenda en núcleo/utils/clases/modal/calendario, y Storage en storage/storage-sync/storage-merge/storage-adjuntos). Archivos que ya superan el límite y son candidatos a dividirse en un próximo prompt: `comunidad/comunidad.js` (3948), `semestres/semestres-tarjetas.js` (~3540), `horario/horario.js` (2064), `horario/horario-modal.js` (1038).
- **Imports circulares intencionales:** varios módulos se importan entre sí en ambas direcciones a propósito (ver lista completa y la razón de cada uno en `ARQUITECTURA.md`, sección "Imports circulares"). Es seguro en módulos ES mientras el nombre importado solo se use *dentro* de una función, nunca en el nivel superior del archivo. Si una IA nueva ve esto y quiere "arreglarlo" separando más archivos, no hace falta - ya funciona así a propósito.
- **Worker de Cloudflare - Competencias (2026-09-17, Partes 4.4 y 5):** además de lo que ya existía (`POST /competencias`, `GET /competencias/:id`, `POST .../unirse`, `POST .../actualizar-horas`, `DELETE /competencias/:id`, `DELETE .../participantes/:pid`, `GET .../historial`, `POST .../delegar-borrado`), ahora hay: `POST /competencias/:id/renombrar` (token_creador), `POST /competencias/:id/estado` (token_creador, `"activa"|"finalizada"`, idempotente), `POST /competencias/:id/participantes/:pid/apodo` (identificador_usuario, solo el propio), y `GET /competencias/:id/resumen` (competencia + historial completo sin LIMIT + participantes con su acumulado de por vida, todo en un viaje). `DELETE .../participantes/:pid` pasó a aceptar **`token_creador` como autorización alternativa** a `identificador_usuario`, que es lo que habilita "Sacar usuario" sin abrir una segunda ruta que borre participantes. **2 columnas D1 nuevas** (`migracion_competencias_gestion.sql`, aplicar ANTES de desplegar el Worker nuevo): `competencias.estado` y `participantes.horas_totales_historicas`. Una competencia `finalizada` no acepta gente nueva (`/unirse` → 403), no guarda horas (`/actualizar-horas` responde 200 pero ignora, para no ensuciar la consola de un endpoint que es best-effort a propósito) y el cron la saltea; **nada se borra**, es reversible. Al finalizar se hace un cierre de semana final (para no perder la última semana parcial justo al archivar); al reactivar se arranca semana limpia (horas en 0 y `ultimo_cierre_en` al lunes actual, si no el cron cerraría de golpe todas las semanas que pasó archivada).
- **Worker - `/unirse` idempotente (2026-09-19, Parte A, estado fantasma):** `POST /competencias/:id/unirse` ya NO responde 409 cuando la persona (mismo `identificador_usuario`) ya tiene fila: hace `UPDATE` de `apodo`/`offset_minutos_utc` y devuelve `200 { participante_id: <el que ya tenía>, ya_estaba: true }`. El `INSERT` fallido se confirma con un `SELECT` antes de asumir duplicado (un error de otro tipo responde 500, no se disfraza de "ya estás"). Motivo: una fila "fantasma" (ej. el DELETE de salir nunca llegó) dejaba a la persona sin poder volver a unirse ni salir, porque el cliente no tenía forma de recuperar ese `participante_id`. Las horas de la fila existente no se tocan (el cliente llama a `/actualizar-horas` al unirse). La finalizada sigue devolviendo 403. Sin cambios de esquema ni migración. **Hay que redesplegar el Worker.**
- **Worker - `GET /identificadores/:correo/competencias` (2026-09-22, recuperar historial perdido):** único endpoint que busca por PERSONA en vez de por `id` de competencia puntual - `SELECT` con JOIN de `participantes`/`competencias` filtrado por `identificador_usuario`, devuelve `{ competencias: [{ id, nombre, estado, participante_id, apodo }] }` de TODAS las que esa persona integra o integró, activas y finalizadas por igual. De solo lectura, no toca D1. Existe porque el cliente solo guarda un puntero local (`competencias_unidas`) de "a qué estoy unido" y, si ese puntero se pierde, ningún otro endpoint tiene forma de encontrar de nuevo esas competencias (todos piden el `id`, que es justo lo perdido). Lo consume `recuperarCompetenciasPerdidas()` en `tiempo-estudio-competencias-gestion.js`. Sin cambios de esquema ni migración. **Hay que redesplegar el Worker.**
- **Worker - blindaje de concurrencia D1 (2026-09-17, auditoría Partes 4.2/4.3/4.5), sin cambios de contrato para el cliente:**
  - **`procesarCierreCompetencia` (punto 4.2, el hallazgo más serio del lado del Worker):** el creador finalizando una competencia justo cuando el cron corre el cierre horario sobre la MISMA fila podía duplicar la entrada de `historial_ganadores` y sumar horas dos veces a `horas_totales_historicas` - las dos rutas (cron y el endpoint `estado`) hacían "leer participantes en JS, decidir, escribir" sin nada que las serializara entre sí. Ahora usa una **guarda optimista (compare-and-swap)** sobre `ultimo_cierre_en`: el llamador (cron o el endpoint) pasa el valor que leyó momentos antes, y las 3 sentencias del cierre (INSERT historial condicional vía `SELECT ... WHERE`, UPDATE de pliegue de horas, UPDATE de avance de `ultimo_cierre_en`) llevan la MISMA condición `ultimo_cierre_en = valorLeido`, todas dentro de un único `.batch()` (una sola transacción D1). Quien pierde la carrera ve las 3 sentencias afectar 0 filas - nunca corre a medias. A propósito NO se ata al chequeo `lunesUtcMs > ultimo_cierre_en` que sí usa el cron para decidir SI debe cerrar (eso habría roto el cierre final de "Finalizar" plegando horas sueltas fuera de una vuelta semanal nueva). Además, `manejarCambiarEstadoCompetencia` reclama `estado` de forma atómica (`UPDATE ... WHERE estado != 'finalizada'`) ANTES de llamar a `procesarCierreCompetencia`, como defensa extra contra el mismo creador finalizando desde 2 dispositivos casi al mismo tiempo (esto se suma a la guarda de arriba, no la reemplaza).
  - **`manejarSalirCompetencia` (punto 4.3):** el `DELETE` (ambas vías de autorización, `token_creador` e `identificador_usuario`) devolvía 404 con `error` cuando el participante ya no existía. El caso real es que el creador saque a alguien justo cuando esa persona toca "Salir" sola - el que llega segundo, sea cual sea, encontraba la fila ya borrada. Ahora `changes === 0` responde `200 { ok: true, ya_no_estaba: true }`: el DELETE es idempotente de verdad. Confirmado en el cliente (`tiempo-estudio-competencias.js`/`tiempo-estudio-competencias-gestion.js`) que un 404 ya se toleraba como éxito en ambos flujos, así que el cambio es compatible sin tocar el cliente.
  - **`manejarActualizarHoras` (punto 4.5):** el chequeo de `estado === "finalizada"` vivía en un `SELECT` separado del `UPDATE` de las horas - si la competencia se finalizaba justo en el medio de esas dos llamadas, las horas se escribían igual (el filtro del lado del cliente no alcanza por sí solo, porque decide con una foto de antes de que el servidor cambiara de estado). Ahora es un único `UPDATE ... WHERE id = ? AND (SELECT estado FROM competencias WHERE id = ?) != 'finalizada'` - sin ninguna ventana entre leer y escribir.
- **Worker de Cloudflare (proyecto separado, `worker-notificaciones-agenda`):** 2026-08-25 - se le quitó TODO lo de Web Push/recordatorios/resumen diario/D1/Cron; solo le queda el relevo de OAuth (`/oauth/exchange`, `/oauth/refresh`). La sincronización de Agenda con Google Calendar ya no pasa por él - el cliente habla directo con la API de Calendar (ver `core/notificaciones-calendario.js`, `core/auth.js`).
- **Índice vivo:** cualquier prompt que cree un archivo nuevo o agregue/quite funciones exportadas a uno existente debe actualizar también su entrada correspondiente acá, en `MAPA_FUNCIONES.md` (y en `ARQUITECTURA.md` si cambia una capa o el cheat-sheet de "¿dónde va cada cosa?").

